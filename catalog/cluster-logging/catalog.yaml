---
defaultChannel: stable-6.3
icon:
  base64data: 
  mediatype: image/svg+xml
name: cluster-logging
schema: olm.package
---
entries:
- name: cluster-logging.v5.8.0
  skipRange: '>=5.6.0-0 <5.8.0'
- name: cluster-logging.v5.8.1
  replaces: cluster-logging.v5.8.0
  skipRange: '>=5.6.0-0 <5.8.1'
- name: cluster-logging.v5.8.2
  replaces: cluster-logging.v5.8.1
  skipRange: '>=5.6.0-0 <5.8.2'
- name: cluster-logging.v5.8.3
  replaces: cluster-logging.v5.8.2
  skipRange: '>=5.6.0-0 <5.8.3'
- name: cluster-logging.v5.8.4
  replaces: cluster-logging.v5.8.3
  skipRange: '>=5.6.0-0 <5.8.4'
- name: cluster-logging.v5.8.5
  replaces: cluster-logging.v5.8.4
  skipRange: '>=5.6.0-0 <5.8.5'
name: stable
package: cluster-logging
schema: olm.channel
---
entries:
- name: cluster-logging.v5.8.0
  skipRange: '>=5.6.0-0 <5.8.0'
- name: cluster-logging.v5.8.1
  replaces: cluster-logging.v5.8.0
  skipRange: '>=5.6.0-0 <5.8.1'
- name: cluster-logging.v5.8.2
  replaces: cluster-logging.v5.8.1
  skipRange: '>=5.6.0-0 <5.8.2'
- name: cluster-logging.v5.8.3
  replaces: cluster-logging.v5.8.2
  skipRange: '>=5.6.0-0 <5.8.3'
- name: cluster-logging.v5.8.4
  replaces: cluster-logging.v5.8.3
  skipRange: '>=5.6.0-0 <5.8.4'
- name: cluster-logging.v5.8.5
  replaces: cluster-logging.v5.8.4
  skipRange: '>=5.6.0-0 <5.8.5'
- name: cluster-logging.v5.8.6
  replaces: cluster-logging.v5.8.5
  skipRange: '>=5.6.0-0 <5.8.6'
- name: cluster-logging.v5.8.7
  replaces: cluster-logging.v5.8.6
  skipRange: '>=5.6.0-0 <5.8.7'
- name: cluster-logging.v5.8.8
  replaces: cluster-logging.v5.8.7
  skipRange: '>=5.6.0-0 <5.8.8'
- name: cluster-logging.v5.8.9
  replaces: cluster-logging.v5.8.8
  skipRange: '>=5.6.0-0 <5.8.9'
- name: cluster-logging.v5.8.10
  replaces: cluster-logging.v5.8.9
  skipRange: '>=5.6.0-0 <5.8.10'
- name: cluster-logging.v5.8.11
  replaces: cluster-logging.v5.8.10
  skipRange: '>=5.6.0-0 <5.8.11'
- name: cluster-logging.v5.8.12
  replaces: cluster-logging.v5.8.11
  skipRange: '>=5.6.0-0 <5.8.12'
- name: cluster-logging.v5.8.13
  replaces: cluster-logging.v5.8.12
  skipRange: '>=5.6.0-0 <5.8.13'
- name: cluster-logging.v5.8.14
  replaces: cluster-logging.v5.8.13
  skipRange: '>=5.6.0-0 <5.8.14'
- name: cluster-logging.v5.8.15
  replaces: cluster-logging.v5.8.14
  skipRange: '>=5.6.0-0 <5.8.15'
- name: cluster-logging.v5.8.16
  replaces: cluster-logging.v5.8.15
  skipRange: '>=5.6.0-0 <5.8.16'
- name: cluster-logging.v5.8.17
  replaces: cluster-logging.v5.8.16
  skipRange: '>=5.6.0-0 <5.8.17'
- name: cluster-logging.v5.8.18
  replaces: cluster-logging.v5.8.17
  skipRange: '>=5.6.0-0 <5.8.18'
- name: cluster-logging.v5.8.19
  replaces: cluster-logging.v5.8.18
  skipRange: '>=5.6.0-0 <5.8.19'
- name: cluster-logging.v5.8.20
  replaces: cluster-logging.v5.8.19
  skipRange: '>=5.6.0-0 <5.8.20'
- name: cluster-logging.v5.8.21
  replaces: cluster-logging.v5.8.20
  skipRange: '>=5.6.0-0 <5.8.21'
- name: cluster-logging.v5.8.22
  replaces: cluster-logging.v5.8.21
  skipRange: '>=5.6.0-0 <5.8.22'
name: stable-5.8
package: cluster-logging
schema: olm.channel
---
entries:
- name: cluster-logging.v6.0.0
  skipRange: '>=5.8.0-0 <6.0.0'
- name: cluster-logging.v6.0.1
  replaces: cluster-logging.v6.0.0
  skipRange: '>=5.8.0-0 <6.0.1'
- name: cluster-logging.v6.0.2
  replaces: cluster-logging.v6.0.1
  skipRange: '>=5.8.0-0 <6.0.2'
- name: cluster-logging.v6.0.3
  replaces: cluster-logging.v6.0.2
  skipRange: '>=5.8.0-0 <6.0.3'
- name: cluster-logging.v6.0.4
  replaces: cluster-logging.v6.0.3
  skipRange: '>=5.8.0-0 <6.0.4'
- name: cluster-logging.v6.0.5
  replaces: cluster-logging.v6.0.4
  skipRange: '>=5.8.0-0 <6.0.5'
- name: cluster-logging.v6.0.6
  replaces: cluster-logging.v6.0.5
  skipRange: '>=5.8.0-0 <6.0.6'
- name: cluster-logging.v6.0.7
  replaces: cluster-logging.v6.0.6
  skipRange: '>=5.8.0-0 <6.0.7'
- name: cluster-logging.v6.0.8
  replaces: cluster-logging.v6.0.7
  skipRange: '>=5.8.0-0 <6.0.8'
- name: cluster-logging.v6.0.9
  replaces: cluster-logging.v6.0.8
  skipRange: '>=5.8.0-0 <6.0.9'
- name: cluster-logging.v6.0.10
  replaces: cluster-logging.v6.0.9
  skipRange: '>=5.8.0-0 <6.0.10'
- name: cluster-logging.v6.0.11
  replaces: cluster-logging.v6.0.10
  skipRange: '>=5.8.0-0 <6.0.11'
- name: cluster-logging.v6.0.12
  replaces: cluster-logging.v6.0.11
  skipRange: '>=5.8.0-0 <6.0.12'
- name: cluster-logging.v6.0.13
  replaces: cluster-logging.v6.0.12
  skipRange: '>=5.8.0-0 <6.0.13'
- name: cluster-logging.v6.0.14
  replaces: cluster-logging.v6.0.13
  skipRange: '>=5.8.0-0 <6.0.14'
- name: cluster-logging.v6.0.15
  replaces: cluster-logging.v6.0.14
  skipRange: '>=5.8.0-0 <6.0.15'
name: stable-6.0
package: cluster-logging
schema: olm.channel
---
entries:
- name: cluster-logging.v6.1.0
  skipRange: '>=5.9.0-0 <6.1.0'
- name: cluster-logging.v6.1.1
  replaces: cluster-logging.v6.1.0
  skipRange: '>=5.9.0-0 <6.1.1'
- name: cluster-logging.v6.1.2
  replaces: cluster-logging.v6.1.1
  skipRange: '>=5.9.0-0 <6.1.2'
- name: cluster-logging.v6.1.3
  replaces: cluster-logging.v6.1.2
  skipRange: '>=5.9.0-0 <6.1.3'
- name: cluster-logging.v6.1.4
  replaces: cluster-logging.v6.1.3
  skipRange: '>=5.9.0-0 <6.1.4'
- name: cluster-logging.v6.1.5
  replaces: cluster-logging.v6.1.4
  skipRange: '>=5.9.0-0 <6.1.5'
- name: cluster-logging.v6.1.6
  replaces: cluster-logging.v6.1.5
  skipRange: '>=5.9.0-0 <6.1.6'
- name: cluster-logging.v6.1.7
  replaces: cluster-logging.v6.1.6
  skipRange: '>=5.9.0-0 <6.1.7'
- name: cluster-logging.v6.1.8
  replaces: cluster-logging.v6.1.7
  skipRange: '>=5.9.0-0 <6.1.8'
- name: cluster-logging.v6.1.9
  replaces: cluster-logging.v6.1.8
  skipRange: '>=5.9.0-0 <6.1.9'
name: stable-6.1
package: cluster-logging
schema: olm.channel
---
entries:
- name: cluster-logging.v6.2.0
  skipRange: '>=6.0.0-0 <6.2.0'
- name: cluster-logging.v6.2.1
  replaces: cluster-logging.v6.2.0
  skipRange: '>=6.0.0-0 <6.2.1'
- name: cluster-logging.v6.2.2
  replaces: cluster-logging.v6.2.1
  skipRange: '>=6.0.0-0 <6.2.2'
- name: cluster-logging.v6.2.3
  replaces: cluster-logging.v6.2.2
  skipRange: '>=6.0.0-0 <6.2.3'
- name: cluster-logging.v6.2.4
  replaces: cluster-logging.v6.2.3
  skipRange: '>=6.0.0-0 <6.2.4'
- name: cluster-logging.v6.2.5
  replaces: cluster-logging.v6.2.4
  skipRange: '>=6.0.0-0 <6.2.5'
- name: cluster-logging.v6.2.6
  replaces: cluster-logging.v6.2.5
  skipRange: '>=6.0.0-0 <6.2.6'
- name: cluster-logging.v6.2.7
  replaces: cluster-logging.v6.2.6
  skipRange: '>=6.0.0-0 <6.2.7'
- name: cluster-logging.v6.2.8
  replaces: cluster-logging.v6.2.7
  skipRange: '>=6.0.0-0 <6.2.8'
- name: cluster-logging.v6.2.9
  replaces: cluster-logging.v6.2.8
  skipRange: '>=6.0.0-0 <6.2.9'
- name: cluster-logging.v6.2.10
  replaces: cluster-logging.v6.2.9
  skipRange: '>=6.0.0-0 <6.2.10'
- name: cluster-logging.v6.2.11
  replaces: cluster-logging.v6.2.10
  skipRange: '>=6.0.0-0 <6.2.11'
name: stable-6.2
package: cluster-logging
schema: olm.channel
---
entries:
- name: cluster-logging.v6.3.0
  skipRange: '>=6.1.0-0 <6.3.0'
- name: cluster-logging.v6.3.1
  replaces: cluster-logging.v6.3.0
  skipRange: '>=6.1.0-0 <6.3.1'
- name: cluster-logging.v6.3.2
  replaces: cluster-logging.v6.3.1
  skipRange: '>=6.1.0-0 <6.3.2'
- name: cluster-logging.v6.3.3
  replaces: cluster-logging.v6.3.2
  skipRange: '>=6.1.0-0 <6.3.3'
- name: cluster-logging.v6.3.4
  replaces: cluster-logging.v6.3.3
  skipRange: '>=6.1.0-0 <6.3.4'
name: stable-6.3
package: cluster-logging
schema: olm.channel
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:eaed444b8f07a30d13f92b065d919b8b0a2dc6922b99a49f90168c3ef79c12f6
name: cluster-logging.v5.8.0
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: ClusterLogging
    version: v1
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.maxOpenShiftVersion
  value: 4.16
- type: olm.package
  value:
    packageName: cluster-logging
    version: 5.8.0
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"name\": \"remote-elasticsearch\",\n          \"secret\": {\n            \"name\": \"elasticsearch\"\n          },\n          \"type\": \"elasticsearch\",\n          \"url\": \"remote.example.org:9200\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"audit\",\n            \"infrastructure\"\n          ],\n          \"name\": \"enable-default-log-store\",\n          \"outputRefs\": [\n            \"default\"\n          ]\n        },\n        {\n          \"inputRefs\": [\n            \"application\"\n          ],\n          \"name\": \"forward-to-remote\",\n          \"outputRefs\": [\n            \"remote-elasticsearch\"\n          ]\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"logging.openshift.io/v1\",\n    \"kind\": \"ClusterLogging\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"collection\": {\n        \"type\": \"vector\"\n      },\n      \"logStore\": {\n        \"elasticsearch\": {\n          \"nodeCount\": 3,\n          \"redundancyPolicy\": \"SingleRedundancy\",\n          \"resources\": {\n            \"requests\": {\n              \"memory\": \"2Gi\"\n            }\n          },\n          \"storage\": {\n            \"size\": \"200G\"\n          }\n        },\n        \"retentionPolicy\": {\n          \"application\": {\n            \"maxAge\": \"7d\"\n          }\n        },\n        \"type\": \"elasticsearch\"\n      },\n      \"managementState\": \"Managed\",\n      \"visualization\": {\n        \"kibana\": {\n          \"replicas\": 1\n        },\n        \"type\": \"kibana\"\n      }\n    }\n  },\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      console.openshift.io/plugins: '["logging-view-plugin"]'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:f578b372f298225821c27844bb25dfbd01fb1f4945fb06e6d15d5392021835e7
      createdAt: '2023-10-12T13:12:40Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing your aggregated logging stack.
annotation
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:2a54d6f84743d734195a66158f2bf841d811d228b16851c43ae8564679b6f6f1
  name: cluster-logging-operator
- image: registry.redhat.io/openshift-logging/fluentd-rhel9@sha256:1fb4eeb6d9177f8c710775c7034fb226c2e33daa2268bd701f1bbb0aba08a6b2
  name: fluentd
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:46afd8cda8178a695ce2e3ce5d7aaa3a769178d7c1f9734f7b039b9ac680f84b
  name: log_file_metric_exporter
- image: registry.redhat.io/openshift-logging/logging-view-plugin-rhel9@sha256:ce6116aaeb3fc2a3a226a51fba4c1e74e7501f654886410286d9c32ad3903e30
  name: logging_console_plugin
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:9c777ffae9891f39e00db66b8f768a2698a207a9b45fab21adda805d666eab04
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:dfcdfdbd88fccd8e364e33e75a78dd6cbcb22835db5989187d2135937282aecb
name: cluster-logging.v5.8.15
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: ClusterLogging
    version: v1
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.maxOpenShiftVersion
  value: 4.17
- type: olm.package
  value:
    packageName: cluster-logging
    version: 5.8.15
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"name\": \"remote-elasticsearch\",\n          \"secret\": {\n            \"name\": \"elasticsearch\"\n          },\n          \"type\": \"elasticsearch\",\n          \"url\": \"remote.example.org:9200\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"audit\",\n            \"infrastructure\"\n          ],\n          \"name\": \"enable-default-log-store\",\n          \"outputRefs\": [\n            \"default\"\n          ]\n        },\n        {\n          \"inputRefs\": [\n            \"application\"\n          ],\n          \"name\": \"forward-to-remote\",\n          \"outputRefs\": [\n            \"remote-elasticsearch\"\n          ]\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"logging.openshift.io/v1\",\n    \"kind\": \"ClusterLogging\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"collection\": {\n        \"type\": \"vector\"\n      },\n      \"logStore\": {\n        \"elasticsearch\": {\n          \"nodeCount\": 3,\n          \"redundancyPolicy\": \"SingleRedundancy\",\n          \"resources\": {\n            \"requests\": {\n              \"memory\": \"2Gi\"\n            }\n          },\n          \"storage\": {\n            \"size\": \"200G\"\n          }\n        },\n        \"retentionPolicy\": {\n          \"application\": {\n            \"maxAge\": \"7d\"\n          }\n        },\n        \"type\": \"elasticsearch\"\n      },\n      \"managementState\": \"Managed\",\n      \"visualization\": {\n        \"kibana\": {\n          \"replicas\": 1\n        },\n        \"type\": \"kibana\"\n      }\n    }\n  },\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      console.openshift.io/plugins: '["logging-view-plugin"]'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:56634bb72282632e71791e838ccc18f25db759f830a139857783ff7e69dd7067
      createdAt: '2024-10-17T17:45:03Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing your aggregated logging stack.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=5.6.0-0 <5.8.15'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v3
      support: AOS Logging (team-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: "ClusterLogForwarder is an API to configure forwarding logs. \n You configure forwarding by specifying a list of `pipelines`, which forward from a set of named inputs to a set of named outputs. \n There are built-in input names for common log categories, and you can define custom inputs to do additional filtering. \n There is a built-in output name for the default openshift log store, but you can define your own outputs with a URL and other connection information to forward logs to other stores or processors, inside or outside the cluster. \n For more details see the documentation on the API fields."
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.logging.openshift.io
        specDescriptors:
        - description: "Inputs are named filters for log messages to be forwarded. \n There are three built-in inputs named `application`, `infrastructure` and `audit`. You don't need to define inputs here if those are sufficient for your needs. See `inputRefs` for more."
          displayName: Forwarder Inputs
          path: inputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderInputs
        - description: "Outputs are named destinations for log messages. \n There is a built-in output named `default` which forwards to the default openshift log store. You can define outputs to forward to other stores or log processors, inside or outside the cluster."
          displayName: Forwarder Outputs
          path: outputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderOutputs
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Forwarder Pipelines
          path: pipelines
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderPipelines
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderConditions
        - description: Filters maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filters
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:filterConditions
        - description: Inputs maps input name to condition of the input.
          displayName: Input Conditions
          path: inputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:inputConditions
        - description: Outputs maps output name to condition of the output.
          displayName: Output Conditions
          path: outputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:outputConditions
        - description: Pipelines maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelines
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:pipelineConditions
        version: v1
      - description: A Red Hat OpenShift Logging instance. ClusterLogging is the Schema for the clusterloggings API
        displayName: Cluster Logging
        kind: ClusterLogging
        name: clusterloggings.logging.openshift.io
        resources:
        - kind: ConfigMap
          name: ''
          version: v1
        - kind: CronJob
          name: ''
          version: v1
        - kind: Deployment
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: ReplicaSet
          name: ''
          version: v1
        - kind: Role
          name: ''
          version: v1
        - kind: RoleBinding
          name: ''
          version: v1
        - kind: Route
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceAccount
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        - kind: persistentvolumeclaims
          name: ''
          version: v1
        specDescriptors:
        - description: Deprecated. Specification of Log Collection for the cluster See spec.collection
          displayName: Logs
          path: collection.logs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:hidden
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Collector Node Selector
          path: collection.logs.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collection.logs.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: Collector Pod Tolerations
          path: collection.logs.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Collector Node Selector
          path: collection.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collection.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: Collector Pod Tolerations
          path: collection.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        - description: The type of Log Collection to configure
          displayName: Collector Implementation
          path: collection.type
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:select:fluentd
          - urn:alm:descriptor:com.tectonic.ui:select:vector
        - description: Deprecated. Specification of the Curation component for the cluster This component was specifically for use with Elasticsearch and was replaced by index management spec
          displayName: Curation
          path: curation
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:hidden
        - description: Deprecated. Specification for Forwarder component for the cluster See spec.collection.fluentd
          displayName: Forwarder
          path: forwarder
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:hidden
        - description: Number of nodes to deploy for Elasticsearch
          displayName: Elasticsearch Size
          path: logStore.elasticsearch.nodeCount
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:podCount
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Elasticsearch Node Selector
          path: logStore.elasticsearch.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:nodeSelector
        - description: The resource requirements for Elasticsearch
          displayName: Elasticsearch Resource Requirements
          path: logStore.elasticsearch.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Kibana Node Selector
          path: visualization.kibana.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:nodeSelector
        - description: Number of instances to deploy for a Kibana deployment
          displayName: Kibana Size
          path: visualization.kibana.replicas
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:podCount
        - description: The resource requirements for Kibana
          displayName: Kibana Resource Requirements
          path: visualization.kibana.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: Kibana Tolerations
          path: visualization.kibana.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Visualization Node Selector
          path: visualization.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:nodeSelector
        - description: LogsLimit is the max number of entries returned for a query.
          displayName: OCP Console Log Limit
          path: visualization.ocpConsole.logsLimit
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:ocpConsoleLogLimit
        - description: Timeout is the max duration before a query timeout
          displayName: OCP Console Query Timeout
          path: visualization.ocpConsole.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:ocpConsoleTimeout
        - description: Define the tolerations the Pods will accept
          displayName: Visualization Pod Tolerations
          path: visualization.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '### Notice for OCP 4.16 and 4.17 Only


      Logging 5.8 is supported on OCP 4.16 and 4.17 to help customers upgrade and migrate to Logging 6.0. **Logging 5.8 will be End of Life in November 2025.**  At that time, it will no longer be available or supported.


      # Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates and manages the aggregated logging stack as a cluster-wide service.


      ##Features

      * **Create/Destroy**: Launch and create an aggregated logging stack to support the entire OCP cluster.

      * **Simplified Configuration**: Configure your aggregated logging cluster''s structure like components and end points easily.

      * Configure log forwarding to an in-cluster, Red Hat managed log storage solution based on LokiStack or Elasticsearch


      ## Prerequisites

      ### Red Hat OpenShift Logging Namespace

      Cluster logging and the Red Hat OpenShift Logging Operator is only deployable to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - elasticsearch
    - kibana
    - fluentd
    - logging
    - aggregated
    - efk
    - vector
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Elastic
      url: https://www.elastic.co/
    - name: Fluentd
      url: https://www.fluentd.org/
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:dfcdfdbd88fccd8e364e33e75a78dd6cbcb22835db5989187d2135937282aecb
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:56634bb72282632e71791e838ccc18f25db759f830a139857783ff7e69dd7067
  name: cluster-logging-rhel9-operator-56634bb72282632e71791e838ccc18f25db759f830a139857783ff7e69dd7067-annotation
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:56634bb72282632e71791e838ccc18f25db759f830a139857783ff7e69dd7067
  name: cluster-logging-operator
- image: registry.redhat.io/openshift-logging/fluentd-rhel9@sha256:712e6b6d5797cc73074f66d5904fd864c9013074c8872c149dec7b9a3a0f4247
  name: fluentd
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:daa8fa30560a787835f7f3fa6d584555ddf8f10beda023a1d747b1f7ab8e2d36
  name: log_file_metric_exporter
- image: registry.redhat.io/openshift-logging/logging-view-plugin-rhel9@sha256:8a65d666e00feba822be4c66da8cc276bb32fe57eebecbd8f33e1ef0688ebc26
  name: logging_console_plugin
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:9bce0b67b79cc60c60af0846763139c3088a184447bd051ca0117fde95e903b2
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:58473a2057e6afafb0c10e6e90eb844a489c439511963fbce58c6e1a5e48eb1c
name: cluster-logging.v5.8.16
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: ClusterLogging
    version: v1
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.maxOpenShiftVersion
  value: 4.17
- type: olm.package
  value:
    packageName: cluster-logging
    version: 5.8.16
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"name\": \"remote-elasticsearch\",\n          \"secret\": {\n            \"name\": \"elasticsearch\"\n          },\n          \"type\": \"elasticsearch\",\n          \"url\": \"remote.example.org:9200\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"audit\",\n            \"infrastructure\"\n          ],\n          \"name\": \"enable-default-log-store\",\n          \"outputRefs\": [\n            \"default\"\n          ]\n        },\n        {\n          \"inputRefs\": [\n            \"application\"\n          ],\n          \"name\": \"forward-to-remote\",\n          \"outputRefs\": [\n            \"remote-elasticsearch\"\n          ]\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"logging.openshift.io/v1\",\n    \"kind\": \"ClusterLogging\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"collection\": {\n        \"type\": \"vector\"\n      },\n      \"logStore\": {\n        \"elasticsearch\": {\n          \"nodeCount\": 3,\n          \"redundancyPolicy\": \"SingleRedundancy\",\n          \"resources\": {\n            \"requests\": {\n              \"memory\": \"2Gi\"\n            }\n          },\n          \"storage\": {\n            \"size\": \"200G\"\n          }\n        },\n        \"retentionPolicy\": {\n          \"application\": {\n            \"maxAge\": \"7d\"\n          }\n        },\n        \"type\": \"elasticsearch\"\n      },\n      \"managementState\": \"Managed\",\n      \"visualization\": {\n        \"kibana\": {\n          \"replicas\": 1\n        },\n        \"type\": \"kibana\"\n      }\n    }\n  },\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      console.openshift.io/plugins: '["logging-view-plugin"]'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:5a3a5279cee0fbf2ea7732deafeea34332273700179037c9a2152c3cfadb74ff
      createdAt: '2024-10-17T17:45:03Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing your aggregated logging stack.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=5.6.0-0 <5.8.16'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v3
      support: AOS Logging (team-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: "ClusterLogForwarder is an API to configure forwarding logs. \n You configure forwarding by specifying a list of `pipelines`, which forward from a set of named inputs to a set of named outputs. \n There are built-in input names for common log categories, and you can define custom inputs to do additional filtering. \n There is a built-in output name for the default openshift log store, but you can define your own outputs with a URL and other connection information to forward logs to other stores or processors, inside or outside the cluster. \n For more details see the documentation on the API fields."
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.logging.openshift.io
        specDescriptors:
        - description: "Inputs are named filters for log messages to be forwarded. \n There are three built-in inputs named `application`, `infrastructure` and `audit`. You don't need to define inputs here if those are sufficient for your needs. See `inputRefs` for more."
          displayName: Forwarder Inputs
          path: inputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderInputs
        - description: "Outputs are named destinations for log messages. \n There is a built-in output named `default` which forwards to the default openshift log store. You can define outputs to forward to other stores or log processors, inside or outside the cluster."
          displayName: Forwarder Outputs
          path: outputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderOutputs
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Forwarder Pipelines
          path: pipelines
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderPipelines
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderConditions
        - description: Filters maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filters
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:filterConditions
        - description: Inputs maps input name to condition of the input.
          displayName: Input Conditions
          path: inputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:inputConditions
        - description: Outputs maps output name to condition of the output.
          displayName: Output Conditions
          path: outputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:outputConditions
        - description: Pipelines maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelines
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:pipelineConditions
        version: v1
      - description: A Red Hat OpenShift Logging instance. ClusterLogging is the Schema for the clusterloggings API
        displayName: Cluster Logging
        kind: ClusterLogging
        name: clusterloggings.logging.openshift.io
        resources:
        - kind: ConfigMap
          name: ''
          version: v1
        - kind: CronJob
          name: ''
          version: v1
        - kind: Deployment
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: ReplicaSet
          name: ''
          version: v1
        - kind: Role
          name: ''
          version: v1
        - kind: RoleBinding
          name: ''
          version: v1
        - kind: Route
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceAccount
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        - kind: persistentvolumeclaims
          name: ''
          version: v1
        specDescriptors:
        - description: Deprecated. Specification of Log Collection for the cluster See spec.collection
          displayName: Logs
          path: collection.logs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:hidden
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Collector Node Selector
          path: collection.logs.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collection.logs.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: Collector Pod Tolerations
          path: collection.logs.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Collector Node Selector
          path: collection.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collection.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: Collector Pod Tolerations
          path: collection.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        - description: The type of Log Collection to configure
          displayName: Collector Implementation
          path: collection.type
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:select:fluentd
          - urn:alm:descriptor:com.tectonic.ui:select:vector
        - description: Deprecated. Specification of the Curation component for the cluster This component was specifically for use with Elasticsearch and was replaced by index management spec
          displayName: Curation
          path: curation
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:hidden
        - description: Deprecated. Specification for Forwarder component for the cluster See spec.collection.fluentd
          displayName: Forwarder
          path: forwarder
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:hidden
        - description: Number of nodes to deploy for Elasticsearch
          displayName: Elasticsearch Size
          path: logStore.elasticsearch.nodeCount
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:podCount
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Elasticsearch Node Selector
          path: logStore.elasticsearch.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:nodeSelector
        - description: The resource requirements for Elasticsearch
          displayName: Elasticsearch Resource Requirements
          path: logStore.elasticsearch.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Kibana Node Selector
          path: visualization.kibana.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:nodeSelector
        - description: Number of instances to deploy for a Kibana deployment
          displayName: Kibana Size
          path: visualization.kibana.replicas
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:podCount
        - description: The resource requirements for Kibana
          displayName: Kibana Resource Requirements
          path: visualization.kibana.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: Kibana Tolerations
          path: visualization.kibana.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Visualization Node Selector
          path: visualization.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:nodeSelector
        - description: LogsLimit is the max number of entries returned for a query.
          displayName: OCP Console Log Limit
          path: visualization.ocpConsole.logsLimit
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:ocpConsoleLogLimit
        - description: Timeout is the max duration before a query timeout
          displayName: OCP Console Query Timeout
          path: visualization.ocpConsole.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:ocpConsoleTimeout
        - description: Define the tolerations the Pods will accept
          displayName: Visualization Pod Tolerations
          path: visualization.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '### Notice for OCP 4.16 and 4.17 Only


      Logging 5.8 is supported on OCP 4.16 and 4.17 to help customers upgrade and migrate to Logging 6.0. **Logging 5.8 will be End of Life in November 2025.**  At that time, it will no longer be available or supported.


      # Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates and manages the aggregated logging stack as a cluster-wide service.


      ##Features

      * **Create/Destroy**: Launch and create an aggregated logging stack to support the entire OCP cluster.

      * **Simplified Configuration**: Configure your aggregated logging cluster''s structure like components and end points easily.

      * Configure log forwarding to an in-cluster, Red Hat managed log storage solution based on LokiStack or Elasticsearch


      ## Prerequisites

      ### Red Hat OpenShift Logging Namespace

      Cluster logging and the Red Hat OpenShift Logging Operator is only deployable to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - elasticsearch
    - kibana
    - fluentd
    - logging
    - aggregated
    - efk
    - vector
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Elastic
      url: https://www.elastic.co/
    - name: Fluentd
      url: https://www.fluentd.org/
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:58473a2057e6afafb0c10e6e90eb844a489c439511963fbce58c6e1a5e48eb1c
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:5a3a5279cee0fbf2ea7732deafeea34332273700179037c9a2152c3cfadb74ff
  name: cluster-logging-rhel9-operator-5a3a5279cee0fbf2ea7732deafeea34332273700179037c9a2152c3cfadb74ff-annotation
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:5a3a5279cee0fbf2ea7732deafeea34332273700179037c9a2152c3cfadb74ff
  name: cluster-logging-operator
- image: registry.redhat.io/openshift-logging/fluentd-rhel9@sha256:209e1f309a8639f17845dfa3bdf286fe98f84615faa02e137af195ea1d79813e
  name: fluentd
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:a4827b83dcc200126c31c77ce4fba1ea6ab6608e052917e1418983a80cfce1c7
  name: log_file_metric_exporter
- image: registry.redhat.io/openshift-logging/logging-view-plugin-rhel9@sha256:71f8b109a49eff0536de2104e5c9cf420cf2cdce370cadadb3dfefb0ec3beafe
  name: logging_console_plugin
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:3da11115c064fa343774100768e04fa017050f8613ea4f8065f2b4f309e4e15b
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:3cf2582ff1b25552465dff4fe83664c766a5396d8bd966460abaae522dc7c8f5
name: cluster-logging.v5.8.17
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: ClusterLogging
    version: v1
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.maxOpenShiftVersion
  value: 4.17
- type: olm.package
  value:
    packageName: cluster-logging
    version: 5.8.17
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"name\": \"remote-elasticsearch\",\n          \"secret\": {\n            \"name\": \"elasticsearch\"\n          },\n          \"type\": \"elasticsearch\",\n          \"url\": \"remote.example.org:9200\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"audit\",\n            \"infrastructure\"\n          ],\n          \"name\": \"enable-default-log-store\",\n          \"outputRefs\": [\n            \"default\"\n          ]\n        },\n        {\n          \"inputRefs\": [\n            \"application\"\n          ],\n          \"name\": \"forward-to-remote\",\n          \"outputRefs\": [\n            \"remote-elasticsearch\"\n          ]\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"logging.openshift.io/v1\",\n    \"kind\": \"ClusterLogging\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"collection\": {\n        \"type\": \"vector\"\n      },\n      \"logStore\": {\n        \"elasticsearch\": {\n          \"nodeCount\": 3,\n          \"redundancyPolicy\": \"SingleRedundancy\",\n          \"resources\": {\n            \"requests\": {\n              \"memory\": \"2Gi\"\n            }\n          },\n          \"storage\": {\n            \"size\": \"200G\"\n          }\n        },\n        \"retentionPolicy\": {\n          \"application\": {\n            \"maxAge\": \"7d\"\n          }\n        },\n        \"type\": \"elasticsearch\"\n      },\n      \"managementState\": \"Managed\",\n      \"visualization\": {\n        \"kibana\": {\n          \"replicas\": 1\n        },\n        \"type\": \"kibana\"\n      }\n    }\n  },\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      console.openshift.io/plugins: '["logging-view-plugin"]'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:4883700d6b52f0acf6ec8de96c580b7839c0d6929ff41be04451c16924b8de12
      createdAt: '2024-10-17T17:45:03Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing your aggregated logging stack.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=5.6.0-0 <5.8.17'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v3
      support: AOS Logging (team-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: "ClusterLogForwarder is an API to configure forwarding logs. \n You configure forwarding by specifying a list of `pipelines`, which forward from a set of named inputs to a set of named outputs. \n There are built-in input names for common log categories, and you can define custom inputs to do additional filtering. \n There is a built-in output name for the default openshift log store, but you can define your own outputs with a URL and other connection information to forward logs to other stores or processors, inside or outside the cluster. \n For more details see the documentation on the API fields."
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.logging.openshift.io
        specDescriptors:
        - description: "Inputs are named filters for log messages to be forwarded. \n There are three built-in inputs named `application`, `infrastructure` and `audit`. You don't need to define inputs here if those are sufficient for your needs. See `inputRefs` for more."
          displayName: Forwarder Inputs
          path: inputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderInputs
        - description: "Outputs are named destinations for log messages. \n There is a built-in output named `default` which forwards to the default openshift log store. You can define outputs to forward to other stores or log processors, inside or outside the cluster."
          displayName: Forwarder Outputs
          path: outputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderOutputs
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Forwarder Pipelines
          path: pipelines
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderPipelines
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderConditions
        - description: Filters maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filters
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:filterConditions
        - description: Inputs maps input name to condition of the input.
          displayName: Input Conditions
          path: inputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:inputConditions
        - description: Outputs maps output name to condition of the output.
          displayName: Output Conditions
          path: outputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:outputConditions
        - description: Pipelines maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelines
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:pipelineConditions
        version: v1
      - description: A Red Hat OpenShift Logging instance. ClusterLogging is the Schema for the clusterloggings API
        displayName: Cluster Logging
        kind: ClusterLogging
        name: clusterloggings.logging.openshift.io
        resources:
        - kind: ConfigMap
          name: ''
          version: v1
        - kind: CronJob
          name: ''
          version: v1
        - kind: Deployment
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: ReplicaSet
          name: ''
          version: v1
        - kind: Role
          name: ''
          version: v1
        - kind: RoleBinding
          name: ''
          version: v1
        - kind: Route
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceAccount
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        - kind: persistentvolumeclaims
          name: ''
          version: v1
        specDescriptors:
        - description: Deprecated. Specification of Log Collection for the cluster See spec.collection
          displayName: Logs
          path: collection.logs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:hidden
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Collector Node Selector
          path: collection.logs.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collection.logs.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: Collector Pod Tolerations
          path: collection.logs.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Collector Node Selector
          path: collection.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collection.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: Collector Pod Tolerations
          path: collection.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        - description: The type of Log Collection to configure
          displayName: Collector Implementation
          path: collection.type
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:select:fluentd
          - urn:alm:descriptor:com.tectonic.ui:select:vector
        - description: Deprecated. Specification of the Curation component for the cluster This component was specifically for use with Elasticsearch and was replaced by index management spec
          displayName: Curation
          path: curation
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:hidden
        - description: Deprecated. Specification for Forwarder component for the cluster See spec.collection.fluentd
          displayName: Forwarder
          path: forwarder
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:hidden
        - description: Number of nodes to deploy for Elasticsearch
          displayName: Elasticsearch Size
          path: logStore.elasticsearch.nodeCount
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:podCount
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Elasticsearch Node Selector
          path: logStore.elasticsearch.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:nodeSelector
        - description: The resource requirements for Elasticsearch
          displayName: Elasticsearch Resource Requirements
          path: logStore.elasticsearch.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Kibana Node Selector
          path: visualization.kibana.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:nodeSelector
        - description: Number of instances to deploy for a Kibana deployment
          displayName: Kibana Size
          path: visualization.kibana.replicas
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:podCount
        - description: The resource requirements for Kibana
          displayName: Kibana Resource Requirements
          path: visualization.kibana.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: Kibana Tolerations
          path: visualization.kibana.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Visualization Node Selector
          path: visualization.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:nodeSelector
        - description: LogsLimit is the max number of entries returned for a query.
          displayName: OCP Console Log Limit
          path: visualization.ocpConsole.logsLimit
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:ocpConsoleLogLimit
        - description: Timeout is the max duration before a query timeout
          displayName: OCP Console Query Timeout
          path: visualization.ocpConsole.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:ocpConsoleTimeout
        - description: Define the tolerations the Pods will accept
          displayName: Visualization Pod Tolerations
          path: visualization.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '### Notice for OCP 4.16 and 4.17 Only


      Logging 5.8 is supported on OCP 4.16 and 4.17 to help customers upgrade and migrate to Logging 6.0. **Logging 5.8 will be End of Life in November 2025.**  At that time, it will no longer be available or supported.


      # Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates and manages the aggregated logging stack as a cluster-wide service.


      ##Features

      * **Create/Destroy**: Launch and create an aggregated logging stack to support the entire OCP cluster.

      * **Simplified Configuration**: Configure your aggregated logging cluster''s structure like components and end points easily.

      * Configure log forwarding to an in-cluster, Red Hat managed log storage solution based on LokiStack or Elasticsearch


      ## Prerequisites

      ### Red Hat OpenShift Logging Namespace

      Cluster logging and the Red Hat OpenShift Logging Operator is only deployable to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - elasticsearch
    - kibana
    - fluentd
    - logging
    - aggregated
    - efk
    - vector
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Elastic
      url: https://www.elastic.co/
    - name: Fluentd
      url: https://www.fluentd.org/
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256
  name: logging_console_plugin
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:b10584213df659dfbce386624477266656ee60bc741d71873b5612112fa842f6
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:436040b1c5c16252c30ac31d1f4ec19132652f8b235ca9587130a0215ef06081
name: cluster-logging.v5.8.8
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: ClusterLogging
    version: v1
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.maxOpenShiftVersion
  value: 4.16
- type: olm.package
  value:
    packageName: cluster-logging
    version: 5.8.8
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"name\": \"remote-elasticsearch\",\n          \"secret\": {\n            \"name\": \"elasticsearch\"\n          },\n          \"type\": \"elasticsearch\",\n          \"url\": \"remote.example.org:9200\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"audit\",\n            \"infrastructure\"\n          ],\n          \"name\": \"enable-default-log-store\",\n          \"outputRefs\": [\n            \"default\"\n          ]\n        },\n        {\n          \"inputRefs\": [\n            \"application\"\n          ],\n          \"name\": \"forward-to-remote\",\n          \"outputRefs\": [\n            \"remote-elasticsearch\"\n          ]\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"logging.openshift.io/v1\",\n    \"kind\": \"ClusterLogging\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"collection\": {\n        \"type\": \"vector\"\n      },\n      \"logStore\": {\n        \"elasticsearch\": {\n          \"nodeCount\": 3,\n          \"redundancyPolicy\": \"SingleRedundancy\",\n          \"resources\": {\n            \"requests\": {\n              \"memory\": \"2Gi\"\n            }\n          },\n          \"storage\": {\n            \"size\": \"200G\"\n          }\n        },\n        \"retentionPolicy\": {\n          \"application\": {\n            \"maxAge\": \"7d\"\n          }\n        },\n        \"type\": \"elasticsearch\"\n      },\n      \"managementState\": \"Managed\",\n      \"visualization\": {\n        \"kibana\": {\n          \"replicas\": 1\n        },\n        \"type\": \"kibana\"\n      }\n    }\n  },\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      console.openshift.io/plugins: '["logging-view-plugin"]'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:2bb119d9fb9fd5194ab65c762260d90417b6c020f5b2aaac7c8010b85165262a
      createdAt: '2024-05-03T19:44:05Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing your aggregated logging stack.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=5.6.0-0 <5.8.8'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v3
      support: AOS Logging (team-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: "ClusterLogForwarder is an API to configure forwarding logs. \n You configure forwarding by specifying a list of `pipelines`, which forward from a set of named inputs to a set of named outputs. \n There are built-in input names for common log categories, and you can define custom inputs to do additional filtering. \n There is a built-in output name for the default openshift log store, but you can define your own outputs with a URL and other connection information to forward logs to other stores or processors, inside or outside the cluster. \n For more details see the documentation on the API fields."
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.logging.openshift.io
        specDescriptors:
        - description: "Inputs are named filters for log messages to be forwarded. \n There are three built-in inputs named `application`, `infrastructure` and `audit`. You don't need to define inputs here if those are sufficient for your needs. See `inputRefs` for more."
          displayName: Forwarder Inputs
          path: inputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderInputs
        - description: "Outputs are named destinations for log messages. \n There is a built-in output named `default` which forwards to the default openshift log store. You can define outputs to forward to other stores or log processors, inside or outside the cluster."
          displayName: Forwarder Outputs
          path: outputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderOutputs
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Forwarder Pipelines
          path: pipelines
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderPipelines
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderConditions
        - description: Filters maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filters
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:filterConditions
        - description: Inputs maps input name to condition of the input.
          displayName: Input Conditions
          path: inputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:inputConditions
        - description: Outputs maps output name to condition of the output.
          displayName: Output Conditions
          path: outputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:outputConditions
        - description: Pipelines maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelines
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:pipelineConditions
        version: v1
      - description: A Red Hat OpenShift Logging instance. ClusterLogging is the Schema for the clusterloggings API
        displayName: Cluster Logging
        kind: ClusterLogging
        name: clusterloggings.logging.openshift.io
        resources:
        - kind: ConfigMap
          name: ''
          version: v1
        - kind: CronJob
          name: ''
          version: v1
        - kind: Deployment
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: ReplicaSet
          name: ''
          version: v1
        - kind: Role
          name: ''
          version: v1
        - kind: RoleBinding
          name: ''
          version: v1
        - kind: Route
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceAccount
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        - kind: persistentvolumeclaims
          name: ''
          version: v1
        specDescriptors:
        - description: Deprecated. Specification of Log Collection for the cluster See spec.collection
          displayName: Logs
          path: collection.logs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:hidden
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Collector Node Selector
          path: collection.logs.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collection.logs.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: Collector Pod Tolerations
          path: collection.logs.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Collector Node Selector
          path: collection.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collection.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: Collector Pod Tolerations
          path: collection.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        - description: The type of Log Collection to configure
          displayName: Collector Implementation
          path: collection.type
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:select:fluentd
          - urn:alm:descriptor:com.tectonic.ui:select:vector
        - description: Deprecated. Specification of the Curation component for the cluster This component was specifically for use with Elasticsearch and was replaced by index management spec
          displayName: Curation
          path: curation
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:hidden
        - description: Deprecated. Specification for Forwarder component for the cluster See spec.collection.fluentd
          displayName: Forwarder
          path: forwarder
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:hidden
        - description: Number of nodes to deploy for Elasticsearch
          displayName: Elasticsearch Size
          path: logStore.elasticsearch.nodeCount
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:podCount
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Elasticsearch Node Selector
          path: logStore.elasticsearch.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:nodeSelector
        - description: The resource requirements for Elasticsearch
          displayName: Elasticsearch Resource Requirements
          path: logStore.elasticsearch.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Kibana Node Selector
          path: visualization.kibana.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:nodeSelector
        - description: Number of instances to deploy for a Kibana deployment
          displayName: Kibana Size
          path: visualization.kibana.replicas
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:podCount
        - description: The resource requirements for Kibana
          displayName: Kibana Resource Requirements
          path: visualization.kibana.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: Kibana Tolerations
          path: visualization.kibana.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Visualization Node Selector
          path: visualization.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:nodeSelector
        - description: LogsLimit is the max number of entries returned for a query.
          displayName: OCP Console Log Limit
          path: visualization.ocpConsole.logsLimit
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:ocpConsoleLogLimit
        - description: Timeout is the max duration before a query timeout
          displayName: OCP Console Query Timeout
          path: visualization.ocpConsole.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:ocpConsoleTimeout
        - description: Define the tolerations the Pods will accept
          displayName: Visualization Pod Tolerations
          path: visualization.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates and manages the aggregated logging stack as a cluster-wide service.


      ##Features

      * **Create/Destroy**: Launch and create an aggregated logging stack to support the entire OCP cluster.

      * **Simplified Configuration**: Configure your aggregated logging cluster''s structure like components and end points easily.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      Cluster logging and the Red Hat OpenShift Logging Operator is only deployable to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.


      ### Elasticsearch Operator

      The Elasticsearch Operator is responsible for orchestrating and managing cluster logging''s Elasticsearch cluster.  This

      operator must be deployed to the global operator group namespace

      ### Memory Considerations

      Elasticsearch is a memory intensive application.  Red Hat OpenShift Logging will specify that each Elasticsearch node needs

      16G of memory for both request and limit unless otherwise defined in the ClusterLogging custom resource. The initial

      set of OCP nodes may not be large enough to support the Elasticsearch cluster.  Additional OCP nodes must be added

      to the OCP cluster if you desire to run with the recommended(or better) memory. Each ES node can operate with a

      lower memory setting though this is not recommended for production deployments.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - elasticsearch
    - kibana
    - fluentd
    - logging
    - aggregated
    - efk
    - vector
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Elastic
      url: https://www.elastic.co/
    - name: Fluentd
      url: https://www.fluentd.org/
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat, Inc
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:436040b1c5c16252c30ac31d1f4ec19132652f8b235ca9587130a0215ef06081
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:2bb119d9fb9fd5194ab65c762260d90417b6c020f5b2aaac7c8010b85165262a
  name: cluster-logging-rhel9-operator-2bb119d9fb9fd5194ab65c762260d90417b6c020f5b2aaac7c8010b85165262a-annotation
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:2bb119d9fb9fd5194ab65c762260d90417b6c020f5b2aaac7c8010b85165262a
  name: cluster-logging-operator
- image: registry.redhat.io/openshift-logging/fluentd-rhel9@sha256:ea07f19095e4d4d75e26043336b8ff0fe330101b452a747ec00a6ac77c49b8e8
  name: fluentd
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:1955dc40c9edb5d39951ce91156774c1bcad1132d23750dab75d6524ab779fd1
  name: log_file_metric_exporter
- image: registry.redhat.io/openshift-logging/logging-view-plugin-rhel9@sha256:fcc1e4f3a6b92da879aa8a79508212233d7190052882750409a102e7ecce2464
  name: logging_console_plugin
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:713b11885ab1134e7a2578d0f3ff49d38c522b18c390a5eeead2baead3e96a14
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:4b3573eac060af0613057a61edeafb4056b6079f94f2b2def1ae433be205bf50
name: cluster-logging.v5.8.9
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: ClusterLogging
    version: v1
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.maxOpenShiftVersion
  value: 4.16
- type: olm.package
  value:
    packageName: cluster-logging
    version: 5.8.9
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"name\": \"remote-elasticsearch\",\n          \"secret\": {\n            \"name\": \"elasticsearch\"\n          },\n          \"type\": \"elasticsearch\",\n          \"url\": \"remote.example.org:9200\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"audit\",\n            \"infrastructure\"\n          ],\n          \"name\": \"enable-default-log-store\",\n          \"outputRefs\": [\n            \"default\"\n          ]\n        },\n        {\n          \"inputRefs\": [\n            \"application\"\n          ],\n          \"name\": \"forward-to-remote\",\n          \"outputRefs\": [\n            \"remote-elasticsearch\"\n          ]\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"logging.openshift.io/v1\",\n    \"kind\": \"ClusterLogging\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"collection\": {\n        \"type\": \"vector\"\n      },\n      \"logStore\": {\n        \"elasticsearch\": {\n          \"nodeCount\": 3,\n          \"redundancyPolicy\": \"SingleRedundancy\",\n          \"resources\": {\n            \"requests\": {\n              \"memory\": \"2Gi\"\n            }\n          },\n          \"storage\": {\n            \"size\": \"200G\"\n          }\n        },\n        \"retentionPolicy\": {\n          \"application\": {\n            \"maxAge\": \"7d\"\n          }\n        },\n        \"type\": \"elasticsearch\"\n      },\n      \"managementState\": \"Managed\",\n      \"visualization\": {\n        \"kibana\": {\n          \"replicas\": 1\n        },\n        \"type\": \"kibana\"\n      }\n    }\n  },\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      console.openshift.io/plugins: '["logging-view-plugin"]'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:8dd9b1686986858e550f3abf7a66307636f0d255e0c1bba3b3c3426fc7c36ec1
      createdAt: '2024-05-03T19:44:05Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing your aggregated logging stack.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=5.6.0-0 <5.8.9'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v3
      support: AOS Logging (team-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: "ClusterLogForwarder is an API to configure forwarding logs. \n You configure forwarding by specifying a list of `pipelines`, which forward from a set of named inputs to a set of named outputs. \n There are built-in input names for common log categories, and you can define custom inputs to do additional filtering. \n There is a built-in output name for the default openshift log store, but you can define your own outputs with a URL and other connection information to forward logs to other stores or processors, inside or outside the cluster. \n For more details see the documentation on the API fields."
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.logging.openshift.io
        specDescriptors:
        - description: "Inputs are named filters for log messages to be forwarded. \n There are three built-in inputs named `application`, `infrastructure` and `audit`. You don't need to define inputs here if those are sufficient for your needs. See `inputRefs` for more."
          displayName: Forwarder Inputs
          path: inputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderInputs
        - description: "Outputs are named destinations for log messages. \n There is a built-in output named `default` which forwards to the default openshift log store. You can define outputs to forward to other stores or log processors, inside or outside the cluster."
          displayName: Forwarder Outputs
          path: outputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderOutputs
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Forwarder Pipelines
          path: pipelines
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderPipelines
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:forwarderConditions
        - description: Filters maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filters
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:filterConditions
        - description: Inputs maps input name to condition of the input.
          displayName: Input Conditions
          path: inputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:inputConditions
        - description: Outputs maps output name to condition of the output.
          displayName: Output Conditions
          path: outputs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:outputConditions
        - description: Pipelines maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelines
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:pipelineConditions
        version: v1
      - description: A Red Hat OpenShift Logging instance. ClusterLogging is the Schema for the clusterloggings API
        displayName: Cluster Logging
        kind: ClusterLogging
        name: clusterloggings.logging.openshift.io
        resources:
        - kind: ConfigMap
          name: ''
          version: v1
        - kind: CronJob
          name: ''
          version: v1
        - kind: Deployment
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: ReplicaSet
          name: ''
          version: v1
        - kind: Role
          name: ''
          version: v1
        - kind: RoleBinding
          name: ''
          version: v1
        - kind: Route
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceAccount
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        - kind: persistentvolumeclaims
          name: ''
          version: v1
        specDescriptors:
        - description: Deprecated. Specification of Log Collection for the cluster See spec.collection
          displayName: Logs
          path: collection.logs
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:hidden
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Collector Node Selector
          path: collection.logs.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collection.logs.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: Collector Pod Tolerations
          path: collection.logs.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Collector Node Selector
          path: collection.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collection.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: Collector Pod Tolerations
          path: collection.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        - description: The type of Log Collection to configure
          displayName: Collector Implementation
          path: collection.type
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:select:fluentd
          - urn:alm:descriptor:com.tectonic.ui:select:vector
        - description: Deprecated. Specification of the Curation component for the cluster This component was specifically for use with Elasticsearch and was replaced by index management spec
          displayName: Curation
          path: curation
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:hidden
        - description: Deprecated. Specification for Forwarder component for the cluster See spec.collection.fluentd
          displayName: Forwarder
          path: forwarder
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:hidden
        - description: Number of nodes to deploy for Elasticsearch
          displayName: Elasticsearch Size
          path: logStore.elasticsearch.nodeCount
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:podCount
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Elasticsearch Node Selector
          path: logStore.elasticsearch.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:nodeSelector
        - description: The resource requirements for Elasticsearch
          displayName: Elasticsearch Resource Requirements
          path: logStore.elasticsearch.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Kibana Node Selector
          path: visualization.kibana.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:nodeSelector
        - description: Number of instances to deploy for a Kibana deployment
          displayName: Kibana Size
          path: visualization.kibana.replicas
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:podCount
        - description: The resource requirements for Kibana
          displayName: Kibana Resource Requirements
          path: visualization.kibana.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: Kibana Tolerations
          path: visualization.kibana.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        - description: Define which Nodes the Pods are scheduled on.
          displayName: Visualization Node Selector
          path: visualization.nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:nodeSelector
        - description: LogsLimit is the max number of entries returned for a query.
          displayName: OCP Console Log Limit
          path: visualization.ocpConsole.logsLimit
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:ocpConsoleLogLimit
        - description: Timeout is the max duration before a query timeout
          displayName: OCP Console Query Timeout
          path: visualization.ocpConsole.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:ocpConsoleTimeout
        - description: Define the tolerations the Pods will accept
          displayName: Visualization Pod Tolerations
          path: visualization.tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates and manages the aggregated logging stack as a cluster-wide service.


      ##Features

      * **Create/Destroy**: Launch and create an aggregated logging stack to support the entire OCP cluster.

      * **Simplified Configuration**: Configure your aggregated logging cluster''s structure like components and end points easily.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      Cluster logging and the Red Hat OpenShift Logging Operator is only deployable to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.


      ### Elasticsearch Operator

      The Elasticsearch Operator is responsible for orchestrating and managing cluster logging''s Elasticsearch cluster.  This

      operator must be deployed to the global operator group namespace

      ### Memory Considerations

      Elasticsearch is a memory intensive application.  Red Hat OpenShift Logging will specify that each Elasticsearch node needs

      16G of memory for both request and limit unless otherwise defined in the ClusterLogging custom resource. The initial

      set of OCP nodes may not be large enough to support the Elasticsearch cluster.  Additional OCP nodes must be added

      to the OCP cluster if you desire to run with the recommended(or better) memory. Each ES node can operate with a

      lower memory setting though this is not recommended for production deployments.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - elasticsearch
    - kibana
    - fluentd
    - logging
    - aggregated
    - efk
    - vector
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Elastic
      url: https://www.elastic.co/
    - name: Fluentd
      url: https://www.fluentd.org/
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat, Inc
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:4b3573eac060af0613057a61edeafb4056b6079f94f2b2def1ae433be205bf50
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:8dd9b1686986858e550f3abf7a66307636f0d255e0c1bba3b3c3426fc7c36ec1
  name: cluster-logging-rhel9-operator-8dd9b1686986858e550f3abf7a66307636f0d255e0c1bba3b3c3426fc7c36ec1-annotation
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:8dd9b1686986858e550f3abf7a66307636f0d255e0c1bba3b3c3426fc7c36ec1
  name: cluster-logging-operator
- image: registry.redhat.io/openshift-logging/fluentd-rhel9@sha256:64693570c61229974f90b38007ed6c89f4cd3bc94fb6ea80b559558bbaad2909
  name: fluentd
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:780c2ddefff6d914e8971d0c18fbedd65b7c5d7b9ba9a02e1ceba264c019cc0b
  name: log_file_metric_exporter
- image: registry.redhat.io/openshift-logging/logging-view-plugin-rhel9@sha256:4b9e62d91d1ae590bff63cd6ff15c45a0ae4a99f670e71cd5bcc84e1e5b08299
  name: logging_console_plugin
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:88ee5d54627bd84f4ddc6aee2e21eb4c88ce579a6400b22bc7d1f790b71add86
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:9fac78aae457a41a045d7c239895c6558c4a5705087613e6ece4556eba1504e0
name: cluster-logging.v6.0.0
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.17
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.0.0
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:09d234b1eb6c7fb675f1f252cbec89ff50ef8f5d48ce5de30c085c61c064c95f
      createdAt: '2024-08-26T20:21:09Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=5.8.0-0 <6.0.0'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: "ClusterLogForwarder is an API to configure forwarding logs. \n You configure forwarding by specifying a list of `pipelines`, which forward from a set of named inputs to a set of named outputs."
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: Specification of the Collector deployment to define resource limits and workload placement
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: Filters are applied to log records passing through a pipeline. There are different types of filter that can select and modify log records in different ways. See [FilterTypeSpec] for a list of filter types.
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: A drop filter applies a sequence of tests to a log record and drops the record if any test passes. Each test contains a sequence of conditions, all conditions must be true for the test to pass. A DropTestsSpec contains an array of tests which contains an array of conditions
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`. The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_). If segments contain characters outside of this range, the segment must be quoted. Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: A regular expression that the field will match. If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped. Must define only one of matches OR notMatches
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: A regular expression that the field does not match. If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped. Must define only one of matches or notMatches
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: Labels applied to log records passing through a pipeline. These labels appear in the `openshift.labels` map in the log record.
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record. \n Each field path expression must start with a \".\" \n The path can contain alphanumeric characters and underscores (a-zA-Z0-9_). \n If segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted. \n Examples: \n - `.kubernetes.namespace_name` \n - `.log_type` \n - '.kubernetes.labels.foobar' \n - `.kubernetes.labels.\"foo-bar/baz\"` \n NOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned. \n NOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record. \n Each field path expression must start with a \".\" \n The path can contain alphanumeric characters and underscores (a-zA-Z0-9_). \n If segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted. \n Examples: \n - `.kubernetes.namespace_name` \n - `.log_type` \n - '.kubernetes.labels.foobar' \n - `.kubernetes.labels.\"foo-bar/baz\"` \n NOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned. \n NOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: Type of filter.
          displayName: Filter Type
          path: filters[0].type
        - description: "Inputs are named filters for log messages to be forwarded. \n There are three built-in inputs named `application`, `infrastructure` and `audit`. You don't need to define inputs here if those are sufficient for your needs. See `inputRefs` for more."
          displayName: Log Forwarder Inputs
          path: inputs
        - description: Application, named set of `application` logs that can specify a set of match criteria
          displayName: Application Logs Input
          path: inputs[0].application
        - description: "Excludes is the set of namespaces and containers to ignore when collecting logs. \n Takes precedence over Includes option."
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: Container spec the containers from which to collect logs Supports glob patterns and presumes "*" if omitted.
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Namespace specs the namespace from which to collect logs Supports glob patterns and presumes "*" if omitted.
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Includes is the set of namespaces and containers to include when collecting logs. \n Note: infrastructure namespaces are still excluded for \"*\" values unless a qualifying glob pattern is specified."
          displayName: Include
          path: inputs[0].application.includes
        - description: Container spec the containers from which to collect logs Supports glob patterns and presumes "*" if omitted.
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Namespace specs the namespace from which to collect logs Supports glob patterns and presumes "*" if omitted.
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Selector for logs from pods with matching labels. \n Only messages from pods with these labels are collected. \n If absent or empty, logs are collected regardless of labels."
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: RateLimitPerContainer is the limit applied to each container by this input. This limit is applied per collector deployment.
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: MaxRecordsPerSecond is the maximum number of log records allowed per input/output in a pipeline
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: Sources defines the list of audit sources to collect. This field is optional and its exclusion results in the collection of all audit sources.
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: Sources defines the list of infrastructure sources to collect. This field is optional and omission results in the collection of all infrastructure sources.
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: "TLS contains settings for controlling options of TLS connections. \n The operator will request certificates from the cluster's cert signing service when TLS is not defined. The certificates are injected into a secret named \"<clusterlogforwarder.name>-<input.name>\" which is mounted into the collector. The collector is configured to use the public and private key provided by the service"
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: AzureResourceId the Resource ID of the Azure resource the data should be associated with. https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: CustomerId che unique identifier for the Log Analytics workspace. https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Host alternative host for dedicated Azure regions. (for example for China region) https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: LogType the record type of the data that is being submitted. Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters. https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: IAMRole points to the secret containing the role ARN to be used for authentication. This can be used for authentication in STS-enabled clusters when additionally specifying a web identity token
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: RoleARN points to the secret containing the role ARN to be used for authentication. This is used for authentication in STS-enabled clusters.
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams \n The GroupName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value. \n A dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`. \n Static values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes. \n Example: \n 1. foo-{.bar||\"none\"} \n 2. {.foo||.bar||\"missing\"} \n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: Compression causes data to be compressed before sending over the network. It is an error if the compression type is not supported by the output.
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: "URL to send log records to. \n The 'username@password' part of `url` is ignored."
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values. \n The Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value. \n A dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`. \n Static values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes. \n Example: \n 1. foo-{.bar||\"none\"} \n 2. {.foo||.bar||\"missing\"} \n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: URL to send log records to. Basic TLS is enabled if the URL scheme requires it (for example 'https' or 'tls'). The 'username@password' part of `url` is ignored.
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used. Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream. \n The LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value. \n A dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`. \n Static values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes. \n Example: \n 1. foo-{.bar||\"none\"} \n 2. {.foo||.bar||\"missing\"} \n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the Http method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: URL to send log records to. Basic TLS is enabled if the URL scheme requires it (for example 'https' or 'tls'). The 'username@password' part of `url` is ignored.
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Brokers specifies the list of broker endpoints of a Kafka cluster. \n The list represents only the initial set used by the collector's Kafka client for the first connection only. The collector's Kafka client fetches constantly an updated list from Kafka. These updates are not reconciled back to the collector configuration. \n If none provided the target URL from the OutputSpec is used as fallback."
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic' \n The Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value. \n A dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`. \n Static values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes. \n Example: \n 1. foo-{.bar||\"none\"} \n 2. {.foo||.bar||\"missing\"} \n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: "URL to send log records to. \n The 'username@password' part of `url` is ignored."
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LabelKeys can be used to customize which log record keys are mapped to Loki stream labels. \n If LabelKeys is not set, the default keys are: \n - log_type \n - kubernetes.container_name \n - kubernetes.namespace_name \n - kubernetes.pod_name \n One additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname where the collector is running and is always present. \n Note: Loki label names must match the regular expression \"[a-zA-Z_:][a-zA-Z0-9_:]*\" Log record keys may contain characters like \".\" and \"/\" that are not allowed in Loki labels. Log record keys are translated to Loki labels by replacing any illegal characters with '_'. \n For example the default log record keys translate to these Loki labels: \n - log_type \n - kubernetes_container_name \n - kubernetes_namespace_name \n - kubernetes_pod_name \n Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed. See https://grafana.com/docs/loki/latest/configuration/#limits_config for more. Loki queries can also query based on any log record field (not just labels) using query filters."
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values. \n The TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value. \n A dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`. \n Static values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes. \n Example: \n 1. foo-{.bar||\"none\"} \n 2. {.foo||.bar||\"missing\"} \n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: URL to send log records to. Basic TLS is enabled if the URL scheme requires it (for example 'https' or 'tls'). The 'username@password' part of `url` is ignored.
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LabelKeys can be used to customize which log record keys are mapped to Loki stream labels. \n Note: Loki label names must match the regular expression \"[a-zA-Z_:][a-zA-Z0-9_:]*\" Log record keys may contain characters like \".\" and \"/\" that are not allowed in Loki labels. Log record keys are translated to Loki labels by replacing any illegal characters with '_'. \n For example the default log record keys translate to these Loki labels: \n - log_type \n - kubernetes_container_name \n - kubernetes_namespace_name \n - kubernetes_pod_name \n Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed. See https://grafana.com/docs/loki/latest/configuration/#limits_config for more. Loki queries can also query based on any log record field (not just labels) using query filters."
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label keys configuration.
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: "LabelKeys contains a list of log record keys that are mapped to Loki stream labels. \n By default, this list is combined with the labels specified in the Global configuration. This behavior can be changed by setting IgnoreGlobal to true."
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label keys configuration.
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: "LabelKeys contains a list of log record keys that are mapped to Loki stream labels. \n By default, this list is combined with the labels specified in the Global configuration. This behavior can be changed by setting IgnoreGlobal to true."
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants. \n If LabelKeys is not set, the default keys are: \n - log_type \n - kubernetes.container_name \n - kubernetes.namespace_name \n - kubernetes.pod_name \n One additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname where the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label keys configuration.
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: "LabelKeys contains a list of log record keys that are mapped to Loki stream labels. \n By default, this list is combined with the labels specified in the Global configuration. This behavior can be changed by setting IgnoreGlobal to true."
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Namespace of the in-cluster LokiStack resource. \n If unset, this defaults to \"openshift-logging\"."
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: Compression causes data to be compressed before sending over the network. It is an error if the compression type is not supported by the output.
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: "URL to send log records to. \n An absolute URL, with a valid http scheme. Must terminate with `/v1/logs` \n Basic TLS is enabled if the URL scheme requires it (for example 'https'). The 'username@password' part of `url` is ignored."
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded to this output from any given collector container. The total log flow from an individual collector container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: MaxRecordsPerSecond is the maximum number of log records allowed per input/output in a pipeline
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values. \n The Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value. \n A dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`. \n Static values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes. \n Example: \n 1. foo-{.bar||\"none\"} \n 2. {.foo||.bar||\"missing\"} \n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: URL to send log records to. Basic TLS is enabled if the URL scheme requires it (for example 'https' or 'tls'). The 'username@password' part of `url` is ignored.
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header. \n AppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48 This supports template syntax to allow dynamic per-event values. \n The AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value. \n A dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`. \n Static values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes. \n Example: \n 1. foo-{.bar||\"none\"} \n 2. {.foo||.bar||\"missing\"} \n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"} \n TODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Enrichment is an additional modification the log message before forwarding it to the receiver
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records. \n Facility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1. \n The value can be a decimal integer. Facility keywords are not standardized, this API recognizes at least the following case-insensitive keywords (defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels): \n kernel user mail daemon auth syslog lpr news uucp cron authpriv ftp ntp security console solaris-cron local0 local1 local2 local3 local4 local5 local6 local7"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values. \n The MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value. \n A dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`. \n Static values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes. \n Example: \n 1. foo-{.bar||\"none\"} \n 2. {.foo||.bar||\"missing\"} \n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"} \n MsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values. \n The PayloadKey must be a single field path encased in single curly brackets `{}`. \n Field paths must only contain alphanumeric and underscores. Any field with other characters must be quoted. \n If left empty, Syslog will use the whole message as the payload key \n Example: \n 1. {.bar} \n 2. {.foo.bar.baz} \n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values. \n The ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value. \n A dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`. \n Static values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes. \n Example: \n 1. foo-{.bar||\"none\"} \n 2. {.foo||.bar||\"missing\"} \n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"} \n ProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records. \n Severity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1 \n The value can be a decimal integer or one of these case-insensitive keywords: \n Emergency Alert Critical Error Warning Notice Informational Debug"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp` and `udps` For example, to send syslog records using secure UDP: url: udps://syslog.example.com:1234'
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates. \n This option is *not* recommended for production configurations."
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: "Filters lists the names of filters to be applied to records going through this pipeline. \n Each filter is applied in order. If a filter drops a records, subsequent filters are not applied."
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline. \n The following built-in input names are always available: \n - `application` selects all logs from application pods. \n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs. \n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat, Inc
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:9fac78aae457a41a045d7c239895c6558c4a5705087613e6ece4556eba1504e0
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:09d234b1eb6c7fb675f1f252cbec89ff50ef8f5d48ce5de30c085c61c064c95f
  name: cluster-logging-rhel9-operator-09d234b1eb6c7fb675f1f252cbec89ff50ef8f5d48ce5de30c085c61c064c95f-annotation
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:09d234b1eb6c7fb675f1f252cbec89ff50ef8f5d48ce5de30c085c61c064c95f
  name: cluster-logging-operator
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:5f84769cf058fbd1df3611ee8925d98dc896e3dbe432197b529583054f4d7f1a
  name: log_file_metric_exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:b0d3ec71e88e062f3c5999f23d97a804cd7297835e0be53bc4857c571be962a2
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:d5e21eba289036c78323183f242aaad7dcef177e2f3cb2e814c27889a4ecaf47
name: cluster-logging.v6.0.1
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.17
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.0.1
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:4c8c65a01064ea7e6f8757e0afee15f53328f5110a8becfc4b9aee0ba251031b
      createdAt: '2024-10-15T13:40:40Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=5.8.0-0 <6.0.1'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.



          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: Type of filter.
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.



            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.



            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.



            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.



            Only messages from pods with these labels are collected.



            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.



            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the Http method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.



            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.



            If none provided the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            If LabelKeys is not set, the default keys are:



            - log_type



            - kubernetes.container_name



            - kubernetes.namespace_name



            - kubernetes.pod_name



            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\n\nIf LabelKeys is not set, the default keys are:\n\n\n - log_type\n\n\n - kubernetes.container_name\n\n\n - kubernetes.namespace_name\n\n\n - kubernetes.pod_name\n\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace of the in-cluster LokiStack resource.



            If unset, this defaults to "openshift-logging".'
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.



            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`



            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nTODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Enrichment is an additional modification the log message before forwarding it to the receiver
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\n\nIf left empty, Syslog will use the whole message as the payload key\n\n\nExample:\n\n\n 1. {.bar}\n\n\n 2. {.foo.bar.baz}\n\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n\n    Emergency Alert Critical Error Warning Notice Informational Debug"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp` and `udps`\nFor example, to send syslog records using secure UDP:\n    url: udps://syslog.example.com:1234"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.



            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.



            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\n\nThe following built-in input names are always available:\n\n\n - `application` selects all logs from application pods.\n\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:d5e21eba289036c78323183f242aaad7dcef177e2f3cb2e814c27889a4ecaf47
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:4c8c65a01064ea7e6f8757e0afee15f53328f5110a8becfc4b9aee0ba251031b
  name: cluster-logging-rhel9-operator-4c8c65a01064ea7e6f8757e0afee15f53328f5110a8becfc4b9aee0ba251031b-annotation
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:4c8c65a01064ea7e6f8757e0afee15f53328f5110a8becfc4b9aee0ba251031b
  name: cluster-logging-operator
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:2d2ae149f49812b01ecf5557cb7c4867b246bb44d9865fa981d19452a4f4fc1a
  name: log_file_metric_exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:c590219ea2e5cce63aa0ddb8d1ec7d80390e9915fb4f55ec5c88d67fb6b00e58
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:c8ac9848bc6beea8217b235df07e3366602088430d00839a291b5c24bd29d946
name: cluster-logging.v6.0.10
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.17
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.0.10
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      com.redhat.delivery.appregistry: 'false'
      com.redhat.delivery.operator.bundle: 'true'
      com.redhat.openshift.versions: v4.14-v4.17
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:3e36ea9a808945d3817d169f736b1758875f2fbc6a7ae845266e1545a1b713c7
      createdAt: '2025-05-14T12:32:39Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=5.8.0-0 <6.0.10'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io.bundle.channel.default.v1: stable-6.0
      operators.operatorframework.io.bundle.channels.v1: stable-6.0
      operators.operatorframework.io.bundle.manifests.v1: manifests/
      operators.operatorframework.io.bundle.mediatype.v1: registry+v1
      operators.operatorframework.io.bundle.metadata.v1: metadata/
      operators.operatorframework.io.bundle.package.v1: cluster-logging
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.



          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: Type of filter.
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.



            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.



            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.



            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.



            Only messages from pods with these labels are collected.



            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.



            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the Http method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.



            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.



            If none provided the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            If LabelKeys is not set, the default keys are:



            - log_type



            - kubernetes.container_name



            - kubernetes.namespace_name



            - kubernetes.pod_name



            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\n\nIf LabelKeys is not set, the default keys are:\n\n\n - log_type\n\n\n - kubernetes.container_name\n\n\n - kubernetes.namespace_name\n\n\n - kubernetes.pod_name\n\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace of the in-cluster LokiStack resource.



            If unset, this defaults to "openshift-logging".'
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.



            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`



            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nTODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Enrichment is an additional modification the log message before forwarding it to the receiver
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7\n\n\nThe Facility can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"user\"}"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\n\nIf left empty, Syslog will use the whole message as the payload key\n\n\nExample:\n\n\n 1. {.bar}\n\n\n 2. {.foo.bar.baz}\n\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'The RFC to which the generated messages conform to.



            Supported values are:

            1. RFC3164

            2. RFC5424'
          displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n\n    Emergency Alert Critical Error Warning Notice Informational Debug\n\n\nThe Severity can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"Error\"}"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp` and `udps`\nFor example, to send syslog records using secure UDP:\n    url: udps://syslog.example.com:1234"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.



            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.



            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\n\nThe following built-in input names are always available:\n\n\n - `application` selects all logs from application pods.\n\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:c8ac9848bc6beea8217b235df07e3366602088430d00839a291b5c24bd29d946
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:3e36ea9a808945d3817d169f736b1758875f2fbc6a7ae845266e1545a1b713c7
  name: ''
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:15487d7b9d8f83d0df0b6fbf380f20df3518dfb1b831fac4c3209e811494b436
  name: log-file-metric-exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:4e9936c090818d1d8f812487a9a8218fda5b11f8c06167fffe35bae1bafcadf9
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:a02b643f94617d518f77d3294e752fa02af0e57b505fc483a169e85cb4ecd626
name: cluster-logging.v6.0.11
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.17
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.0.11
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      com.redhat.delivery.appregistry: 'false'
      com.redhat.delivery.operator.bundle: 'true'
      com.redhat.openshift.versions: v4.14-v4.17
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:c51e4075ceb3e83747d0f830324f384da43f829104246447b007b43b91a117fe
      createdAt: '2025-05-14T12:32:39Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=5.8.0-0 <6.0.11'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io.bundle.channel.default.v1: stable-6.0
      operators.operatorframework.io.bundle.channels.v1: stable-6.0
      operators.operatorframework.io.bundle.manifests.v1: manifests/
      operators.operatorframework.io.bundle.mediatype.v1: registry+v1
      operators.operatorframework.io.bundle.metadata.v1: metadata/
      operators.operatorframework.io.bundle.package.v1: cluster-logging
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.



          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: Type of filter.
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.



            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.



            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.



            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.



            Only messages from pods with these labels are collected.



            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.



            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the Http method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.



            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.



            If none provided the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            If LabelKeys is not set, the default keys are:



            - log_type



            - kubernetes.container_name



            - kubernetes.namespace_name



            - kubernetes.pod_name



            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\n\nIf LabelKeys is not set, the default keys are:\n\n\n - log_type\n\n\n - kubernetes.container_name\n\n\n - kubernetes.namespace_name\n\n\n - kubernetes.pod_name\n\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace of the in-cluster LokiStack resource.



            If unset, this defaults to "openshift-logging".'
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.



            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`



            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nTODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Enrichment is an additional modification the log message before forwarding it to the receiver
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7\n\n\nThe Facility can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"user\"}"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\n\nIf left empty, Syslog will use the whole message as the payload key\n\n\nExample:\n\n\n 1. {.bar}\n\n\n 2. {.foo.bar.baz}\n\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'The RFC to which the generated messages conform to.



            Supported values are:

            1. RFC3164

            2. RFC5424'
          displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n\n    Emergency Alert Critical Error Warning Notice Informational Debug\n\n\nThe Severity can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"Error\"}"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp` and `udps`\nFor example, to send syslog records using secure UDP:\n    url: udps://syslog.example.com:1234"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.



            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.



            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\n\nThe following built-in input names are always available:\n\n\n - `application` selects all logs from application pods.\n\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:a02b643f94617d518f77d3294e752fa02af0e57b505fc483a169e85cb4ecd626
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:c51e4075ceb3e83747d0f830324f384da43f829104246447b007b43b91a117fe
  name: ''
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:b0dbddbb655465d4bbad8a9637e0c0604cb42322e7a3bd2cbd846b4ecc55510c
  name: log-file-metric-exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:d1dcf66b2d57fba8d9e993813d2764f615e39ce0ac3764ec7f702fe479dec001
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:83771fed3881ab61f5b3d7aa11db0a86b7c87237197d191d54735b60e01e7939
name: cluster-logging.v6.0.12
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.17
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.0.12
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      com.redhat.delivery.appregistry: 'false'
      com.redhat.delivery.operator.bundle: 'true'
      com.redhat.openshift.versions: v4.14-v4.17
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:358e6fcb227f6414685f33a751db051fe0d1b8bb4ac16aa9d492646db205b25e
      createdAt: '2025-11-13T10:22:22Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=5.8.0-0 <6.0.12'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/must-gather-image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:358e6fcb227f6414685f33a751db051fe0d1b8bb4ac16aa9d492646db205b25e
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io.bundle.channel.default.v1: stable-6.0
      operators.operatorframework.io.bundle.channels.v1: stable-6.0
      operators.operatorframework.io.bundle.manifests.v1: manifests/
      operators.operatorframework.io.bundle.mediatype.v1: registry+v1
      operators.operatorframework.io.bundle.metadata.v1: metadata/
      operators.operatorframework.io.bundle.package.v1: cluster-logging
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.



          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: Type of filter.
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.



            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.



            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.



            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.



            Only messages from pods with these labels are collected.



            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.



            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the Http method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.



            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.



            If none provided the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            If LabelKeys is not set, the default keys are:



            - log_type



            - kubernetes.container_name



            - kubernetes.namespace_name



            - kubernetes.pod_name



            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\n\nIf LabelKeys is not set, the default keys are:\n\n\n - log_type\n\n\n - kubernetes.container_name\n\n\n - kubernetes.namespace_name\n\n\n - kubernetes.pod_name\n\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace of the in-cluster LokiStack resource.



            If unset, this defaults to "openshift-logging".'
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.



            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`



            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nTODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Enrichment is an additional modification the log message before forwarding it to the receiver
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7\n\n\nThe Facility can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"user\"}"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\n\nIf left empty, Syslog will use the whole message as the payload key\n\n\nExample:\n\n\n 1. {.bar}\n\n\n 2. {.foo.bar.baz}\n\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n\n    Emergency Alert Critical Error Warning Notice Informational Debug\n\n\nThe Severity can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"Error\"}"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp` and `udps`\nFor example, to send syslog records using secure UDP:\n    url: udps://syslog.example.com:1234"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.



            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.



            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\n\nThe following built-in input names are always available:\n\n\n - `application` selects all logs from application pods.\n\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:83771fed3881ab61f5b3d7aa11db0a86b7c87237197d191d54735b60e01e7939
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:358e6fcb227f6414685f33a751db051fe0d1b8bb4ac16aa9d492646db205b25e
  name: ''
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:9af259f46dba6b34f8a6fcf9f4b2818decf855e9cbdec497824b7a521f7d5aff
  name: log-file-metric-exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:20f0b640f80ca0ee97301549b5aa5741faf7593b961635634108958227738f8b
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:b155e98e7614578afc9a7e15b597f1b35df3b55c84c8983cd9783a22a504ea8c
name: cluster-logging.v6.0.13
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.17
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.0.13
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      com.redhat.delivery.appregistry: 'false'
      com.redhat.delivery.operator.bundle: 'true'
      com.redhat.openshift.versions: v4.14-v4.17
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:f15cb7e8a008ad0dcb2d3b485970ea844a20273e157ee550a152bc3c1155ad97
      createdAt: '2025-11-13T10:22:22Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=5.8.0-0 <6.0.13'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/must-gather-image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:f15cb7e8a008ad0dcb2d3b485970ea844a20273e157ee550a152bc3c1155ad97
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io.bundle.channel.default.v1: stable-6.0
      operators.operatorframework.io.bundle.channels.v1: stable-6.0
      operators.operatorframework.io.bundle.manifests.v1: manifests/
      operators.operatorframework.io.bundle.mediatype.v1: registry+v1
      operators.operatorframework.io.bundle.metadata.v1: metadata/
      operators.operatorframework.io.bundle.package.v1: cluster-logging
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.



          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: Type of filter.
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.



            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.



            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.



            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.



            Only messages from pods with these labels are collected.



            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.



            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the Http method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.



            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.



            If none provided the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            If LabelKeys is not set, the default keys are:



            - log_type



            - kubernetes.container_name



            - kubernetes.namespace_name



            - kubernetes.pod_name



            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\n\nIf LabelKeys is not set, the default keys are:\n\n\n - log_type\n\n\n - kubernetes.container_name\n\n\n - kubernetes.namespace_name\n\n\n - kubernetes.pod_name\n\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace of the in-cluster LokiStack resource.



            If unset, this defaults to "openshift-logging".'
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.



            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`



            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nTODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Enrichment is an additional modification the log message before forwarding it to the receiver
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7\n\n\nThe Facility can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"user\"}"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\n\nIf left empty, Syslog will use the whole message as the payload key\n\n\nExample:\n\n\n 1. {.bar}\n\n\n 2. {.foo.bar.baz}\n\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n\n    Emergency Alert Critical Error Warning Notice Informational Debug\n\n\nThe Severity can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"Error\"}"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp` and `udps`\nFor example, to send syslog records using secure UDP:\n    url: udps://syslog.example.com:1234"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.



            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.



            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\n\nThe following built-in input names are always available:\n\n\n - `application` selects all logs from application pods.\n\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:b155e98e7614578afc9a7e15b597f1b35df3b55c84c8983cd9783a22a504ea8c
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:f15cb7e8a008ad0dcb2d3b485970ea844a20273e157ee550a152bc3c1155ad97
  name: ''
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:68867f0fe29f61009471d307e5a4c88fa4a271704f041becd1038a9b8a0445b4
  name: log-file-metric-exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:08ee737d9a331cb70e14852a7d0e370c1882157084dd08eda650975eead6b500
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:d209573e6df01cd36afacc1a1f8df31e8de3d2a7a8387930f17796ee8b9048c5
name: cluster-logging.v6.0.14
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.17
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.0.14
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      com.redhat.delivery.appregistry: 'false'
      com.redhat.delivery.operator.bundle: 'true'
      com.redhat.openshift.versions: v4.14-v4.17
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:ed8ce7b6e2d6b0e195f989ffdd7c745c990f5512d821a32ddb6e797ac2b16864
      createdAt: '2026-02-05T14:25:39Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=5.8.0-0 <6.0.14'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/must-gather-image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:ed8ce7b6e2d6b0e195f989ffdd7c745c990f5512d821a32ddb6e797ac2b16864
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io.bundle.channel.default.v1: stable-6.0
      operators.operatorframework.io.bundle.channels.v1: stable-6.0
      operators.operatorframework.io.bundle.manifests.v1: manifests/
      operators.operatorframework.io.bundle.mediatype.v1: registry+v1
      operators.operatorframework.io.bundle.metadata.v1: metadata/
      operators.operatorframework.io.bundle.package.v1: cluster-logging
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.


          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\nEach field path expression must start with a \".\"\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\nExamples:\n\n - `.kubernetes.namespace_name`\n\n - `.log_type`\n\n - '.kubernetes.labels.foobar'\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\nEach field path expression must start with a \".\"\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\nExamples:\n\n - `.kubernetes.namespace_name`\n\n - `.log_type`\n\n - '.kubernetes.labels.foobar'\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: Type of filter.
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.


            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.


            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.


            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.


            Only messages from pods with these labels are collected.


            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.


            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.


            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the Http method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.


            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.


            If none provided the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to.


            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.


            If LabelKeys is not set, the default keys are:


            - log_type


            - kubernetes.container_name


            - kubernetes.namespace_name


            - kubernetes.pod_name


            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.


            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.


            For example the default log record keys translate to these Loki labels:


            - log_type


            - kubernetes_container_name


            - kubernetes_namespace_name


            - kubernetes_pod_name


            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.


            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.


            For example the default log record keys translate to these Loki labels:


            - log_type


            - kubernetes_container_name


            - kubernetes_namespace_name


            - kubernetes_pod_name


            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\nIf LabelKeys is not set, the default keys are:\n\n - log_type\n\n - kubernetes.container_name\n\n - kubernetes.namespace_name\n\n - kubernetes.pod_name\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace of the in-cluster LokiStack resource.


            If unset, this defaults to "openshift-logging".'
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.


            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`


            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Enrichment is an additional modification the log message before forwarding it to the receiver
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7\n\nThe Facility can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. {.foo||\"user\"}"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\nIf left empty, Syslog will use the whole message as the payload key\n\nExample:\n\n 1. {.bar}\n\n 2. {.foo.bar.baz}\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n    Emergency Alert Critical Error Warning Notice Informational Debug\n\nThe Severity can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. {.foo||\"Error\"}"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp` and `udps`\nFor example, to send syslog records using secure UDP:\n    url: udps://syslog.example.com:1234"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.


            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.


            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\nThe following built-in input names are always available:\n\n - `application` selects all logs from application pods.\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:d209573e6df01cd36afacc1a1f8df31e8de3d2a7a8387930f17796ee8b9048c5
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:ed8ce7b6e2d6b0e195f989ffdd7c745c990f5512d821a32ddb6e797ac2b16864
  name: ''
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:a658afee5f6c22d7f5a9cdccc194705aefbac1769844642a23095196beb488fe
  name: log-file-metric-exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:abf5078dfd589ca2efb73aff49b784ad0b0fbb5082c83111653d582e007eb396
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:a1891080cfe8ab25d9b8bcef1e960407879bdb8f46ce7ee577a13f3d5136065e
name: cluster-logging.v6.0.2
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.17
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.0.2
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:8860c455880c7ce27e89dca11ff004d9a6f00148cf9dc44d2566bef6b02cb736
      createdAt: '2024-10-15T13:40:40Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=5.8.0-0 <6.0.2'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.



          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: Type of filter.
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.



            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.



            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.



            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.



            Only messages from pods with these labels are collected.



            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.



            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the Http method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.



            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.



            If none provided the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            If LabelKeys is not set, the default keys are:



            - log_type



            - kubernetes.container_name



            - kubernetes.namespace_name



            - kubernetes.pod_name



            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\n\nIf LabelKeys is not set, the default keys are:\n\n\n - log_type\n\n\n - kubernetes.container_name\n\n\n - kubernetes.namespace_name\n\n\n - kubernetes.pod_name\n\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace of the in-cluster LokiStack resource.



            If unset, this defaults to "openshift-logging".'
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.



            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`



            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nTODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Enrichment is an additional modification the log message before forwarding it to the receiver
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\n\nIf left empty, Syslog will use the whole message as the payload key\n\n\nExample:\n\n\n 1. {.bar}\n\n\n 2. {.foo.bar.baz}\n\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n\n    Emergency Alert Critical Error Warning Notice Informational Debug"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp` and `udps`\nFor example, to send syslog records using secure UDP:\n    url: udps://syslog.example.com:1234"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.



            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.



            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\n\nThe following built-in input names are always available:\n\n\n - `application` selects all logs from application pods.\n\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:a1891080cfe8ab25d9b8bcef1e960407879bdb8f46ce7ee577a13f3d5136065e
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:8860c455880c7ce27e89dca11ff004d9a6f00148cf9dc44d2566bef6b02cb736
  name: cluster-logging-rhel9-operator-8860c455880c7ce27e89dca11ff004d9a6f00148cf9dc44d2566bef6b02cb736-annotation
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:8860c455880c7ce27e89dca11ff004d9a6f00148cf9dc44d2566bef6b02cb736
  name: cluster-logging-operator
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:88403c696496b065bbae5f16014bfe7e41f0b4cc5f66ceb12502829b21f86092
  name: log_file_metric_exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:08ce96608fa81c77d7cf080a88d8f85809f41b463e30a5e583c7cc10c6b5a363
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:c2b07b89547959c8c68bae32f2176aef2e47e69b711a8858cac436263cf01de6
name: cluster-logging.v6.0.3
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.17
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.0.3
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:0f9bd51f2df4ce0046b64be3403b9bf81155a30ff363a25d01debb33c3cb49e1
      createdAt: '2024-10-15T13:40:40Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=5.8.0-0 <6.0.3'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.



          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: Type of filter.
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.



            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.



            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.



            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.



            Only messages from pods with these labels are collected.



            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.



            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the Http method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.



            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.



            If none provided the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            If LabelKeys is not set, the default keys are:



            - log_type



            - kubernetes.container_name



            - kubernetes.namespace_name



            - kubernetes.pod_name



            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\n\nIf LabelKeys is not set, the default keys are:\n\n\n - log_type\n\n\n - kubernetes.container_name\n\n\n - kubernetes.namespace_name\n\n\n - kubernetes.pod_name\n\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace of the in-cluster LokiStack resource.



            If unset, this defaults to "openshift-logging".'
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.



            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`



            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nTODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Enrichment is an additional modification the log message before forwarding it to the receiver
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\n\nIf left empty, Syslog will use the whole message as the payload key\n\n\nExample:\n\n\n 1. {.bar}\n\n\n 2. {.foo.bar.baz}\n\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n\n    Emergency Alert Critical Error Warning Notice Informational Debug"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp` and `udps`\nFor example, to send syslog records using secure UDP:\n    url: udps://syslog.example.com:1234"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.



            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.



            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\n\nThe following built-in input names are always available:\n\n\n - `application` selects all logs from application pods.\n\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:c2b07b89547959c8c68bae32f2176aef2e47e69b711a8858cac436263cf01de6
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:0f9bd51f2df4ce0046b64be3403b9bf81155a30ff363a25d01debb33c3cb49e1
  name: cluster-logging-rhel9-operator-0f9bd51f2df4ce0046b64be3403b9bf81155a30ff363a25d01debb33c3cb49e1-annotation
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:0f9bd51f2df4ce0046b64be3403b9bf81155a30ff363a25d01debb33c3cb49e1
  name: cluster-logging-operator
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:cfa1d92300a5a7826546a10a069be134b6f3b3f407ba818d0a76ae9aed5da77f
  name: log_file_metric_exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:f544ccb5b01273aed2682ed705b63f02600f7b683b792e06f598964fa7bf39d4
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:a919953767cb3a92d0a342c6b4d970bf3faa0802381cc40f28eadc2a4f57cb6d
name: cluster-logging.v6.0.4
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.17
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.0.4
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:8cdab7bb74d1d947259c599140238f9584a5f7e130482bbf920fa89e93d71c61
      createdAt: '2024-10-15T13:40:40Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=5.8.0-0 <6.0.4'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.



          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: Type of filter.
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.



            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.



            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.



            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.



            Only messages from pods with these labels are collected.



            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.



            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the Http method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.



            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.



            If none provided the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            If LabelKeys is not set, the default keys are:



            - log_type



            - kubernetes.container_name



            - kubernetes.namespace_name



            - kubernetes.pod_name



            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\n\nIf LabelKeys is not set, the default keys are:\n\n\n - log_type\n\n\n - kubernetes.container_name\n\n\n - kubernetes.namespace_name\n\n\n - kubernetes.pod_name\n\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace of the in-cluster LokiStack resource.



            If unset, this defaults to "openshift-logging".'
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.



            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`



            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nTODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Enrichment is an additional modification the log message before forwarding it to the receiver
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\n\nIf left empty, Syslog will use the whole message as the payload key\n\n\nExample:\n\n\n 1. {.bar}\n\n\n 2. {.foo.bar.baz}\n\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n\n    Emergency Alert Critical Error Warning Notice Informational Debug"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp` and `udps`\nFor example, to send syslog records using secure UDP:\n    url: udps://syslog.example.com:1234"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.



            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.



            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\n\nThe following built-in input names are always available:\n\n\n - `application` selects all logs from application pods.\n\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:a919953767cb3a92d0a342c6b4d970bf3faa0802381cc40f28eadc2a4f57cb6d
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:8cdab7bb74d1d947259c599140238f9584a5f7e130482bbf920fa89e93d71c61
  name: cluster-logging-rhel9-operator-8cdab7bb74d1d947259c599140238f9584a5f7e130482bbf920fa89e93d71c61-annotation
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:8cdab7bb74d1d947259c599140238f9584a5f7e130482bbf920fa89e93d71c61
  name: cluster-logging-operator
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:da7f1fcd657cd106dc7d9797022c3af040c4f662fe43732b0c502085b2f49b1a
name: cluster-logging.v6.2.0
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.19
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.2.0
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:ad6e3df6b7afdafc4008bc58bfb5f09e52df52eb27262a3f75b283fd8d7b3aca
      createdAt: '2025-02-06T14:39:16Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=6.0.0-0 <6.2.0'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.



          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: Type of filter.
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.



            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.



            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.



            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.



            Only messages from pods with these labels are collected.



            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.



            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - description: AzureMonitor configures forwarding log events to the Azure Monitor Logs service
          displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - description: Cloudwatch configures forwarding log events to AWS Cloudwatch logs
          displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by \"\\|\\|\" followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets \"{}\" and MUST end with a static fallback value separated with \"\\|\\|\".\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar\\|\\|\"none\"}\n\n\n 2. {.foo\\|\\|.bar\\|\\|\"missing\"}\n\n\n 3. foo.{.bar.baz\\|\\|.qux.quux.corge\\|\\|.grault\\|\\|\"nil\"}-waldo.fred{.plugh\\|\\|\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Elasticsearch configures forwarding log events to an Elasticsearch cluster
          displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nWhen forwarding logs to the Red Hat Managed Elasticsearch, the index must match the pattern ^(app|infra|audit)-write$\nwhere the prefix depends upon the log_type. This requires defining a distinct output for each log type or distinct pipelines\nwith the openshiftLabels filter. See the product documentation for examples.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: GoogleCloudLogging configures forwarding log events to GCP (formally Stackdriver) Operations
          displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - description: HTTP configures forwarding log events to an HTTP server
          displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the HTTP method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Kafka configures forwarding log events to Apache Kafka topics
          displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.



            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.



            If none provided the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Loki configures forwarding log events to a Loki aggregation system
          displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            If LabelKeys is not set, the default keys are:



            - log_type



            - kubernetes.container_name



            - kubernetes.namespace_name



            - kubernetes.pod_name



            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LokiStack configures forwarding log events to a Red Hat managed Loki deployment

            using the Red Hat tenancy model'
          displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "DataModel can be used to customize how log data is stored in LokiStack.\n\n\nThere are two different models to choose from:\n\n\n - Viaq\n - Otel\n\n\nWhen the data model is not set, it currently defaults to the \"Viaq\" data model."
          displayName: Data Model
          path: outputs[0].lokiStack.dataModel
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\n\nIf LabelKeys is not set, the default keys are:\n\n\n - log_type\n\n\n - kubernetes.container_name\n\n\n - kubernetes.namespace_name\n\n\n - kubernetes.pod_name\n\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Namespace of the in-cluster LokiStack resource.
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - description: 'OTLP configures forwarding log events to a receiver using the OpenTelemetry Protocol

            with Red Openshift logging semantic conventions (ref: https://github.com/rhobs/observability-data-model/blob/main/cluster-logging.md)'
          displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.



            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`



            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Splunk configures forwarding log events to Splunk's HTTP event collector
          displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nTODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Enrichment is an additional modification the log message before forwarding it to the receiver
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\n\nIf left empty, Syslog will use the whole message as the payload key\n\n\nExample:\n\n\n 1. {.bar}\n\n\n 2. {.foo.bar.baz}\n\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n\n    Emergency Alert Critical Error Warning Notice Informational Debug"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp` and `udps`\nFor example, to send syslog records using secure UDP:\n    url: udps://syslog.example.com:1234"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.



            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.



            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\n\nThe following built-in input names are always available:\n\n\n - `application` selects all logs from application pods.\n\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:da7f1fcd657cd106dc7d9797022c3af040c4f662fe43732b0c502085b2f49b1a
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:ad6e3df6b7afdafc4008bc58bfb5f09e52df52eb27262a3f75b283fd8d7b3aca
  name: cluster-logging-rhel9-operator-ad6e3df6b7afdafc4008bc58bfb5f09e52df52eb27262a3f75b283fd8d7b3aca-annotation
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:ad6e3df6b7afdafc4008bc58bfb5f09e52df52eb27262a3f75b283fd8d7b3aca
  name: cluster-logging-operator
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:bc96af03fbce20f31bb2bdd0edcda8ff1a0a60ef112ad06070fd0b613e333d6c
  name: log_file_metric_exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:8cb8cca9e9d2e7b0b811d365a16a2134429c18270b8e4ffe121fd592d5d00baa
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:54f134e2f5978d68af40496e161c511bad7a5c23a48c56853b013d656dcf2422
name: cluster-logging.v6.2.1
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.19
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.2.1
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:d6da79436410dd2c9676670096661f869ff874993729032e6c9146a20166a357
      createdAt: '2025-03-31T19:37:39Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=6.0.0-0 <6.2.1'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.



          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: 'Type of filter.



            Possible filter types are:



            1. detectMultilineException - Enables multi-line error detection of container logs. No additional configuration required.

            2. drop - Drop whole log records based on the evaluation of a set of regex tests. See field `drop` for configuration.

            3. kubeAPIAudit - Remove unwanted audit events and reduce event size to create a manageable audit trail. See field `kubeAPIaudit` for configuration.

            4. openshiftLabels - Labels to be applied to log records passing through a pipeline. See field `openshiftLabels` for configuration.

            5. parse - Enables parsing of log entries into structured logs. No additional configuration required.

            6. prune - Prune log record fields to reduce the size of logs flowing into a log store. See field `prune` for configuration.'
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.



            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.



            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.



            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.



            Only messages from pods with these labels are collected.



            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.



            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - description: AzureMonitor configures forwarding log events to the Azure Monitor Logs service
          displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - description: Cloudwatch configures forwarding log events to AWS Cloudwatch logs
          displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by \"\\|\\|\" followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets \"{}\" and MUST end with a static fallback value separated with \"\\|\\|\".\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar\\|\\|\"none\"}\n\n\n 2. {.foo\\|\\|.bar\\|\\|\"missing\"}\n\n\n 3. foo.{.bar.baz\\|\\|.qux.quux.corge\\|\\|.grault\\|\\|\"nil\"}-waldo.fred{.plugh\\|\\|\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Elasticsearch configures forwarding log events to an Elasticsearch cluster
          displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nWhen forwarding logs to the Red Hat Managed Elasticsearch, the index must match the pattern ^(app|infra|audit)-write$\nwhere the prefix depends upon the log_type. This requires defining a distinct output for each log type or distinct pipelines\nwith the openshiftLabels filter. See the product documentation for examples.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: GoogleCloudLogging configures forwarding log events to GCP (formally Stackdriver) Operations
          displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - description: HTTP configures forwarding log events to an HTTP server
          displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the HTTP method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Kafka configures forwarding log events to Apache Kafka topics
          displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.



            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.



            If none provided the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Loki configures forwarding log events to a Loki aggregation system
          displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            If LabelKeys is not set, the default keys are:



            - log_type



            - kubernetes.container_name



            - kubernetes.namespace_name



            - kubernetes.pod_name



            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LokiStack configures forwarding log events to a Red Hat managed Loki deployment

            using the Red Hat tenancy model'
          displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "DataModel can be used to customize how log data is stored in LokiStack.\n\n\nThere are two different models to choose from:\n\n\n - Viaq\n - Otel\n\n\nWhen the data model is not set, it currently defaults to the \"Viaq\" data model."
          displayName: Data Model
          path: outputs[0].lokiStack.dataModel
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\n\nIf LabelKeys is not set, the default keys are:\n\n\n - log_type\n\n\n - kubernetes.container_name\n\n\n - kubernetes.namespace_name\n\n\n - kubernetes.pod_name\n\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Namespace of the in-cluster LokiStack resource.
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - description: 'OTLP configures forwarding log events to a receiver using the OpenTelemetry Protocol

            with Red Openshift logging semantic conventions (ref: https://github.com/rhobs/observability-data-model/blob/main/cluster-logging.md)'
          displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.



            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`



            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Splunk configures forwarding log events to Splunk's HTTP event collector
          displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nTODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Enrichment is an additional modification to the log message before forwarding it to the receiver.\n\n\nSupported values are:\n1. None\n   - Adds no additional enrichment to the record\n2. KubernetesMinimal\n   - Adds namespace_name, pod_name, and container_name to the beginning of the message body (e.g. namespace_name=myproject, container_name=server, pod_name=pod-123, message={\"foo\":\"bar\"}).\nThis may result in the message body being an invalid JSON structure."
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\n\nIf left empty, Syslog will use the whole message as the payload key\n\n\nExample:\n\n\n 1. {.bar}\n\n\n 2. {.foo.bar.baz}\n\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'The RFC to which the generated messages conform to.



            Supported values are:

            1. RFC3164

            2. RFC5424'
          displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n\n    Emergency Alert Critical Error Warning Notice Informational Debug"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp`\nFor example, to send syslog records using UDP:\n    url: udp://syslog.example.com:514"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.



            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.



            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\n\nThe following built-in input names are always available:\n\n\n - `application` selects all logs from application pods.\n\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:54f134e2f5978d68af40496e161c511bad7a5c23a48c56853b013d656dcf2422
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:d6da79436410dd2c9676670096661f869ff874993729032e6c9146a20166a357
  name: cluster-logging-rhel9-operator-d6da79436410dd2c9676670096661f869ff874993729032e6c9146a20166a357-annotation
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:d6da79436410dd2c9676670096661f869ff874993729032e6c9146a20166a357
  name: cluster-logging-operator
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:6f32f0942d349d00f9c745a5e856a59766bd0d38158c35ff3069d00b0ed60b1e
  name: log_file_metric_exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:40be76c7e3211df0f56517e6a9e1491df3bec21ed6b290448e0887fbee1c7f54
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:17353255c31e79c2ec45de5367019e0fb30570a1cc3cff6be170199eeb18e4dc
name: cluster-logging.v6.2.10
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.19
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.2.10
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:50243d65c2df55161a9b225b309e8565c9643dd05bb9f0fbcde15f8d80dbf7f2
      createdAt: '2026-02-03T00:15:38Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=6.0.0-0 <6.2.10'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/must-gather-image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:50243d65c2df55161a9b225b309e8565c9643dd05bb9f0fbcde15f8d80dbf7f2
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.



          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: 'Type of filter.



            Possible filter types are:



            1. detectMultilineException - Enables multi-line error detection of container logs. No additional configuration required.

            2. drop - Drop whole log records based on the evaluation of a set of regex tests. See field `drop` for configuration.

            3. kubeAPIAudit - Remove unwanted audit events and reduce event size to create a manageable audit trail. See field `kubeAPIaudit` for configuration.

            4. openshiftLabels - Labels to be applied to log records passing through a pipeline. See field `openshiftLabels` for configuration.

            5. parse - Enables parsing of log entries into structured logs. No additional configuration required.

            6. prune - Prune log record fields to reduce the size of logs flowing into a log store. See field `prune` for configuration.'
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.



            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.



            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.



            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.



            Only messages from pods with these labels are collected.



            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'MaxMessageSize  The maximum message length in bytes that a single log event can be when all

            partial log lines are merged.  Messages exceeding this limit are dropped.'
          displayName: Max Message Size
          path: inputs[0].application.tuning.maxMessageSize
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Tuning is the infrastructure input tuning spec, for now available only for container sources
          displayName: Container Input Tuning
          path: inputs[0].infrastructure.tuning
        - description: Container is the input tuning spec for container sources
          displayName: Input Tuning
          path: inputs[0].infrastructure.tuning.container
        - description: 'MaxMessageSize  The maximum message length in bytes that a single log event can be when all

            partial log lines are merged.  Messages exceeding this limit are dropped.'
          displayName: Max Message Size
          path: inputs[0].infrastructure.tuning.container.maxMessageSize
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].infrastructure.tuning.container.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].infrastructure.tuning.container.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.



            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - description: AzureMonitor configures forwarding log events to the Azure Monitor Logs service
          displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - description: Cloudwatch configures forwarding log events to AWS Cloudwatch logs
          displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by \"\\|\\|\" followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets \"{}\" and MUST end with a static fallback value separated with \"\\|\\|\".\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar\\|\\|\"none\"}\n\n\n 2. {.foo\\|\\|.bar\\|\\|\"missing\"}\n\n\n 3. foo.{.bar.baz\\|\\|.qux.quux.corge\\|\\|.grault\\|\\|\"nil\"}-waldo.fred{.plugh\\|\\|\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Elasticsearch configures forwarding log events to an Elasticsearch cluster
          displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nWhen forwarding logs to the Red Hat Managed Elasticsearch, the index must match the pattern ^(app|infra|audit)-write$\nwhere the prefix depends upon the log_type. This requires defining a distinct output for each log type or distinct pipelines\nwith the openshiftLabels filter. See the product documentation for examples.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: GoogleCloudLogging configures forwarding log events to GCP (formally Stackdriver) Operations
          displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - description: HTTP configures forwarding log events to an HTTP server
          displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the HTTP method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Kafka configures forwarding log events to Apache Kafka topics
          displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.



            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.

            If provided, it must be a valid URL with a ''tcp'' or ''tls'' scheme.

            If none is provided, the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to. This field is optional.

            If provided, it must be a valid URL with a ''tcp'' or ''tls'' scheme, for example: ''tls://kafka.secure.com:9093/app-topic''.

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Loki configures forwarding log events to a Loki aggregation system
          displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            If LabelKeys is not set, the default keys are:



            - log_type



            - kubernetes.container_name



            - kubernetes.namespace_name



            - kubernetes.pod_name



            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LokiStack configures forwarding log events to a Red Hat managed Loki deployment

            using the Red Hat tenancy model'
          displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "DataModel can be used to customize how log data is stored in LokiStack.\n\n\nThere are two different models to choose from:\n\n\n - Viaq\n - Otel\n\n\nWhen the data model is not set, it currently defaults to the \"Viaq\" data model."
          displayName: Data Model
          path: outputs[0].lokiStack.dataModel
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\n\nIf LabelKeys is not set, the default keys are:\n\n\n - log_type\n\n\n - kubernetes.container_name\n\n\n - kubernetes.namespace_name\n\n\n - kubernetes.pod_name\n\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Namespace of the in-cluster LokiStack resource.
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - description: 'OTLP configures forwarding log events to a receiver using the OpenTelemetry Protocol

            with Red Openshift logging semantic conventions (ref: https://github.com/rhobs/observability-data-model/blob/main/cluster-logging.md)'
          displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.



            An absolute URL, with a valid http scheme. The OTLP spec recommends it terminate with `/v1/logs` but

            that ''Non-default URL paths for requests MAY be configured on the client and server sides.''



            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Splunk configures forwarding log events to Splunk's HTTP event collector
          displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nTODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Enrichment is an additional modification to the log message before forwarding it to the receiver.\n\n\nSupported values are:\n1. None\n   - Adds no additional enrichment to the record\n2. KubernetesMinimal\n   - Adds namespace_name, pod_name, and container_name to the beginning of the message body (e.g. namespace_name=myproject, container_name=server, pod_name=pod-123, message={\"foo\":\"bar\"}).\nThis may result in the message body being an invalid JSON structure."
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7\n\n\nThe Facility can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"user\"}"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\n\nIf left empty, Syslog will use the whole message as the payload key\n\n\nExample:\n\n\n 1. {.bar}\n\n\n 2. {.foo.bar.baz}\n\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'The RFC to which the generated messages conform to.



            Supported values are:

            1. RFC3164

            2. RFC5424'
          displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n\n    Emergency Alert Critical Error Warning Notice Informational Debug\n\n\nThe Severity can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"Error\"}"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp`\nFor example, to send syslog records using UDP:\n    url: udp://syslog.example.com:514"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.



            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.



            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\n\nThe following built-in input names are always available:\n\n\n - `application` selects all logs from application pods.\n\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Installing logging](https://docs.redhat.com/en/documentation/openshift_container_platform/latest/html-single/logging/index#installing-logging-6-2)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:17353255c31e79c2ec45de5367019e0fb30570a1cc3cff6be170199eeb18e4dc
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:50243d65c2df55161a9b225b309e8565c9643dd05bb9f0fbcde15f8d80dbf7f2
  name: cluster-logging-rhel9-operator
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:4b2f0932f9aa4f25b6cb732412c79aaabb12be2176b9be4411793413eed3d7f1
  name: log-file-metric-exporter-rhel9
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:5af1968fd2fa137e9b066b993a780aab8eebd042a0a29a8176c1abf310b92be5
  name: vector-rhel9
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:e497325745458e5a1673cbb69fceccff713ecbebf385850d4031609ae1bf9331
name: cluster-logging.v6.2.11
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: '4.20'
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.2.11
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:4576188fb3efcdf6f3a3181d90ed6b56568f7160dfd59551776a6fc100943887
      createdAt: '2026-02-03T00:15:38Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=6.0.0-0 <6.2.11'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/must-gather-image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:4576188fb3efcdf6f3a3181d90ed6b56568f7160dfd59551776a6fc100943887
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.



          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: 'Type of filter.



            Possible filter types are:



            1. detectMultilineException - Enables multi-line error detection of container logs. No additional configuration required.

            2. drop - Drop whole log records based on the evaluation of a set of regex tests. See field `drop` for configuration.

            3. kubeAPIAudit - Remove unwanted audit events and reduce event size to create a manageable audit trail. See field `kubeAPIaudit` for configuration.

            4. openshiftLabels - Labels to be applied to log records passing through a pipeline. See field `openshiftLabels` for configuration.

            5. parse - Enables parsing of log entries into structured logs. No additional configuration required.

            6. prune - Prune log record fields to reduce the size of logs flowing into a log store. See field `prune` for configuration.'
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.



            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.



            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.



            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.



            Only messages from pods with these labels are collected.



            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'MaxMessageSize  The maximum message length in bytes that a single log event can be when all

            partial log lines are merged.  Messages exceeding this limit are dropped.'
          displayName: Max Message Size
          path: inputs[0].application.tuning.maxMessageSize
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Tuning is the infrastructure input tuning spec, for now available only for container sources
          displayName: Container Input Tuning
          path: inputs[0].infrastructure.tuning
        - description: Container is the input tuning spec for container sources
          displayName: Input Tuning
          path: inputs[0].infrastructure.tuning.container
        - description: 'MaxMessageSize  The maximum message length in bytes that a single log event can be when all

            partial log lines are merged.  Messages exceeding this limit are dropped.'
          displayName: Max Message Size
          path: inputs[0].infrastructure.tuning.container.maxMessageSize
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].infrastructure.tuning.container.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].infrastructure.tuning.container.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.



            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - description: AzureMonitor configures forwarding log events to the Azure Monitor Logs service
          displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - description: Cloudwatch configures forwarding log events to AWS Cloudwatch logs
          displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by \"\\|\\|\" followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets \"{}\" and MUST end with a static fallback value separated with \"\\|\\|\".\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar\\|\\|\"none\"}\n\n\n 2. {.foo\\|\\|.bar\\|\\|\"missing\"}\n\n\n 3. foo.{.bar.baz\\|\\|.qux.quux.corge\\|\\|.grault\\|\\|\"nil\"}-waldo.fred{.plugh\\|\\|\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Elasticsearch configures forwarding log events to an Elasticsearch cluster
          displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nWhen forwarding logs to the Red Hat Managed Elasticsearch, the index must match the pattern ^(app|infra|audit)-write$\nwhere the prefix depends upon the log_type. This requires defining a distinct output for each log type or distinct pipelines\nwith the openshiftLabels filter. See the product documentation for examples.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: GoogleCloudLogging configures forwarding log events to GCP (formally Stackdriver) Operations
          displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - description: HTTP configures forwarding log events to an HTTP server
          displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the HTTP method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Kafka configures forwarding log events to Apache Kafka topics
          displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.



            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.

            If provided, it must be a valid URL with a ''tcp'' or ''tls'' scheme.

            If none is provided, the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to. This field is optional.

            If provided, it must be a valid URL with a ''tcp'' or ''tls'' scheme, for example: ''tls://kafka.secure.com:9093/app-topic''.

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Loki configures forwarding log events to a Loki aggregation system
          displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            If LabelKeys is not set, the default keys are:



            - log_type



            - kubernetes.container_name



            - kubernetes.namespace_name



            - kubernetes.pod_name



            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LokiStack configures forwarding log events to a Red Hat managed Loki deployment

            using the Red Hat tenancy model'
          displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "DataModel can be used to customize how log data is stored in LokiStack.\n\n\nThere are two different models to choose from:\n\n\n - Viaq\n - Otel\n\n\nWhen the data model is not set, it currently defaults to the \"Viaq\" data model."
          displayName: Data Model
          path: outputs[0].lokiStack.dataModel
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\n\nIf LabelKeys is not set, the default keys are:\n\n\n - log_type\n\n\n - kubernetes.container_name\n\n\n - kubernetes.namespace_name\n\n\n - kubernetes.pod_name\n\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Namespace of the in-cluster LokiStack resource.
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - description: 'OTLP configures forwarding log events to a receiver using the OpenTelemetry Protocol

            with Red Openshift logging semantic conventions (ref: https://github.com/rhobs/observability-data-model/blob/main/cluster-logging.md)'
          displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.



            An absolute URL, with a valid http scheme. The OTLP spec recommends it terminate with `/v1/logs` but

            that ''Non-default URL paths for requests MAY be configured on the client and server sides.''



            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Splunk configures forwarding log events to Splunk's HTTP event collector
          displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nTODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Enrichment is an additional modification to the log message before forwarding it to the receiver.\n\n\nSupported values are:\n1. None\n   - Adds no additional enrichment to the record\n2. KubernetesMinimal\n   - Adds namespace_name, pod_name, and container_name to the beginning of the message body (e.g. namespace_name=myproject, container_name=server, pod_name=pod-123, message={\"foo\":\"bar\"}).\nThis may result in the message body being an invalid JSON structure."
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7\n\n\nThe Facility can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"user\"}"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\n\nIf left empty, Syslog will use the whole message as the payload key\n\n\nExample:\n\n\n 1. {.bar}\n\n\n 2. {.foo.bar.baz}\n\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'The RFC to which the generated messages conform to.



            Supported values are:

            1. RFC3164

            2. RFC5424'
          displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n\n    Emergency Alert Critical Error Warning Notice Informational Debug\n\n\nThe Severity can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"Error\"}"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp`\nFor example, to send syslog records using UDP:\n    url: udp://syslog.example.com:514"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.



            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.



            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\n\nThe following built-in input names are always available:\n\n\n - `application` selects all logs from application pods.\n\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Installing logging](https://docs.redhat.com/en/documentation/openshift_container_platform/latest/html-single/logging/index#installing-logging-6-2)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:e497325745458e5a1673cbb69fceccff713ecbebf385850d4031609ae1bf9331
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:4576188fb3efcdf6f3a3181d90ed6b56568f7160dfd59551776a6fc100943887
  name: cluster-logging-rhel9-operator
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:d6cfb273a43888e906930cfb194257ae00729e84d189ff6b5fdc71ce82bca9e7
  name: log-file-metric-exporter-rhel9
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:2c6c3bd7337c96bd129317276ea0838f862bdc71197c8d8af99e2ed5839ee43f
  name: vector-rhel9
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:bd26ed53e4d68c0007d5a6a9f8a5258d7a0f87d499d7173d3c197ed873ee48ed
name: cluster-logging.v6.2.2
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.19
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.2.2
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:3ad1e070bd2fa5d86651b191841a4f84d2750beaadb89f365e39b32c4b6a2511
      createdAt: '2025-03-31T19:37:39Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=6.0.0-0 <6.2.2'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.



          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: 'Type of filter.



            Possible filter types are:



            1. detectMultilineException - Enables multi-line error detection of container logs. No additional configuration required.

            2. drop - Drop whole log records based on the evaluation of a set of regex tests. See field `drop` for configuration.

            3. kubeAPIAudit - Remove unwanted audit events and reduce event size to create a manageable audit trail. See field `kubeAPIaudit` for configuration.

            4. openshiftLabels - Labels to be applied to log records passing through a pipeline. See field `openshiftLabels` for configuration.

            5. parse - Enables parsing of log entries into structured logs. No additional configuration required.

            6. prune - Prune log record fields to reduce the size of logs flowing into a log store. See field `prune` for configuration.'
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.



            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.



            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.



            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.



            Only messages from pods with these labels are collected.



            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.



            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - description: AzureMonitor configures forwarding log events to the Azure Monitor Logs service
          displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - description: Cloudwatch configures forwarding log events to AWS Cloudwatch logs
          displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by \"\\|\\|\" followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets \"{}\" and MUST end with a static fallback value separated with \"\\|\\|\".\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar\\|\\|\"none\"}\n\n\n 2. {.foo\\|\\|.bar\\|\\|\"missing\"}\n\n\n 3. foo.{.bar.baz\\|\\|.qux.quux.corge\\|\\|.grault\\|\\|\"nil\"}-waldo.fred{.plugh\\|\\|\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Elasticsearch configures forwarding log events to an Elasticsearch cluster
          displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nWhen forwarding logs to the Red Hat Managed Elasticsearch, the index must match the pattern ^(app|infra|audit)-write$\nwhere the prefix depends upon the log_type. This requires defining a distinct output for each log type or distinct pipelines\nwith the openshiftLabels filter. See the product documentation for examples.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: GoogleCloudLogging configures forwarding log events to GCP (formally Stackdriver) Operations
          displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - description: HTTP configures forwarding log events to an HTTP server
          displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the HTTP method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Kafka configures forwarding log events to Apache Kafka topics
          displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.



            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.



            If none provided the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Loki configures forwarding log events to a Loki aggregation system
          displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            If LabelKeys is not set, the default keys are:



            - log_type



            - kubernetes.container_name



            - kubernetes.namespace_name



            - kubernetes.pod_name



            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LokiStack configures forwarding log events to a Red Hat managed Loki deployment

            using the Red Hat tenancy model'
          displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "DataModel can be used to customize how log data is stored in LokiStack.\n\n\nThere are two different models to choose from:\n\n\n - Viaq\n - Otel\n\n\nWhen the data model is not set, it currently defaults to the \"Viaq\" data model."
          displayName: Data Model
          path: outputs[0].lokiStack.dataModel
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\n\nIf LabelKeys is not set, the default keys are:\n\n\n - log_type\n\n\n - kubernetes.container_name\n\n\n - kubernetes.namespace_name\n\n\n - kubernetes.pod_name\n\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Namespace of the in-cluster LokiStack resource.
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - description: 'OTLP configures forwarding log events to a receiver using the OpenTelemetry Protocol

            with Red Openshift logging semantic conventions (ref: https://github.com/rhobs/observability-data-model/blob/main/cluster-logging.md)'
          displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.



            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`



            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Splunk configures forwarding log events to Splunk's HTTP event collector
          displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nTODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Enrichment is an additional modification to the log message before forwarding it to the receiver.\n\n\nSupported values are:\n1. None\n   - Adds no additional enrichment to the record\n2. KubernetesMinimal\n   - Adds namespace_name, pod_name, and container_name to the beginning of the message body (e.g. namespace_name=myproject, container_name=server, pod_name=pod-123, message={\"foo\":\"bar\"}).\nThis may result in the message body being an invalid JSON structure."
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\n\nIf left empty, Syslog will use the whole message as the payload key\n\n\nExample:\n\n\n 1. {.bar}\n\n\n 2. {.foo.bar.baz}\n\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'The RFC to which the generated messages conform to.



            Supported values are:

            1. RFC3164

            2. RFC5424'
          displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n\n    Emergency Alert Critical Error Warning Notice Informational Debug"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp`\nFor example, to send syslog records using UDP:\n    url: udp://syslog.example.com:514"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.



            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.



            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\n\nThe following built-in input names are always available:\n\n\n - `application` selects all logs from application pods.\n\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:bd26ed53e4d68c0007d5a6a9f8a5258d7a0f87d499d7173d3c197ed873ee48ed
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:3ad1e070bd2fa5d86651b191841a4f84d2750beaadb89f365e39b32c4b6a2511
  name: cluster-logging-rhel9-operator-3ad1e070bd2fa5d86651b191841a4f84d2750beaadb89f365e39b32c4b6a2511-annotation
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:3ad1e070bd2fa5d86651b191841a4f84d2750beaadb89f365e39b32c4b6a2511
  name: cluster-logging-operator
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:6f32f0942d349d00f9c745a5e856a59766bd0d38158c35ff3069d00b0ed60b1e
  name: log_file_metric_exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:fbc510b5084ef97e51bc92c77553fdb9ee865d1e6cbbd0e6f2510d822ece7987
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:efdfa7b2492d880c88f372b20720fc20cb5994256ef78d4ec2050e2b82e9c153
name: cluster-logging.v6.2.3
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.19
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.2.3
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:cb0dbbc9bfc1ee86128c39c7765e2fc56013cd4e0ae5f172bed0d1b7b3932671
      createdAt: '2025-05-29T17:32:45Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=6.0.0-0 <6.2.3'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.



          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: 'Type of filter.



            Possible filter types are:



            1. detectMultilineException - Enables multi-line error detection of container logs. No additional configuration required.

            2. drop - Drop whole log records based on the evaluation of a set of regex tests. See field `drop` for configuration.

            3. kubeAPIAudit - Remove unwanted audit events and reduce event size to create a manageable audit trail. See field `kubeAPIaudit` for configuration.

            4. openshiftLabels - Labels to be applied to log records passing through a pipeline. See field `openshiftLabels` for configuration.

            5. parse - Enables parsing of log entries into structured logs. No additional configuration required.

            6. prune - Prune log record fields to reduce the size of logs flowing into a log store. See field `prune` for configuration.'
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.



            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.



            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.



            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.



            Only messages from pods with these labels are collected.



            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.



            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - description: AzureMonitor configures forwarding log events to the Azure Monitor Logs service
          displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - description: Cloudwatch configures forwarding log events to AWS Cloudwatch logs
          displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by \"\\|\\|\" followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets \"{}\" and MUST end with a static fallback value separated with \"\\|\\|\".\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar\\|\\|\"none\"}\n\n\n 2. {.foo\\|\\|.bar\\|\\|\"missing\"}\n\n\n 3. foo.{.bar.baz\\|\\|.qux.quux.corge\\|\\|.grault\\|\\|\"nil\"}-waldo.fred{.plugh\\|\\|\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Elasticsearch configures forwarding log events to an Elasticsearch cluster
          displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nWhen forwarding logs to the Red Hat Managed Elasticsearch, the index must match the pattern ^(app|infra|audit)-write$\nwhere the prefix depends upon the log_type. This requires defining a distinct output for each log type or distinct pipelines\nwith the openshiftLabels filter. See the product documentation for examples.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: GoogleCloudLogging configures forwarding log events to GCP (formally Stackdriver) Operations
          displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - description: HTTP configures forwarding log events to an HTTP server
          displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the HTTP method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Kafka configures forwarding log events to Apache Kafka topics
          displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.



            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.



            If none provided the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Loki configures forwarding log events to a Loki aggregation system
          displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            If LabelKeys is not set, the default keys are:



            - log_type



            - kubernetes.container_name



            - kubernetes.namespace_name



            - kubernetes.pod_name



            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LokiStack configures forwarding log events to a Red Hat managed Loki deployment

            using the Red Hat tenancy model'
          displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "DataModel can be used to customize how log data is stored in LokiStack.\n\n\nThere are two different models to choose from:\n\n\n - Viaq\n - Otel\n\n\nWhen the data model is not set, it currently defaults to the \"Viaq\" data model."
          displayName: Data Model
          path: outputs[0].lokiStack.dataModel
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\n\nIf LabelKeys is not set, the default keys are:\n\n\n - log_type\n\n\n - kubernetes.container_name\n\n\n - kubernetes.namespace_name\n\n\n - kubernetes.pod_name\n\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Namespace of the in-cluster LokiStack resource.
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - description: 'OTLP configures forwarding log events to a receiver using the OpenTelemetry Protocol

            with Red Openshift logging semantic conventions (ref: https://github.com/rhobs/observability-data-model/blob/main/cluster-logging.md)'
          displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.



            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`



            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Splunk configures forwarding log events to Splunk's HTTP event collector
          displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nTODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Enrichment is an additional modification to the log message before forwarding it to the receiver.\n\n\nSupported values are:\n1. None\n   - Adds no additional enrichment to the record\n2. KubernetesMinimal\n   - Adds namespace_name, pod_name, and container_name to the beginning of the message body (e.g. namespace_name=myproject, container_name=server, pod_name=pod-123, message={\"foo\":\"bar\"}).\nThis may result in the message body being an invalid JSON structure."
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7\n\n\nThe Facility can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"user\"}"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\n\nIf left empty, Syslog will use the whole message as the payload key\n\n\nExample:\n\n\n 1. {.bar}\n\n\n 2. {.foo.bar.baz}\n\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'The RFC to which the generated messages conform to.



            Supported values are:

            1. RFC3164

            2. RFC5424'
          displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n\n    Emergency Alert Critical Error Warning Notice Informational Debug\n\n\nThe Severity can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"Error\"}"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp`\nFor example, to send syslog records using UDP:\n    url: udp://syslog.example.com:514"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.



            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.



            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\n\nThe following built-in input names are always available:\n\n\n - `application` selects all logs from application pods.\n\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Installing logging](https://docs.redhat.com/en/documentation/openshift_container_platform/latest/html-single/logging/index#installing-logging-6-2)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:efdfa7b2492d880c88f372b20720fc20cb5994256ef78d4ec2050e2b82e9c153
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:cb0dbbc9bfc1ee86128c39c7765e2fc56013cd4e0ae5f172bed0d1b7b3932671
  name: cluster-logging-rhel9-operator-cb0dbbc9bfc1ee86128c39c7765e2fc56013cd4e0ae5f172bed0d1b7b3932671-annotation
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:cb0dbbc9bfc1ee86128c39c7765e2fc56013cd4e0ae5f172bed0d1b7b3932671
  name: cluster-logging-operator
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:eb6d38ee3467ec83946fc907a50cc4cf3fd6bd37a26fc62abc720e135e7dc4fd
  name: log_file_metric_exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:0082837213dbd220cc7200b52f57d4ea0ca4c0984149209c8c7a8068edbda67f
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:94f256283b590b46946eb090170b62b824331c1d969102d214f04eb6537227e4
name: cluster-logging.v6.2.4
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.19
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.2.4
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      com.redhat.delivery.appregistry: 'false'
      com.redhat.delivery.operator.bundle: 'true'
      com.redhat.openshift.versions: v4.16-v4.19
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:de5688356c196b4942ed9206b6e0ccc976c15d7397cdd88c85ce9009f63f38b3
      createdAt: '2025-07-29T15:25:15Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=6.0.0-0 <6.2
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:faf0ac7aae5f601668f81b64852c807c7852b256b9653a8fa2c7a697c8ec2ec4
name: cluster-logging.v6.2.8
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: '4.20'
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.2.8
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      com.redhat.delivery.appregistry: 'false'
      com.redhat.delivery.operator.bundle: 'true'
      com.redhat.openshift.versions: v4.16-v4.20
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:3303e932fe310496b6ae4a8d40a4d6cea0fd93cb4059506d88e774c124cf1b3c
      createdAt: '2026-02-03T00:15:38Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=6.0.0-0 <6.2.8'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/must-gather-image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:3303e932fe310496b6ae4a8d40a4d6cea0fd93cb4059506d88e774c124cf1b3c
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io.bundle.channel.default.v1: stable-6.2
      operators.operatorframework.io.bundle.channels.v1: stable-6.2
      operators.operatorframework.io.bundle.manifests.v1: manifests/
      operators.operatorframework.io.bundle.mediatype.v1: registry+v1
      operators.operatorframework.io.bundle.metadata.v1: metadata/
      operators.operatorframework.io.bundle.package.v1: cluster-logging
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.



          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: 'Type of filter.



            Possible filter types are:



            1. detectMultilineException - Enables multi-line error detection of container logs. No additional configuration required.

            2. drop - Drop whole log records based on the evaluation of a set of regex tests. See field `drop` for configuration.

            3. kubeAPIAudit - Remove unwanted audit events and reduce event size to create a manageable audit trail. See field `kubeAPIaudit` for configuration.

            4. openshiftLabels - Labels to be applied to log records passing through a pipeline. See field `openshiftLabels` for configuration.

            5. parse - Enables parsing of log entries into structured logs. No additional configuration required.

            6. prune - Prune log record fields to reduce the size of logs flowing into a log store. See field `prune` for configuration.'
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.



            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.



            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.



            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.



            Only messages from pods with these labels are collected.



            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'MaxMessageSize  The maximum message length in bytes that a single log event can be when all

            partial log lines are merged.  Messages exceeding this limit are dropped.'
          displayName: Max Message Size
          path: inputs[0].application.tuning.maxMessageSize
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Tuning is the infrastructure input tuning spec, for now available only for container sources
          displayName: Container Input Tuning
          path: inputs[0].infrastructure.tuning
        - description: Container is the input tuning spec for container sources
          displayName: Input Tuning
          path: inputs[0].infrastructure.tuning.container
        - description: 'MaxMessageSize  The maximum message length in bytes that a single log event can be when all

            partial log lines are merged.  Messages exceeding this limit are dropped.'
          displayName: Max Message Size
          path: inputs[0].infrastructure.tuning.container.maxMessageSize
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].infrastructure.tuning.container.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].infrastructure.tuning.container.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.



            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - description: AzureMonitor configures forwarding log events to the Azure Monitor Logs service
          displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - description: Cloudwatch configures forwarding log events to AWS Cloudwatch logs
          displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by \"\\|\\|\" followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets \"{}\" and MUST end with a static fallback value separated with \"\\|\\|\".\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar\\|\\|\"none\"}\n\n\n 2. {.foo\\|\\|.bar\\|\\|\"missing\"}\n\n\n 3. foo.{.bar.baz\\|\\|.qux.quux.corge\\|\\|.grault\\|\\|\"nil\"}-waldo.fred{.plugh\\|\\|\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Elasticsearch configures forwarding log events to an Elasticsearch cluster
          displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nWhen forwarding logs to the Red Hat Managed Elasticsearch, the index must match the pattern ^(app|infra|audit)-write$\nwhere the prefix depends upon the log_type. This requires defining a distinct output for each log type or distinct pipelines\nwith the openshiftLabels filter. See the product documentation for examples.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: GoogleCloudLogging configures forwarding log events to GCP (formally Stackdriver) Operations
          displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - description: HTTP configures forwarding log events to an HTTP server
          displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the HTTP method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Kafka configures forwarding log events to Apache Kafka topics
          displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.



            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.

            If provided, it must be a valid URL with a ''tcp'' or ''tls'' scheme.

            If none is provided, the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to. This field is optional.

            If provided, it must be a valid URL with a ''tcp'' or ''tls'' scheme, for example: ''tls://kafka.secure.com:9093/app-topic''.

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Loki configures forwarding log events to a Loki aggregation system
          displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            If LabelKeys is not set, the default keys are:



            - log_type



            - kubernetes.container_name



            - kubernetes.namespace_name



            - kubernetes.pod_name



            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LokiStack configures forwarding log events to a Red Hat managed Loki deployment

            using the Red Hat tenancy model'
          displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "DataModel can be used to customize how log data is stored in LokiStack.\n\n\nThere are two different models to choose from:\n\n\n - Viaq\n - Otel\n\n\nWhen the data model is not set, it currently defaults to the \"Viaq\" data model."
          displayName: Data Model
          path: outputs[0].lokiStack.dataModel
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\n\nIf LabelKeys is not set, the default keys are:\n\n\n - log_type\n\n\n - kubernetes.container_name\n\n\n - kubernetes.namespace_name\n\n\n - kubernetes.pod_name\n\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Namespace of the in-cluster LokiStack resource.
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - description: 'OTLP configures forwarding log events to a receiver using the OpenTelemetry Protocol

            with Red Openshift logging semantic conventions (ref: https://github.com/rhobs/observability-data-model/blob/main/cluster-logging.md)'
          displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.



            An absolute URL, with a valid http scheme. The OTLP spec recommends it terminate with `/v1/logs` but

            that ''Non-default URL paths for requests MAY be configured on the client and server sides.''



            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Splunk configures forwarding log events to Splunk's HTTP event collector
          displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nTODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Enrichment is an additional modification to the log message before forwarding it to the receiver.\n\n\nSupported values are:\n1. None\n   - Adds no additional enrichment to the record\n2. KubernetesMinimal\n   - Adds namespace_name, pod_name, and container_name to the beginning of the message body (e.g. namespace_name=myproject, container_name=server, pod_name=pod-123, message={\"foo\":\"bar\"}).\nThis may result in the message body being an invalid JSON structure."
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7\n\n\nThe Facility can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"user\"}"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\n\nIf left empty, Syslog will use the whole message as the payload key\n\n\nExample:\n\n\n 1. {.bar}\n\n\n 2. {.foo.bar.baz}\n\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'The RFC to which the generated messages conform to.



            Supported values are:

            1. RFC3164

            2. RFC5424'
          displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n\n    Emergency Alert Critical Error Warning Notice Informational Debug\n\n\nThe Severity can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"Error\"}"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp`\nFor example, to send syslog records using UDP:\n    url: udp://syslog.example.com:514"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.



            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.



            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\n\nThe following built-in input names are always available:\n\n\n - `application` selects all logs from application pods.\n\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Installing logging](https://docs.redhat.com/en/documentation/openshift_container_platform/latest/html-single/logging/index#installing-logging-6-2)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:faf0ac7aae5f601668f81b64852c807c7852b256b9653a8fa2c7a697c8ec2ec4
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:3303e932fe310496b6ae4a8d40a4d6cea0fd93cb4059506d88e774c124cf1b3c
  name: ''
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:851dab959c56bb98cb5ecc33f8c6cf9d5887bf757599567a3f5f653ef56a79ec
  name: log-file-metric-exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:7683a6c06eb7e40d1338bcf4a3f846b03e4993c0caef74234126696c21551978
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:a9b04cfa7910cde745e6a05c949d7dc8ea22d17f8d621fd6cd0964ef6d938d03
name: cluster-logging.v6.2.9
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: '4.20'
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.2.9
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      com.redhat.delivery.appregistry: 'false'
      com.redhat.delivery.operator.bundle: 'true'
      com.redhat.openshift.versions: v4.16-v4.20
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:bf86702a43f0ab4fda541a3aa8ace4560088be90be5eb59cd475ce1d97f5afef
      createdAt: '2026-02-03T00:15:38Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=6.0.0-0 <6.2.9'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/must-gather-image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:bf86702a43f0ab4fda541a3aa8ace4560088be90be5eb59cd475ce1d97f5afef
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io.bundle.channel.default.v1: stable-6.2
      operators.operatorframework.io.bundle.channels.v1: stable-6.2
      operators.operatorframework.io.bundle.manifests.v1: manifests/
      operators.operatorframework.io.bundle.mediatype.v1: registry+v1
      operators.operatorframework.io.bundle.metadata.v1: metadata/
      operators.operatorframework.io.bundle.package.v1: cluster-logging
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.



          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\n\nEach field path expression must start with a \".\"\n\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\n\nExamples:\n\n\n - `.kubernetes.namespace_name`\n\n\n - `.log_type`\n\n\n - '.kubernetes.labels.foobar'\n\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: 'Type of filter.



            Possible filter types are:



            1. detectMultilineException - Enables multi-line error detection of container logs. No additional configuration required.

            2. drop - Drop whole log records based on the evaluation of a set of regex tests. See field `drop` for configuration.

            3. kubeAPIAudit - Remove unwanted audit events and reduce event size to create a manageable audit trail. See field `kubeAPIaudit` for configuration.

            4. openshiftLabels - Labels to be applied to log records passing through a pipeline. See field `openshiftLabels` for configuration.

            5. parse - Enables parsing of log entries into structured logs. No additional configuration required.

            6. prune - Prune log record fields to reduce the size of logs flowing into a log store. See field `prune` for configuration.'
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.



            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.



            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.



            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.



            Only messages from pods with these labels are collected.



            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'MaxMessageSize  The maximum message length in bytes that a single log event can be when all

            partial log lines are merged.  Messages exceeding this limit are dropped.'
          displayName: Max Message Size
          path: inputs[0].application.tuning.maxMessageSize
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Tuning is the infrastructure input tuning spec, for now available only for container sources
          displayName: Container Input Tuning
          path: inputs[0].infrastructure.tuning
        - description: Container is the input tuning spec for container sources
          displayName: Input Tuning
          path: inputs[0].infrastructure.tuning.container
        - description: 'MaxMessageSize  The maximum message length in bytes that a single log event can be when all

            partial log lines are merged.  Messages exceeding this limit are dropped.'
          displayName: Max Message Size
          path: inputs[0].infrastructure.tuning.container.maxMessageSize
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].infrastructure.tuning.container.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].infrastructure.tuning.container.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.



            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - description: AzureMonitor configures forwarding log events to the Azure Monitor Logs service
          displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - description: Cloudwatch configures forwarding log events to AWS Cloudwatch logs
          displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by \"\\|\\|\" followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets \"{}\" and MUST end with a static fallback value separated with \"\\|\\|\".\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar\\|\\|\"none\"}\n\n\n 2. {.foo\\|\\|.bar\\|\\|\"missing\"}\n\n\n 3. foo.{.bar.baz\\|\\|.qux.quux.corge\\|\\|.grault\\|\\|\"nil\"}-waldo.fred{.plugh\\|\\|\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.



            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Elasticsearch configures forwarding log events to an Elasticsearch cluster
          displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nWhen forwarding logs to the Red Hat Managed Elasticsearch, the index must match the pattern ^(app|infra|audit)-write$\nwhere the prefix depends upon the log_type. This requires defining a distinct output for each log type or distinct pipelines\nwith the openshiftLabels filter. See the product documentation for examples.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: GoogleCloudLogging configures forwarding log events to GCP (formally Stackdriver) Operations
          displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - description: HTTP configures forwarding log events to an HTTP server
          displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the HTTP method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Kafka configures forwarding log events to Apache Kafka topics
          displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.



            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.

            If provided, it must be a valid URL with a ''tcp'' or ''tls'' scheme.

            If none is provided, the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to. This field is optional.

            If provided, it must be a valid URL with a ''tcp'' or ''tls'' scheme, for example: ''tls://kafka.secure.com:9093/app-topic''.

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Loki configures forwarding log events to a Loki aggregation system
          displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            If LabelKeys is not set, the default keys are:



            - log_type



            - kubernetes.container_name



            - kubernetes.namespace_name



            - kubernetes.pod_name



            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LokiStack configures forwarding log events to a Red Hat managed Loki deployment

            using the Red Hat tenancy model'
          displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "DataModel can be used to customize how log data is stored in LokiStack.\n\n\nThere are two different models to choose from:\n\n\n - Viaq\n - Otel\n\n\nWhen the data model is not set, it currently defaults to the \"Viaq\" data model."
          displayName: Data Model
          path: outputs[0].lokiStack.dataModel
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.



            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.



            For example the default log record keys translate to these Loki labels:



            - log_type



            - kubernetes_container_name



            - kubernetes_namespace_name



            - kubernetes_pod_name



            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\n\nIf LabelKeys is not set, the default keys are:\n\n\n - log_type\n\n\n - kubernetes.container_name\n\n\n - kubernetes.namespace_name\n\n\n - kubernetes.pod_name\n\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.



            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Namespace of the in-cluster LokiStack resource.
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - description: 'OTLP configures forwarding log events to a receiver using the OpenTelemetry Protocol

            with Red Openshift logging semantic conventions (ref: https://github.com/rhobs/observability-data-model/blob/main/cluster-logging.md)'
          displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.



            An absolute URL, with a valid http scheme. The OTLP spec recommends it terminate with `/v1/logs` but

            that ''Non-default URL paths for requests MAY be configured on the client and server sides.''



            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Splunk configures forwarding log events to Splunk's HTTP event collector
          displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nTODO: DETERMIN HOW to default the app name that isnt based on fluentd assumptions of \"tag\" when this is empty"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Enrichment is an additional modification to the log message before forwarding it to the receiver.\n\n\nSupported values are:\n1. None\n   - Adds no additional enrichment to the record\n2. KubernetesMinimal\n   - Adds namespace_name, pod_name, and container_name to the beginning of the message body (e.g. namespace_name=myproject, container_name=server, pod_name=pod-123, message={\"foo\":\"bar\"}).\nThis may result in the message body being an invalid JSON structure."
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7\n\n\nThe Facility can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"user\"}"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\n\nIf left empty, Syslog will use the whole message as the payload key\n\n\nExample:\n\n\n 1. {.bar}\n\n\n 2. {.foo.bar.baz}\n\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. foo-{.bar||\"none\"}\n\n\n 2. {.foo||.bar||\"missing\"}\n\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'The RFC to which the generated messages conform to.



            Supported values are:

            1. RFC3164

            2. RFC5424'
          displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n\n    Emergency Alert Critical Error Warning Notice Informational Debug\n\n\nThe Severity can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\n\nExample:\n\n\n 1. {.foo||\"Error\"}"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp`\nFor example, to send syslog records using UDP:\n    url: udp://syslog.example.com:514"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.



            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.



            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\n\nThe following built-in input names are always available:\n\n\n - `application` selects all logs from application pods.\n\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Installing logging](https://docs.redhat.com/en/documentation/openshift_container_platform/latest/html-single/logging/index#installing-logging-6-2)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:a9b04cfa7910cde745e6a05c949d7dc8ea22d17f8d621fd6cd0964ef6d938d03
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:bf86702a43f0ab4fda541a3aa8ace4560088be90be5eb59cd475ce1d97f5afef
  name: ''
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:bcf10e3b5907e2264480c34ab3fa2cf601658c59866f16d2e0ccfe36fafee7bd
  name: log-file-metric-exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:bacedab86fc0ed94a09cfa903e4d88e85b321df18224c43efcba4f6640aa8203
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:fe0e119828acbfd66a6a2a299eca873ff00f012b22c7e8ac9b40f18cdadc2760
name: cluster-logging.v6.3.0
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: '4.20'
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.3.0
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing, Observability
      certified: 'false'
      com.redhat.delivery.appregistry: 'false'
      com.redhat.delivery.operator.bundle: 'true'
      com.redhat.openshift.versions: v4.17-v4.20
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:df22078bf4a2318fe6d5452f47d92c01e1f6473e41acfb5a17ce53cee4bb2b49
      createdAt: '2025-05-14T12:47:05Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=6.1.0-0 <6.3.0'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io.bundle.channel.default.v1: stable-6.3
      operators.operatorframework.io.bundle.channels.v1: stable-6.3
      operators.operatorframework.io.bundle.manifests.v1: manifests/
      operators.operatorframework.io.bundle.mediatype.v1: registry+v1
      operators.operatorframework.io.bundle.metadata.v1: metadata/
      operators.operatorframework.io.bundle.package.v1: cluster-logging
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.


          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define scheduling rules that influence pod placement based on node or pod affinity/anti-affinity constraints.
          displayName: Affinity
          path: collector.affinity
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\nEach field path expression must start with a \".\"\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\nExamples:\n\n - `.kubernetes.namespace_name`\n\n - `.log_type`\n\n - '.kubernetes.labels.foobar'\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\nEach field path expression must start with a \".\"\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\nExamples:\n\n - `.kubernetes.namespace_name`\n\n - `.log_type`\n\n - '.kubernetes.labels.foobar'\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: 'Type of filter.


            Possible filter types are:


            1. detectMultilineException - Enables multi-line error detection of container logs. No additional configuration required.

            2. drop - Drop whole log records based on the evaluation of a set of regex tests. See field `drop` for configuration.

            3. kubeAPIAudit - Remove unwanted audit events and reduce event size to create a manageable audit trail. See field `kubeAPIaudit` for configuration.

            4. openshiftLabels - Labels to be applied to log records passing through a pipeline. See field `openshiftLabels` for configuration.

            5. parse - Enables parsing of log entries into structured logs. No additional configuration required.

            6. prune - Prune log record fields to reduce the size of logs flowing into a log store. See field `prune` for configuration.'
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.


            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.


            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.


            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.


            Only messages from pods with these labels are collected.


            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: 'Format is the format of incoming log data.


            The only currently supported format is `kubeAPIAudit`.'
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.


            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: "Type of Receiver plugin.\n\nSupported Receiver types are:\n\n1. http\n   - Currently only supports kubernetes audit logs (log_type = \"audit\")\n2. syslog\n   - Currently only supports node infrastucture logs (log_type = \"infrastructure\")"
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - description: AzureMonitor configures forwarding log events to the Azure Monitor Logs service
          displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - description: Cloudwatch configures forwarding log events to AWS Cloudwatch logs
          displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by \"\\|\\|\" followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets \"{}\" and MUST end with a static fallback value separated with \"\\|\\|\".\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar\\|\\|\"none\"}\n\n 2. {.foo\\|\\|.bar\\|\\|\"missing\"}\n\n 3. foo.{.bar.baz\\|\\|.qux.quux.corge\\|\\|.grault\\|\\|\"nil\"}-waldo.fred{.plugh\\|\\|\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.


            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Elasticsearch configures forwarding log events to an Elasticsearch cluster
          displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nWhen forwarding logs to the Red Hat Managed Elasticsearch, the index must match the pattern ^(app|infra|audit)-write$\nwhere the prefix depends upon the log_type. This requires defining a distinct output for each log type or distinct pipelines\nwith the openshiftLabels filter. See the product documentation for examples.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: GoogleCloudLogging configures forwarding log events to GCP (formally Stackdriver) Operations
          displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - description: HTTP configures forwarding log events to an HTTP server
          displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the HTTP method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Kafka configures forwarding log events to Apache Kafka topics
          displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.


            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.


            If none provided the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to.


            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Loki configures forwarding log events to a Loki aggregation system
          displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.


            If LabelKeys is not set, the default keys are:


            - log_type


            - kubernetes.container_name


            - kubernetes.namespace_name


            - kubernetes.pod_name


            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.


            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.


            For example the default log record keys translate to these Loki labels:


            - log_type


            - kubernetes_container_name


            - kubernetes_namespace_name


            - kubernetes_pod_name


            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LokiStack configures forwarding log events to a Red Hat managed Loki deployment

            using the Red Hat tenancy model'
          displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "DataModel can be used to customize how log data is stored in LokiStack.\n\nThere are two different models to choose from:\n\n - Viaq\n - Otel\n\nWhen the data model is not set, it currently defaults to the \"Viaq\" data model."
          displayName: Data Model
          path: outputs[0].lokiStack.dataModel
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.


            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.


            For example the default log record keys translate to these Loki labels:


            - log_type


            - kubernetes_container_name


            - kubernetes_namespace_name


            - kubernetes_pod_name


            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\nIf LabelKeys is not set, the default keys are:\n\n - log_type\n\n - kubernetes.container_name\n\n - kubernetes.namespace_name\n\n - kubernetes.pod_name\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Namespace of the in-cluster LokiStack resource.
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - description: 'OTLP configures forwarding log events to a receiver using the OpenTelemetry Protocol

            with Red Openshift logging semantic conventions (ref: https://github.com/rhobs/observability-data-model/blob/main/cluster-logging.md)'
          displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.


            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`


            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Splunk configures forwarding log events to Splunk's HTTP event collector
          displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IndexedFields are the list of fields to be indexed by Splunk, increase storage usage, they should be used sparingly and only for high-value fields that provide significant search benefits.

            Nested fields are flattened into top-level fields.

            Field paths are joined using dot notation, and unsupported characters are replaced with underscores (_).

            Non-string values are automatically converted to strings (e.g., 3 → "3", true → "true").

            Object values serialized as JSON strings (e.g., { status: 200 } → "{\"status\":200}").


            Examples: [`.kubernetes`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`]'
          displayName: Indexed Fields
          path: outputs[0].splunk.indexedFields
        - description: 'PayloadKey specifies record field to use as payload.

            The PayloadKey must be a single field path.


            Field paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.


            By default, payloadKey is not set, which means the complete log record is forwarded as the payload.

            Use payloadKey carefully. Selecting a single field as the payload may cause other important information in the

            log to be dropped, potentially leading to inconsistent or incomplete log events.


            Examples: `.kubernetes`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Payload Key
          path: outputs[0].splunk.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Source identifies the origin of a log event.\nThe Source can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\nIf not specified will be detected according to .log_source and .log_type value.\nDetails see in: docs/features/logforwarding/outputs/splunk-forwarding.adoc\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Source
          path: outputs[0].splunk.source
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Enrichment is an additional modification to the log message before forwarding it to the receiver.\n\nSupported values are:\n1. None\n   - Adds no additional enrichment to the record\n2. KubernetesMinimal\n   - Adds namespace_name, pod_name, and container_name to the beginning of the message body (e.g. namespace_name=myproject, container_name=server, pod_name=pod-123, message={\"foo\":\"bar\"}).\nThis may result in the message body being an invalid JSON structure."
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7\n\nThe Facility can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. {.foo||\"user\"}"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\nIf left empty, Syslog will use the whole message as the payload key\n\nExample:\n\n 1. {.bar}\n\n 2. {.foo.bar.baz}\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'The RFC to which the generated messages conform to.


            Supported values are:

            1. RFC3164

            2. RFC5424'
          displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n    Emergency Alert Critical Error Warning Notice Informational Debug\n\nThe Severity can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. {.foo||\"Error\"}"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp`\nFor example, to send syslog records using UDP:\n    url: udp://syslog.example.com:514"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.


            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.


            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\nThe following built-in input names are always available:\n\n - `application` selects all logs from application pods.\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:fe0e119828acbfd66a6a2a299eca873ff00f012b22c7e8ac9b40f18cdadc2760
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:df22078bf4a2318fe6d5452f47d92c01e1f6473e41acfb5a17ce53cee4bb2b49
  name: ''
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:43032d65c1fb6bfb62e6b9919084f4eafed1f8bb68cd822856abc040efc60c38
  name: log-file-metric-exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:b9557fce700da0b882e979125aad2f50f3d47c1dc31a630cfc17e3120d219dad
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:8c6decb25972dffb37917fb5f5a91ea05fb21b87823eb75807d8c601c12fc1bb
name: cluster-logging.v6.3.1
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: '4.20'
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.3.1
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing, Observability
      certified: 'false'
      com.redhat.delivery.appregistry: 'false'
      com.redhat.delivery.operator.bundle: 'true'
      com.redhat.openshift.versions: v4.17-v4.20
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:5f96aaba248207054bdf1dd8dd8e43a2550a210c62367535fb31fa9907c5ee7a
      createdAt: '2025-05-14T12:47:05Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=6.1.0-0 <6.3.1'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io.bundle.channel.default.v1: stable-6.3
      operators.operatorframework.io.bundle.channels.v1: stable-6.3
      operators.operatorframework.io.bundle.manifests.v1: manifests/
      operators.operatorframework.io.bundle.mediatype.v1: registry+v1
      operators.operatorframework.io.bundle.metadata.v1: metadata/
      operators.operatorframework.io.bundle.package.v1: cluster-logging
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.


          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define scheduling rules that influence pod placement based on node or pod affinity/anti-affinity constraints.
          displayName: Affinity
          path: collector.affinity
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\nEach field path expression must start with a \".\"\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\nExamples:\n\n - `.kubernetes.namespace_name`\n\n - `.log_type`\n\n - '.kubernetes.labels.foobar'\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\nEach field path expression must start with a \".\"\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\nExamples:\n\n - `.kubernetes.namespace_name`\n\n - `.log_type`\n\n - '.kubernetes.labels.foobar'\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: 'Type of filter.


            Possible filter types are:


            1. detectMultilineException - Enables multi-line error detection of container logs. No additional configuration required.

            2. drop - Drop whole log records based on the evaluation of a set of regex tests. See field `drop` for configuration.

            3. kubeAPIAudit - Remove unwanted audit events and reduce event size to create a manageable audit trail. See field `kubeAPIaudit` for configuration.

            4. openshiftLabels - Labels to be applied to log records passing through a pipeline. See field `openshiftLabels` for configuration.

            5. parse - Enables parsing of log entries into structured logs. No additional configuration required.

            6. prune - Prune log record fields to reduce the size of logs flowing into a log store. See field `prune` for configuration.'
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.


            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.


            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.


            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.


            Only messages from pods with these labels are collected.


            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: 'Format is the format of incoming log data.


            The only currently supported format is `kubeAPIAudit`.'
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.


            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: "Type of Receiver plugin.\n\nSupported Receiver types are:\n\n1. http\n   - Currently only supports kubernetes audit logs (log_type = \"audit\")\n2. syslog\n   - Currently only supports node infrastucture logs (log_type = \"infrastructure\")"
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - description: AzureMonitor configures forwarding log events to the Azure Monitor Logs service
          displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - description: Cloudwatch configures forwarding log events to AWS Cloudwatch logs
          displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by \"\\|\\|\" followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets \"{}\" and MUST end with a static fallback value separated with \"\\|\\|\".\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar\\|\\|\"none\"}\n\n 2. {.foo\\|\\|.bar\\|\\|\"missing\"}\n\n 3. foo.{.bar.baz\\|\\|.qux.quux.corge\\|\\|.grault\\|\\|\"nil\"}-waldo.fred{.plugh\\|\\|\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.


            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Elasticsearch configures forwarding log events to an Elasticsearch cluster
          displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nWhen forwarding logs to the Red Hat Managed Elasticsearch, the index must match the pattern ^(app|infra|audit)-write$\nwhere the prefix depends upon the log_type. This requires defining a distinct output for each log type or distinct pipelines\nwith the openshiftLabels filter. See the product documentation for examples.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: GoogleCloudLogging configures forwarding log events to GCP (formally Stackdriver) Operations
          displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - description: HTTP configures forwarding log events to an HTTP server
          displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the HTTP method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Kafka configures forwarding log events to Apache Kafka topics
          displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.


            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.


            If none provided the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to.


            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Loki configures forwarding log events to a Loki aggregation system
          displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.


            If LabelKeys is not set, the default keys are:


            - log_type


            - kubernetes.container_name


            - kubernetes.namespace_name


            - kubernetes.pod_name


            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.


            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.


            For example the default log record keys translate to these Loki labels:


            - log_type


            - kubernetes_container_name


            - kubernetes_namespace_name


            - kubernetes_pod_name


            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LokiStack configures forwarding log events to a Red Hat managed Loki deployment

            using the Red Hat tenancy model'
          displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "DataModel can be used to customize how log data is stored in LokiStack.\n\nThere are two different models to choose from:\n\n - Viaq\n - Otel\n\nWhen the data model is not set, it currently defaults to the \"Viaq\" data model."
          displayName: Data Model
          path: outputs[0].lokiStack.dataModel
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.


            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.


            For example the default log record keys translate to these Loki labels:


            - log_type


            - kubernetes_container_name


            - kubernetes_namespace_name


            - kubernetes_pod_name


            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\nIf LabelKeys is not set, the default keys are:\n\n - log_type\n\n - kubernetes.container_name\n\n - kubernetes.namespace_name\n\n - kubernetes.pod_name\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Namespace of the in-cluster LokiStack resource.
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - description: 'OTLP configures forwarding log events to a receiver using the OpenTelemetry Protocol

            with Red Openshift logging semantic conventions (ref: https://github.com/rhobs/observability-data-model/blob/main/cluster-logging.md)'
          displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.


            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`


            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Splunk configures forwarding log events to Splunk's HTTP event collector
          displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IndexedFields are the list of fields to be indexed by Splunk, increase storage usage, they should be used sparingly and only for high-value fields that provide significant search benefits.

            Nested fields are flattened into top-level fields.

            Field paths are joined using dot notation, and unsupported characters are replaced with underscores (_).

            Non-string values are automatically converted to strings (e.g., 3 → "3", true → "true").

            Object values serialized as JSON strings (e.g., { status: 200 } → "{\"status\":200}").


            Examples: [`.kubernetes`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`]'
          displayName: Indexed Fields
          path: outputs[0].splunk.indexedFields
        - description: 'PayloadKey specifies record field to use as payload.

            The PayloadKey must be a single field path.


            Field paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.


            By default, payloadKey is not set, which means the complete log record is forwarded as the payload.

            Use payloadKey carefully. Selecting a single field as the payload may cause other important information in the

            log to be dropped, potentially leading to inconsistent or incomplete log events.


            Examples: `.kubernetes`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Payload Key
          path: outputs[0].splunk.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Source identifies the origin of a log event.\nThe Source can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\nIf not specified will be detected according to .log_source and .log_type value.\nDetails see in: docs/features/logforwarding/outputs/splunk-forwarding.adoc\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Source
          path: outputs[0].splunk.source
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Enrichment is an additional modification to the log message before forwarding it to the receiver.\n\nSupported values are:\n1. None\n   - Adds no additional enrichment to the record\n2. KubernetesMinimal\n   - Adds namespace_name, pod_name, and container_name to the beginning of the message body (e.g. namespace_name=myproject, container_name=server, pod_name=pod-123, message={\"foo\":\"bar\"}).\nThis may result in the message body being an invalid JSON structure."
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7\n\nThe Facility can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. {.foo||\"user\"}"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\nIf left empty, Syslog will use the whole message as the payload key\n\nExample:\n\n 1. {.bar}\n\n 2. {.foo.bar.baz}\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'The RFC to which the generated messages conform to.


            Supported values are:

            1. RFC3164

            2. RFC5424'
          displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n    Emergency Alert Critical Error Warning Notice Informational Debug\n\nThe Severity can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. {.foo||\"Error\"}"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp`\nFor example, to send syslog records using UDP:\n    url: udp://syslog.example.com:514"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.


            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.


            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\nThe following built-in input names are always available:\n\n - `application` selects all logs from application pods.\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:8c6decb25972dffb37917fb5f5a91ea05fb21b87823eb75807d8c601c12fc1bb
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:5f96aaba248207054bdf1dd8dd8e43a2550a210c62367535fb31fa9907c5ee7a
  name: ''
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:255a73d34b85f25b3655fb2b63954aa6b635b89080a8c249fc41568fd8365b10
  name: log-file-metric-exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:2495de935f503808bf8700d9d66b8412d0f15fc754cf569d02abfbd74b5a6ee8
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:56ee655fc9fd5aca984a7c552c00112b7314d2e79117f58c469ef91f3cefe640
name: cluster-logging.v6.3.2
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: '4.20'
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.3.2
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing, Observability
      certified: 'false'
      com.redhat.delivery.appregistry: 'false'
      com.redhat.delivery.operator.bundle: 'true'
      com.redhat.openshift.versions: v4.17-v4.20
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:61ced99002f57d20727adc2dbad34b99c1347541b4605c5ec2847db577532dc5
      createdAt: '2025-11-13T11:03:18Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=6.1.0-0 <6.3.2'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/must-gather-image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:61ced99002f57d20727adc2dbad34b99c1347541b4605c5ec2847db577532dc5
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io.bundle.channel.default.v1: stable-6.3
      operators.operatorframework.io.bundle.channels.v1: stable-6.3
      operators.operatorframework.io.bundle.manifests.v1: manifests/
      operators.operatorframework.io.bundle.mediatype.v1: registry+v1
      operators.operatorframework.io.bundle.metadata.v1: metadata/
      operators.operatorframework.io.bundle.package.v1: cluster-logging
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.


          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define scheduling rules that influence pod placement based on node or pod affinity/anti-affinity constraints.
          displayName: Affinity
          path: collector.affinity
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\nEach field path expression must start with a \".\"\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\nExamples:\n\n - `.kubernetes.namespace_name`\n\n - `.log_type`\n\n - '.kubernetes.labels.foobar'\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\nEach field path expression must start with a \".\"\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\nExamples:\n\n - `.kubernetes.namespace_name`\n\n - `.log_type`\n\n - '.kubernetes.labels.foobar'\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: 'Type of filter.


            Possible filter types are:


            1. detectMultilineException - Enables multi-line error detection of container logs. No additional configuration required.

            2. drop - Drop whole log records based on the evaluation of a set of regex tests. See field `drop` for configuration.

            3. kubeAPIAudit - Remove unwanted audit events and reduce event size to create a manageable audit trail. See field `kubeAPIaudit` for configuration.

            4. openshiftLabels - Labels to be applied to log records passing through a pipeline. See field `openshiftLabels` for configuration.

            5. parse - Enables parsing of log entries into structured logs. No additional configuration required.

            6. prune - Prune log record fields to reduce the size of logs flowing into a log store. See field `prune` for configuration.'
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.


            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.


            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.


            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.


            Only messages from pods with these labels are collected.


            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: 'Format is the format of incoming log data.


            The only currently supported format is `kubeAPIAudit`.'
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.


            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: "Type of Receiver plugin.\n\nSupported Receiver types are:\n\n1. http\n   - Currently only supports kubernetes audit logs (log_type = \"audit\")\n2. syslog\n   - Currently only supports node infrastucture logs (log_type = \"infrastructure\")"
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - description: AzureMonitor configures forwarding log events to the Azure Monitor Logs service
          displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - description: Cloudwatch configures forwarding log events to AWS Cloudwatch logs
          displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by \"\\|\\|\" followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets \"{}\" and MUST end with a static fallback value separated with \"\\|\\|\".\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar\\|\\|\"none\"}\n\n 2. {.foo\\|\\|.bar\\|\\|\"missing\"}\n\n 3. foo.{.bar.baz\\|\\|.qux.quux.corge\\|\\|.grault\\|\\|\"nil\"}-waldo.fred{.plugh\\|\\|\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.


            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Elasticsearch configures forwarding log events to an Elasticsearch cluster
          displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nWhen forwarding logs to the Red Hat Managed Elasticsearch, the index must match the pattern ^(app|infra|audit)-write$\nwhere the prefix depends upon the log_type. This requires defining a distinct output for each log type or distinct pipelines\nwith the openshiftLabels filter. See the product documentation for examples.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: GoogleCloudLogging configures forwarding log events to GCP (formally Stackdriver) Operations
          displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - description: HTTP configures forwarding log events to an HTTP server
          displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the HTTP method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Kafka configures forwarding log events to Apache Kafka topics
          displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.


            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.

            If provided, it must be a valid URL with a ''tcp'' or ''tls'' scheme.

            If none is provided, the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to. This field is optional.

            If provided, it must be a valid URL with a ''tcp'' or ''tls'' scheme, for example: ''tls://kafka.secure.com:9093/app-topic''.

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Loki configures forwarding log events to a Loki aggregation system
          displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.


            If LabelKeys is not set, the default keys are:


            - log_type


            - kubernetes.container_name


            - kubernetes.namespace_name


            - kubernetes.pod_name


            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.


            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.


            For example the default log record keys translate to these Loki labels:


            - log_type


            - kubernetes_container_name


            - kubernetes_namespace_name


            - kubernetes_pod_name


            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LokiStack configures forwarding log events to a Red Hat managed Loki deployment

            using the Red Hat tenancy model'
          displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "DataModel can be used to customize how log data is stored in LokiStack.\n\nThere are two different models to choose from:\n\n - Viaq\n - Otel\n\nWhen the data model is not set, it currently defaults to the \"Viaq\" data model."
          displayName: Data Model
          path: outputs[0].lokiStack.dataModel
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.


            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.


            For example the default log record keys translate to these Loki labels:


            - log_type


            - kubernetes_container_name


            - kubernetes_namespace_name


            - kubernetes_pod_name


            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\nIf LabelKeys is not set, the default keys are:\n\n - log_type\n\n - kubernetes.container_name\n\n - kubernetes.namespace_name\n\n - kubernetes.pod_name\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Namespace of the in-cluster LokiStack resource.
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - description: 'OTLP configures forwarding log events to a receiver using the OpenTelemetry Protocol

            with Red Openshift logging semantic conventions (ref: https://github.com/rhobs/observability-data-model/blob/main/cluster-logging.md)'
          displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.


            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`


            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Splunk configures forwarding log events to Splunk's HTTP event collector
          displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IndexedFields are the list of fields to be indexed by Splunk, increase storage usage, they should be used sparingly and only for high-value fields that provide significant search benefits.

            Nested fields are flattened into top-level fields.

            Field paths are joined using dot notation, and unsupported characters are replaced with underscores (_).

            Non-string values are automatically converted to strings (e.g., 3 → "3", true → "true").

            Object values serialized as JSON strings (e.g., { status: 200 } → "{\"status\":200}").


            Examples: [`.kubernetes`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`]'
          displayName: Indexed Fields
          path: outputs[0].splunk.indexedFields
        - description: 'PayloadKey specifies record field to use as payload.

            The PayloadKey must be a single field path.


            Field paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.


            By default, payloadKey is not set, which means the complete log record is forwarded as the payload.

            Use payloadKey carefully. Selecting a single field as the payload may cause other important information in the

            log to be dropped, potentially leading to inconsistent or incomplete log events.


            Examples: `.kubernetes`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Payload Key
          path: outputs[0].splunk.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Source identifies the origin of a log event.\nThe Source can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\nIf not specified will be detected according to .log_source and .log_type value.\nDetails see in: docs/features/logforwarding/outputs/splunk-forwarding.adoc\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Source
          path: outputs[0].splunk.source
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Enrichment is an additional modification to the log message before forwarding it to the receiver.\n\nSupported values are:\n1. None\n   - Adds no additional enrichment to the record\n2. KubernetesMinimal\n   - Adds namespace_name, pod_name, and container_name to the beginning of the message body (e.g. namespace_name=myproject, container_name=server, pod_name=pod-123, message={\"foo\":\"bar\"}).\nThis may result in the message body being an invalid JSON structure."
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7\n\nThe Facility can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. {.foo||\"user\"}"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\nIf left empty, Syslog will use the whole message as the payload key\n\nExample:\n\n 1. {.bar}\n\n 2. {.foo.bar.baz}\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'The RFC to which the generated messages conform to.


            Supported values are:

            1. RFC3164

            2. RFC5424'
          displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n    Emergency Alert Critical Error Warning Notice Informational Debug\n\nThe Severity can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. {.foo||\"Error\"}"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp`\nFor example, to send syslog records using UDP:\n    url: udp://syslog.example.com:514"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.


            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.


            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\nThe following built-in input names are always available:\n\n - `application` selects all logs from application pods.\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:56ee655fc9fd5aca984a7c552c00112b7314d2e79117f58c469ef91f3cefe640
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:61ced99002f57d20727adc2dbad34b99c1347541b4605c5ec2847db577532dc5
  name: ''
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:c7bfa11f2cf8403ef437bdcf514fe78823472e578766192f6742322750f1df18
  name: log-file-metric-exporter
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:5eeb9f00760e286b2088fe4384bc0de5b18178fb3811796608ffe98d1100312e
  name: vector
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:95ffe34c4060b0e903225c4494bcd551210d5d7f95d293b86292a76235fad859
name: cluster-logging.v6.3.3
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: '4.20'
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.3.3
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing, Observability
      certified: 'false'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:30398a6bd735501cfa28004f6c53ae25000aee964ac22524ac53d36ac7bffd0f
      createdAt: '2025-12-12T18:19:58Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=6.1.0-0 <6.3.3'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/must-gather-image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:30398a6bd735501cfa28004f6c53ae25000aee964ac22524ac53d36ac7bffd0f
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.


          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define scheduling rules that influence pod placement based on node or pod affinity/anti-affinity constraints.
          displayName: Affinity
          path: collector.affinity
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\nEach field path expression must start with a \".\"\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\nExamples:\n\n - `.kubernetes.namespace_name`\n\n - `.log_type`\n\n - '.kubernetes.labels.foobar'\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\nEach field path expression must start with a \".\"\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\nExamples:\n\n - `.kubernetes.namespace_name`\n\n - `.log_type`\n\n - '.kubernetes.labels.foobar'\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: 'Type of filter.


            Possible filter types are:


            1. detectMultilineException - Enables multi-line error detection of container logs. No additional configuration required.

            2. drop - Drop whole log records based on the evaluation of a set of regex tests. See field `drop` for configuration.

            3. kubeAPIAudit - Remove unwanted audit events and reduce event size to create a manageable audit trail. See field `kubeAPIaudit` for configuration.

            4. openshiftLabels - Labels to be applied to log records passing through a pipeline. See field `openshiftLabels` for configuration.

            5. parse - Enables parsing of log entries into structured logs. No additional configuration required.

            6. prune - Prune log record fields to reduce the size of logs flowing into a log store. See field `prune` for configuration.'
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.


            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.


            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.


            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.


            Only messages from pods with these labels are collected.


            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'MaxMessageSize  The maximum message length in bytes that a single log event can be when all

            partial log lines are merged.  Messages exceeding this limit are dropped.'
          displayName: Max Message Size
          path: inputs[0].application.tuning.maxMessageSize
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Tuning is the infrastructure input tuning spec, for now available only for container sources
          displayName: Container Input Tuning
          path: inputs[0].infrastructure.tuning
        - description: Container is the input tuning spec for container sources
          displayName: Input Tuning
          path: inputs[0].infrastructure.tuning.container
        - description: 'MaxMessageSize  The maximum message length in bytes that a single log event can be when all

            partial log lines are merged.  Messages exceeding this limit are dropped.'
          displayName: Max Message Size
          path: inputs[0].infrastructure.tuning.container.maxMessageSize
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].infrastructure.tuning.container.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].infrastructure.tuning.container.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: 'Format is the format of incoming log data.


            The only currently supported format is `kubeAPIAudit`.'
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.


            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: "Type of Receiver plugin.\n\nSupported Receiver types are:\n\n1. http\n   - Currently only supports kubernetes audit logs (log_type = \"audit\")\n2. syslog\n   - Currently only supports node infrastucture logs (log_type = \"infrastructure\")"
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - description: AzureMonitor configures forwarding log events to the Azure Monitor Logs service
          displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - description: Cloudwatch configures forwarding log events to AWS Cloudwatch logs
          displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by \"\\|\\|\" followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets \"{}\" and MUST end with a static fallback value separated with \"\\|\\|\".\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar\\|\\|\"none\"}\n\n 2. {.foo\\|\\|.bar\\|\\|\"missing\"}\n\n 3. foo.{.bar.baz\\|\\|.qux.quux.corge\\|\\|.grault\\|\\|\"nil\"}-waldo.fred{.plugh\\|\\|\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.


            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Elasticsearch configures forwarding log events to an Elasticsearch cluster
          displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nWhen forwarding logs to the Red Hat Managed Elasticsearch, the index must match the pattern ^(app|infra|audit)-write$\nwhere the prefix depends upon the log_type. This requires defining a distinct output for each log type or distinct pipelines\nwith the openshiftLabels filter. See the product documentation for examples.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: GoogleCloudLogging configures forwarding log events to GCP (formally Stackdriver) Operations
          displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - description: HTTP configures forwarding log events to an HTTP server
          displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the HTTP method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Kafka configures forwarding log events to Apache Kafka topics
          displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.


            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.

            If provided, it must be a valid URL with a ''tcp'' or ''tls'' scheme.

            If none is provided, the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to. This field is optional.

            If provided, it must be a valid URL with a ''tcp'' or ''tls'' scheme, for example: ''tls://kafka.secure.com:9093/app-topic''.

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Loki configures forwarding log events to a Loki aggregation system
          displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.


            If LabelKeys is not set, the default keys are:


            - log_type


            - kubernetes.container_name


            - kubernetes.namespace_name


            - kubernetes.pod_name


            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.


            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.


            For example the default log record keys translate to these Loki labels:


            - log_type


            - kubernetes_container_name


            - kubernetes_namespace_name


            - kubernetes_pod_name


            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LokiStack configures forwarding log events to a Red Hat managed Loki deployment

            using the Red Hat tenancy model'
          displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "DataModel can be used to customize how log data is stored in LokiStack.\n\nThere are two different models to choose from:\n\n - Viaq\n - Otel\n\nWhen the data model is not set, it currently defaults to the \"Viaq\" data model."
          displayName: Data Model
          path: outputs[0].lokiStack.dataModel
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.


            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.


            For example the default log record keys translate to these Loki labels:


            - log_type


            - kubernetes_container_name


            - kubernetes_namespace_name


            - kubernetes_pod_name


            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\nIf LabelKeys is not set, the default keys are:\n\n - log_type\n\n - kubernetes.container_name\n\n - kubernetes.namespace_name\n\n - kubernetes.pod_name\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Namespace of the in-cluster LokiStack resource.
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - description: 'OTLP configures forwarding log events to a receiver using the OpenTelemetry Protocol

            with Red Openshift logging semantic conventions (ref: https://github.com/rhobs/observability-data-model/blob/main/cluster-logging.md)'
          displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.


            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`


            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Splunk configures forwarding log events to Splunk's HTTP event collector
          displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IndexedFields are the list of fields to be indexed by Splunk, increase storage usage, they should be used sparingly and only for high-value fields that provide significant search benefits.

            Nested fields are flattened into top-level fields.

            Field paths are joined using dot notation, and unsupported characters are replaced with underscores (_).

            Non-string values are automatically converted to strings (e.g., 3 → "3", true → "true").

            Object values serialized as JSON strings (e.g., { status: 200 } → "{\"status\":200}").


            Examples: [`.kubernetes`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`]'
          displayName: Indexed Fields
          path: outputs[0].splunk.indexedFields
        - description: 'PayloadKey specifies record field to use as payload.

            The PayloadKey must be a single field path.


            Field paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.


            By default, payloadKey is not set, which means the complete log record is forwarded as the payload.

            Use payloadKey carefully. Selecting a single field as the payload may cause other important information in the

            log to be dropped, potentially leading to inconsistent or incomplete log events.


            Examples: `.kubernetes`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Payload Key
          path: outputs[0].splunk.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Source identifies the origin of a log event.\nThe Source can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\nIf not specified will be detected according to .log_source and .log_type value.\nDetails see in: docs/features/logforwarding/outputs/splunk-forwarding.adoc\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Source
          path: outputs[0].splunk.source
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Enrichment is an additional modification to the log message before forwarding it to the receiver.\n\nSupported values are:\n1. None\n   - Adds no additional enrichment to the record\n2. KubernetesMinimal\n   - Adds namespace_name, pod_name, and container_name to the beginning of the message body (e.g. namespace_name=myproject, container_name=server, pod_name=pod-123, message={\"foo\":\"bar\"}).\nThis may result in the message body being an invalid JSON structure."
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7\n\nThe Facility can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. {.foo||\"user\"}"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\nIf left empty, Syslog will use the whole message as the payload key\n\nExample:\n\n 1. {.bar}\n\n 2. {.foo.bar.baz}\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'The RFC to which the generated messages conform to.


            Supported values are:

            1. RFC3164

            2. RFC5424'
          displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n    Emergency Alert Critical Error Warning Notice Informational Debug\n\nThe Severity can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. {.foo||\"Error\"}"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp`\nFor example, to send syslog records using UDP:\n    url: udp://syslog.example.com:514"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.


            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.


            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\nThe following built-in input names are always available:\n\n - `application` selects all logs from application pods.\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:95ffe34c4060b0e903225c4494bcd551210d5d7f95d293b86292a76235fad859
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:30398a6bd735501cfa28004f6c53ae25000aee964ac22524ac53d36ac7bffd0f
  name: cluster-logging-rhel9-operator
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:0d0a40ef6fac7353d70764d7c90edc1f31aba3bf85c94a0d236a6dc36003dcb7
  name: log-file-metric-exporter-rhel9
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:99f9509d7983a176e9ea93f24e97ccc24a4168794f7ad360f0e774e9c9ebe262
  name: vector-rhel9
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:ef8278e7016f18b9acaf38d4d114d2033d7bb29bd6cfafeedeaeae9686edeeeb
name: cluster-logging.v6.3.4
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: '4.20'
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.3.4
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing, Observability
      certified: 'false'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:aef6bd625afeef1cb73da59a000dbd6064f77529bfb325a4dcbd068ca6554503
      createdAt: '2025-12-12T18:19:58Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=6.1.0-0 <6.3.4'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/must-gather-image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:aef6bd625afeef1cb73da59a000dbd6064f77529bfb325a4dcbd068ca6554503
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.


          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define scheduling rules that influence pod placement based on node or pod affinity/anti-affinity constraints.
          displayName: Affinity
          path: collector.affinity
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\nEach field path expression must start with a \".\"\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\nExamples:\n\n - `.kubernetes.namespace_name`\n\n - `.log_type`\n\n - '.kubernetes.labels.foobar'\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\nEach field path expression must start with a \".\"\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\nExamples:\n\n - `.kubernetes.namespace_name`\n\n - `.log_type`\n\n - '.kubernetes.labels.foobar'\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: 'Type of filter.


            Possible filter types are:


            1. detectMultilineException - Enables multi-line error detection of container logs. No additional configuration required.

            2. drop - Drop whole log records based on the evaluation of a set of regex tests. See field `drop` for configuration.

            3. kubeAPIAudit - Remove unwanted audit events and reduce event size to create a manageable audit trail. See field `kubeAPIaudit` for configuration.

            4. openshiftLabels - Labels to be applied to log records passing through a pipeline. See field `openshiftLabels` for configuration.

            5. parse - Enables parsing of log entries into structured logs. No additional configuration required.

            6. prune - Prune log record fields to reduce the size of logs flowing into a log store. See field `prune` for configuration.'
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.


            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.


            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.


            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.


            Only messages from pods with these labels are collected.


            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'MaxMessageSize  The maximum message length in bytes that a single log event can be when all

            partial log lines are merged.  Messages exceeding this limit are dropped.'
          displayName: Max Message Size
          path: inputs[0].application.tuning.maxMessageSize
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Tuning is the infrastructure input tuning spec, for now available only for container sources
          displayName: Container Input Tuning
          path: inputs[0].infrastructure.tuning
        - description: Container is the input tuning spec for container sources
          displayName: Input Tuning
          path: inputs[0].infrastructure.tuning.container
        - description: 'MaxMessageSize  The maximum message length in bytes that a single log event can be when all

            partial log lines are merged.  Messages exceeding this limit are dropped.'
          displayName: Max Message Size
          path: inputs[0].infrastructure.tuning.container.maxMessageSize
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].infrastructure.tuning.container.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].infrastructure.tuning.container.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: 'Format is the format of incoming log data.


            The only currently supported format is `kubeAPIAudit`.'
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.


            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: "Type of Receiver plugin.\n\nSupported Receiver types are:\n\n1. http\n   - Currently only supports kubernetes audit logs (log_type = \"audit\")\n2. syslog\n   - Currently only supports node infrastucture logs (log_type = \"infrastructure\")"
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - description: AzureMonitor configures forwarding log events to the Azure Monitor Logs service
          displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - description: Cloudwatch configures forwarding log events to AWS Cloudwatch logs
          displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by \"\\|\\|\" followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets \"{}\" and MUST end with a static fallback value separated with \"\\|\\|\".\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar\\|\\|\"none\"}\n\n 2. {.foo\\|\\|.bar\\|\\|\"missing\"}\n\n 3. foo.{.bar.baz\\|\\|.qux.quux.corge\\|\\|.grault\\|\\|\"nil\"}-waldo.fred{.plugh\\|\\|\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.


            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Elasticsearch configures forwarding log events to an Elasticsearch cluster
          displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nWhen forwarding logs to the Red Hat Managed Elasticsearch, the index must match the pattern ^(app|infra|audit)-write$\nwhere the prefix depends upon the log_type. This requires defining a distinct output for each log type or distinct pipelines\nwith the openshiftLabels filter. See the product documentation for examples.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: GoogleCloudLogging configures forwarding log events to GCP (formally Stackdriver) Operations
          displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - description: HTTP configures forwarding log events to an HTTP server
          displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the HTTP method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Kafka configures forwarding log events to Apache Kafka topics
          displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.


            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.

            If provided, it must be a valid URL with a ''tcp'' or ''tls'' scheme.

            If none is provided, the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to. This field is optional.

            If provided, it must be a valid URL with a ''tcp'' or ''tls'' scheme, for example: ''tls://kafka.secure.com:9093/app-topic''.

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Loki configures forwarding log events to a Loki aggregation system
          displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.


            If LabelKeys is not set, the default keys are:


            - log_type


            - kubernetes.container_name


            - kubernetes.namespace_name


            - kubernetes.pod_name


            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.


            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.


            For example the default log record keys translate to these Loki labels:


            - log_type


            - kubernetes_container_name


            - kubernetes_namespace_name


            - kubernetes_pod_name


            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LokiStack configures forwarding log events to a Red Hat managed Loki deployment

            using the Red Hat tenancy model'
          displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "DataModel can be used to customize how log data is stored in LokiStack.\n\nThere are two different models to choose from:\n\n - Viaq\n - Otel\n\nWhen the data model is not set, it currently defaults to the \"Viaq\" data model."
          displayName: Data Model
          path: outputs[0].lokiStack.dataModel
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.


            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.


            For example the default log record keys translate to these Loki labels:


            - log_type


            - kubernetes_container_name


            - kubernetes_namespace_name


            - kubernetes_pod_name


            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\nIf LabelKeys is not set, the default keys are:\n\n - log_type\n\n - kubernetes.container_name\n\n - kubernetes.namespace_name\n\n - kubernetes.pod_name\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Namespace of the in-cluster LokiStack resource.
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - description: 'OTLP configures forwarding log events to a receiver using the OpenTelemetry Protocol

            with Red Openshift logging semantic conventions (ref: https://github.com/rhobs/observability-data-model/blob/main/cluster-logging.md)'
          displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.


            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`


            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Splunk configures forwarding log events to Splunk's HTTP event collector
          displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IndexedFields are the list of fields to be indexed by Splunk, increase storage usage, they should be used sparingly and only for high-value fields that provide significant search benefits.

            Nested fields are flattened into top-level fields.

            Field paths are joined using dot notation, and unsupported characters are replaced with underscores (_).

            Non-string values are automatically converted to strings (e.g., 3 → "3", true → "true").

            Object values serialized as JSON strings (e.g., { status: 200 } → "{\"status\":200}").


            Examples: [`.kubernetes`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`]'
          displayName: Indexed Fields
          path: outputs[0].splunk.indexedFields
        - description: 'PayloadKey specifies record field to use as payload.

            The PayloadKey must be a single field path.


            Field paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.


            By default, payloadKey is not set, which means the complete log record is forwarded as the payload.

            Use payloadKey carefully. Selecting a single field as the payload may cause other important information in the

            log to be dropped, potentially leading to inconsistent or incomplete log events.


            Examples: `.kubernetes`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Payload Key
          path: outputs[0].splunk.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Source identifies the origin of a log event.\nThe Source can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\nIf not specified will be detected according to .log_source and .log_type value.\nDetails see in: docs/features/logforwarding/outputs/splunk-forwarding.adoc\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Source
          path: outputs[0].splunk.source
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Enrichment is an additional modification to the log message before forwarding it to the receiver.\n\nSupported values are:\n1. None\n   - Adds no additional enrichment to the record\n2. KubernetesMinimal\n   - Adds namespace_name, pod_name, and container_name to the beginning of the message body (e.g. namespace_name=myproject, container_name=server, pod_name=pod-123, message={\"foo\":\"bar\"}).\nThis may result in the message body being an invalid JSON structure."
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7\n\nThe Facility can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. {.foo||\"user\"}"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\nIf left empty, Syslog will use the whole message as the payload key\n\nExample:\n\n 1. {.bar}\n\n 2. {.foo.bar.baz}\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'The RFC to which the generated messages conform to.


            Supported values are:

            1. RFC3164

            2. RFC5424'
          displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n    Emergency Alert Critical Error Warning Notice Informational Debug\n\nThe Severity can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. {.foo||\"Error\"}"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp`\nFor example, to send syslog records using UDP:\n    url: udp://syslog.example.com:514"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.


            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.


            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\nThe following built-in input names are always available:\n\n - `application` selects all logs from application pods.\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:ef8278e7016f18b9acaf38d4d114d2033d7bb29bd6cfafeedeaeae9686edeeeb
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:aef6bd625afeef1cb73da59a000dbd6064f77529bfb325a4dcbd068ca6554503
  name: cluster-logging-rhel9-operator
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:d4d4131515c8faf77df0d6209d3e57ce5707637d81a4f386d47c3163ccbee279
  name: log-file-metric-exporter-rhel9
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:ba551a55aad2ce9eb0ddc18c781959de0c3cc86f2a3f79249e312f2a44f86bde
  name: vector-rhel9
schema: olm.bundle
---
image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:98ccff244568d2e388e2f8c270d2ce47515b8254754b43b2deefd63bdd4dd19b
name: cluster-logging.v6.0.15
package: cluster-logging
properties:
- type: olm.gvk
  value:
    group: logging.openshift.io
    kind: LogFileMetricExporter
    version: v1alpha1
- type: olm.gvk
  value:
    group: observability.openshift.io
    kind: ClusterLogForwarder
    version: v1
- type: olm.maxOpenShiftVersion
  value: 4.17
- type: olm.package
  value:
    packageName: cluster-logging
    version: 6.0.15
- type: olm.csv.metadata
  value:
    annotations:
      alm-examples: "[\n  {\n    \"apiVersion\": \"logging.openshift.io/v1alpha1\",\n    \"kind\": \"LogFileMetricExporter\",\n    \"metadata\": {\n      \"name\": \"instance\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"resources\": {\n        \"limits\": {\n          \"cpu\": \"500m\"\n        },\n        \"requests\": {\n          \"cpu\": \"200m\",\n          \"memory\": \"128Mi\"\n        }\n      },\n      \"tolerations\": [\n        {\n          \"effect\": \"NoSchedule\",\n          \"key\": \"node-role.kubernetes.io/master\",\n          \"operator\": \"Exists\"\n        }\n      ]\n    }\n  },\n  {\n    \"apiVersion\": \"observability.openshift.io/v1\",\n    \"kind\": \"ClusterLogForwarder\",\n    \"metadata\": {\n      \"name\": \"logging\",\n      \"namespace\": \"openshift-logging\"\n    },\n    \"spec\": {\n      \"outputs\": [\n        {\n          \"lokiStack\": {\n            \"authentication\": {\n              \"token\": {\n                \"from\": \"serviceAccount\"\n              }\n            },\n            \"target\": {\n              \"name\": \"logging-loki\"\n            }\n          },\n          \"name\": \"logging-loki\",\n          \"tls\": {\n            \"ca\": {\n              \"configMapName\": \"openshift-service-ca.crt\",\n              \"key\": \"service-ca.crt\"\n            }\n          },\n          \"type\": \"lokiStack\"\n        }\n      ],\n      \"pipelines\": [\n        {\n          \"inputRefs\": [\n            \"application\",\n            \"infrastructure\"\n          ],\n          \"name\": \"logs-to-loki\",\n          \"outputRefs\": [\n            \"logging-loki\"\n          ]\n        }\n      ],\n      \"serviceAccount\": {\n        \"name\": \"log-collector\"\n      }\n    }\n  }\n]"
      capabilities: Seamless Upgrades
      categories: OpenShift Optional, Logging & Tracing
      certified: 'false'
      containerImage: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:9563279ac943e6cecef10877d9298146bed6571bcef846574e06752cd3a7fac9
      createdAt: '2026-02-05T14:25:39Z'
      description: The Red Hat OpenShift Logging Operator for OCP provides a means for configuring and managing log collection and forwarding.
      features.operators.openshift.io/cnf: 'false'
      features.operators.openshift.io/cni: 'false'
      features.operators.openshift.io/csi: 'false'
      features.operators.openshift.io/disconnected: 'true'
      features.operators.openshift.io/fips-compliant: 'true'
      features.operators.openshift.io/proxy-aware: 'true'
      features.operators.openshift.io/tls-profiles: 'true'
      features.operators.openshift.io/token-auth-aws: 'false'
      features.operators.openshift.io/token-auth-azure: 'false'
      features.operators.openshift.io/token-auth-gcp: 'false'
      olm.skipRange: '>=5.8.0-0 <6.0.15'
      operatorframework.io/cluster-monitoring: 'true'
      operatorframework.io/initialization-resource: "{\n  \"apiVersion\": \"observability.openshift.io/v1\",\n  \"kind\": \"ClusterLogForwarder\",\n  \"metadata\": {\n    \"name\": \"logging\"\n  }\n}\n"
      operatorframework.io/suggested-namespace: openshift-logging
      operators.openshift.io/infrastructure-features: '["disconnected","proxy-aware"]'
      operators.openshift.io/must-gather-image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:9563279ac943e6cecef10877d9298146bed6571bcef846574e06752cd3a7fac9
      operators.openshift.io/valid-subscription: '["OpenShift Kubernetes Engine", "OpenShift Container Platform", "OpenShift Platform Plus"]'
      operators.operatorframework.io/builder: operator-sdk-unknown
      operators.operatorframework.io/project_layout: go.kubebuilder.io/v4
      support: AOS Logging (aos-logging@redhat.com)
      target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
    apiServiceDefinitions: {}
    crdDescriptions:
      owned:
      - description: 'ClusterLogForwarder is an API to configure forwarding logs.


          You configure forwarding by specifying a list of `pipelines`,

          which forward from a set of named inputs to a set of named outputs.'
        displayName: Cluster Log Forwarder
        kind: ClusterLogForwarder
        name: clusterlogforwarders.observability.openshift.io
        specDescriptors:
        - description: 'Specification of the Collector deployment to define

            resource limits and workload placement'
          displayName: Collector Resources and Placement
          path: collector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:advanced
        - description: Define nodes for scheduling the pods.
          displayName: Node Selector
          path: collector.nodeSelector
        - description: The resource requirements for the collector
          displayName: Collector Resource Requirements
          path: collector.resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the collector pods will accept
          displayName: Tolerations
          path: collector.tolerations
        - description: 'Filters are applied to log records passing through a pipeline.

            There are different types of filter that can select and modify log records in different ways.

            See [FilterTypeSpec] for a list of filter types.'
          displayName: Log Forwarder Pipeline Filters
          path: filters
        - description: 'A drop filter applies a sequence of tests to a log record and drops the record if any test passes.

            Each test contains a sequence of conditions, all conditions must be true for the test to pass.

            A DropTestsSpec contains an array of tests which contains an array of conditions'
          displayName: Drop Filters
          path: filters[0].drop
        - description: DropConditions is an array of DropCondition which are conditions that are ANDed together
          displayName: Drop Filter Conditions
          path: filters[0].drop[0].test
        - description: 'A dot delimited path to a field in the log record. It must start with a `.`.

            The path can contain alpha-numeric characters and underscores (a-zA-Z0-9_).

            If segments contain characters outside of this range, the segment must be quoted.

            Examples: `.kubernetes.namespace_name`, `.log_type`, ''.kubernetes.labels.foobar'', `.kubernetes.labels."foo-bar/baz"`'
          displayName: Field Path
          path: filters[0].drop[0].test[0].field
        - description: 'A regular expression that the field will match.

            If the value of the field defined in the DropTest matches the regular expression, the log record will be dropped.

            Must define only one of matches OR notMatches'
          displayName: Drop Match Expression
          path: filters[0].drop[0].test[0].matches
        - description: 'A regular expression that the field does not match.

            If the value of the field defined in the DropTest does not match the regular expression, the log record will be dropped.

            Must define only one of matches or notMatches'
          displayName: Keep Match Expression
          path: filters[0].drop[0].test[0].notMatches
        - displayName: Kubernetes API Audit Filter
          path: filters[0].kubeAPIAudit
        - description: Name used to refer to the filter from a "pipeline".
          displayName: Filter Name
          path: filters[0].name
        - description: 'Labels applied to log records passing through a pipeline.

            These labels appear in the `openshift.labels` map in the log record.'
          displayName: Labels
          path: filters[0].openshiftLabels
        - description: The PruneFilterSpec consists of two arrays, namely in and notIn, which dictate the fields to be pruned.
          displayName: Prune Filters
          path: filters[0].prune
        - description: "`In` is an array of dot-delimited field paths. Fields included here are removed from the log record.\n\nEach field path expression must start with a \".\"\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\nExamples:\n\n - `.kubernetes.namespace_name`\n\n - `.log_type`\n\n - '.kubernetes.labels.foobar'\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\nNOTE1: `In` CANNOT contain `.log_type` or `.message` as those fields are required and cannot be pruned.\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` CANNOT be added to this list as it is a required field."
          displayName: Fields to be dropped
          path: filters[0].prune.in
        - description: "`NotIn` is an array of dot-delimited field paths. All fields besides the ones listed here are removed from the log record.\n\nEach field path expression must start with a \".\"\n\nThe path can contain alphanumeric characters and underscores (a-zA-Z0-9_).\n\nIf segments contain characters outside of this range, the segment must be quoted otherwise paths do NOT need to be quoted.\n\nExamples:\n\n - `.kubernetes.namespace_name`\n\n - `.log_type`\n\n - '.kubernetes.labels.foobar'\n\n - `.kubernetes.labels.\"foo-bar/baz\"`\n\nNOTE1: `NotIn` MUST contain `.log_type` and `.message` as those fields are required and cannot be pruned.\n\nNOTE2: If this filter is used in a pipeline with GoogleCloudLogging, `.hostname` MUST be added to this list as it is a required field."
          displayName: Fields to be kept
          path: filters[0].prune.notIn
        - description: Type of filter.
          displayName: Filter Type
          path: filters[0].type
        - description: 'Inputs are named filters for log messages to be forwarded.


            There are three built-in inputs named `application`, `infrastructure` and

            `audit`. You don''t need to define inputs here if those are sufficient for

            your needs. See `inputRefs` for more.'
          displayName: Log Forwarder Inputs
          path: inputs
        - description: 'Application, named set of `application` logs that

            can specify a set of match criteria'
          displayName: Application Logs Input
          path: inputs[0].application
        - description: 'Excludes is the set of namespaces and containers to ignore when collecting logs.


            Takes precedence over Includes option.'
          displayName: Exclude
          path: inputs[0].application.excludes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.excludes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.excludes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Includes is the set of namespaces and containers to include when collecting logs.


            Note: infrastructure namespaces are still excluded for "*" values unless a qualifying glob pattern is specified.'
          displayName: Include
          path: inputs[0].application.includes
        - description: 'Container spec the containers from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Container Glob
          path: inputs[0].application.includes[0].container
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace specs the namespace from which to collect logs

            Supports glob patterns and presumes "*" if omitted.'
          displayName: Namespace Glob
          path: inputs[0].application.includes[0].namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Selector for logs from pods with matching labels.


            Only messages from pods with these labels are collected.


            If absent or empty, logs are collected regardless of labels.'
          displayName: Pod Selector
          path: inputs[0].application.selector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Pod
        - description: Tuning is the container input tuning spec for this container sources
          displayName: Input Tuning
          path: inputs[0].application.tuning
        - description: 'RateLimitPerContainer is the limit applied to each container

            by this input. This limit is applied per collector deployment.'
          displayName: Per-Container Rate Limit
          path: inputs[0].application.tuning.rateLimitPerContainer
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: inputs[0].application.tuning.rateLimitPerContainer.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Audit, enables `audit` logs.
          displayName: Audit Logs Input
          path: inputs[0].audit
        - description: 'Sources defines the list of audit sources to collect.

            This field is optional and its exclusion results in the collection of all audit sources.'
          displayName: Log Sources
          path: inputs[0].audit.sources
        - description: Infrastructure, Enables `infrastructure` logs.
          displayName: Infrastructure Logs Input
          path: inputs[0].infrastructure
        - description: 'Sources defines the list of infrastructure sources to collect.

            This field is optional and omission results in the collection of all infrastructure sources.'
          displayName: Log Sources
          path: inputs[0].infrastructure.sources
        - description: Name used to refer to the input of a `pipeline`.
          displayName: Input Name
          path: inputs[0].name
        - description: Receiver to receive logs from non-cluster sources.
          displayName: Log Receiver
          path: inputs[0].receiver
        - displayName: HTTP Receiver Configuration
          path: inputs[0].receiver.http
        - description: Format is the format of incoming log data.
          displayName: Data Format
          path: inputs[0].receiver.http.format
        - description: Port the Receiver listens on. It must be a value between 1024 and 65535
          displayName: Listen Port
          path: inputs[0].receiver.port
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: 'TLS contains settings for controlling options of TLS connections.


            The operator will request certificates from the cluster''s cert signing service when TLS is not defined.

            The certificates are injected into a secret named "<clusterlogforwarder.name>-<input.name>" which is mounted into

            the collector. The collector is configured to use the public and private key provided by the service'
          displayName: TLS Options
          path: inputs[0].receiver.tls
        - description: Type of Receiver plugin.
          displayName: Receiver Type
          path: inputs[0].receiver.type
        - description: Type of output sink.
          displayName: Input Type
          path: inputs[0].type
        - description: Indicator if the resource is 'Managed' or 'Unmanaged' by the operator.
          displayName: Management State
          path: managementState
        - description: Outputs are named destinations for log messages.
          displayName: Log Forwarder Outputs
          path: outputs
        - displayName: Azure Monitor
          path: outputs[0].azureMonitor
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].azureMonitor.authentication
        - description: SharedKey points to the secret containing the shared key used for authenticating requests.
          displayName: Shared Key
          path: outputs[0].azureMonitor.authentication.sharedKey
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].azureMonitor.authentication.sharedKey.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].azureMonitor.authentication.sharedKey.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'AzureResourceId the Resource ID of the Azure resource the data should be associated with.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Azure Resource ID
          path: outputs[0].azureMonitor.azureResourceId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'CustomerId che unique identifier for the Log Analytics workspace.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-uri-parameters'
          displayName: Customer ID
          path: outputs[0].azureMonitor.customerId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Host alternative host for dedicated Azure regions. (for example for China region)

            https://docs.azure.cn/en-us/articles/guidance/developerdifferences#check-endpoints-in-azure'
          displayName: Azure Host
          path: outputs[0].azureMonitor.host
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LogType the record type of the data that is being submitted.

            Can only contain letters, numbers, and underscores (_), and may not exceed 100 characters.

            https://learn.microsoft.com/en-us/azure/azure-monitor/logs/data-collector-api?tabs=powershell#request-headers'
          displayName: Log Type
          path: outputs[0].azureMonitor.logType
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].azureMonitor.tuning
        - displayName: Delivery Mode
          path: outputs[0].azureMonitor.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].azureMonitor.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].azureMonitor.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].azureMonitor.tuning.minRetryDuration
        - displayName: Amazon CloudWatch
          path: outputs[0].cloudwatch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].cloudwatch.authentication
        - description: AWSAccessKey points to the AWS access key id and secret to be used for authentication.
          displayName: Access Key
          path: outputs[0].cloudwatch.authentication.awsAccessKey
        - description: KeyId points to the AWS access key id to be used for authentication.
          displayName: Secret with Access Key ID
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keyId.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeySecret points to the AWS access key secret to be used for authentication.
          displayName: Secret with Access Key Secret
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.awsAccessKey.keySecret.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'IAMRole points to the secret containing the role ARN to be used for authentication.

            This can be used for authentication in STS-enabled clusters when additionally specifying

            a web identity token'
          displayName: Amazon IAM Role
          path: outputs[0].cloudwatch.authentication.iamRole
        - description: 'RoleARN points to the secret containing the role ARN to be used for authentication.

            This is used for authentication in STS-enabled clusters.'
          displayName: RoleARN Secret
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.roleARN.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Token
          path: outputs[0].cloudwatch.authentication.iamRole.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].cloudwatch.authentication.iamRole.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].cloudwatch.authentication.iamRole.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Type is the type of cloudwatch authentication to configure
          displayName: Authentication Type
          path: outputs[0].cloudwatch.authentication.type
        - description: "GroupName defines the strategy for grouping logstreams\n\nThe GroupName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Group Name
          path: outputs[0].cloudwatch.groupName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Amazon Region
          path: outputs[0].cloudwatch.region
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].cloudwatch.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].cloudwatch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].cloudwatch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].cloudwatch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].cloudwatch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].cloudwatch.tuning.minRetryDuration
        - description: 'URL to send log records to.


            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].cloudwatch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: ElasticSearch
          path: outputs[0].elasticsearch
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].elasticsearch.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].elasticsearch.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].elasticsearch.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].elasticsearch.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].elasticsearch.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].elasticsearch.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].elasticsearch.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].elasticsearch.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Index
          path: outputs[0].elasticsearch.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].elasticsearch.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].elasticsearch.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].elasticsearch.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].elasticsearch.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].elasticsearch.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].elasticsearch.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].elasticsearch.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Version specifies the version of Elasticsearch to be used.

            Must be one of: 6-8'
          displayName: ElasticSearch Version
          path: outputs[0].elasticsearch.version
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Google Cloud Logging
          path: outputs[0].googleCloudLogging
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].googleCloudLogging.authentication
        - description: Credentials points to the secret containing the `google-application-credentials.json`.
          displayName: Secret with Credentials File
          path: outputs[0].googleCloudLogging.authentication.credentials
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].googleCloudLogging.authentication.credentials.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].googleCloudLogging.authentication.credentials.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: ID must be one of the required ID fields for the output
          displayName: Logging ID
          path: outputs[0].googleCloudLogging.id
        - description: Type is the ID type provided
          displayName: Logging ID Type
          path: outputs[0].googleCloudLogging.id.type
        - description: Value is the value of the ID
          displayName: Logging ID Value
          path: outputs[0].googleCloudLogging.id.value
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "LogId is the log ID to which to publish logs. This identifies log stream.\n\nThe LogId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Log Stream ID
          path: outputs[0].googleCloudLogging.logId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].googleCloudLogging.tuning
        - displayName: Delivery Mode
          path: outputs[0].googleCloudLogging.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].googleCloudLogging.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].googleCloudLogging.tuning.minRetryDuration
        - displayName: HTTP Output
          path: outputs[0].http
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].http.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].http.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].http.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].http.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].http.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].http.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].http.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].http.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].http.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Headers specify optional headers to be sent with the request
          displayName: Headers
          path: outputs[0].http.headers
        - description: Method specifies the Http method to be used for sending logs. If not set, 'POST' is used.
          displayName: HTTP Method
          path: outputs[0].http.method
        - description: Timeout specifies the Http request timeout in seconds. If not set, 10secs is used.
          displayName: Timeout
          path: outputs[0].http.timeout
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].http.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].http.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].http.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].http.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].http.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].http.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].http.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Apache Kafka
          path: outputs[0].kafka
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].kafka.authentication
        - description: SASL contains options configuring SASL authentication.
          displayName: SASL Options
          path: outputs[0].kafka.authentication.sasl
        - description: Mechanism sets the SASL mechanism to use.
          displayName: SASL Mechanism
          path: outputs[0].kafka.authentication.sasl.mechanism
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL password.
          displayName: Secret with Password
          path: outputs[0].kafka.authentication.sasl.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username points to the secret to be used as SASL username.
          displayName: Secret with Username
          path: outputs[0].kafka.authentication.sasl.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].kafka.authentication.sasl.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].kafka.authentication.sasl.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Brokers specifies the list of broker endpoints of a Kafka cluster.


            The list represents only the initial set used by the collector''s Kafka client for the

            first connection only. The collector''s Kafka client fetches constantly an updated list

            from Kafka. These updates are not reconciled back to the collector configuration.


            If none provided the target URL from the OutputSpec is used as fallback.'
          displayName: Kafka Brokers
          path: outputs[0].kafka.brokers
        - description: "Topic specifies the target topic to send logs to. The value when not specified is 'topic'\n\nThe Topic can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Kafka Topic
          path: outputs[0].kafka.topic
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].kafka.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].kafka.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].kafka.tuning.deliveryMode
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].kafka.tuning.maxWrite
        - description: 'URL to send log records to.


            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].kafka.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Grafana Loki
          path: outputs[0].loki
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].loki.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].loki.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].loki.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].loki.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].loki.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].loki.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].loki.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].loki.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].loki.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.


            If LabelKeys is not set, the default keys are:


            - log_type


            - kubernetes.container_name


            - kubernetes.namespace_name


            - kubernetes.pod_name


            One additional label "kubernetes_host" is not part of the label keys configuration. It contains the hostname

            where the collector is running and is always present.


            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.


            For example the default log record keys translate to these Loki labels:


            - log_type


            - kubernetes_container_name


            - kubernetes_namespace_name


            - kubernetes_pod_name


            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].loki.labelKeys
        - description: "TenantKey is the tenant for the logs. This supports vector's template syntax to allow dynamic per-event values.\n\nThe TenantKey can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Tenant Key
          path: outputs[0].loki.tenantKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].loki.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].loki.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].loki.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].loki.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].loki.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].loki.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].loki.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: LokiStack
          path: outputs[0].lokiStack
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].lokiStack.authentication
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].lokiStack.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].lokiStack.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].lokiStack.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].lokiStack.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].lokiStack.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'LabelKeys can be used to customize which log record keys are mapped to Loki stream labels.


            Note: Loki label names must match the regular expression "[a-zA-Z_:][a-zA-Z0-9_:]*"

            Log record keys may contain characters like "." and "/" that are not allowed in Loki labels.

            Log record keys are translated to Loki labels by replacing any illegal characters with ''_''.


            For example the default log record keys translate to these Loki labels:


            - log_type


            - kubernetes_container_name


            - kubernetes_namespace_name


            - kubernetes_pod_name


            Note: the set of labels should be small, Loki imposes limits on the size and number of labels allowed.

            See https://grafana.com/docs/loki/latest/configuration/#limits_config for more.

            Loki queries can also query based on any log record field (not just labels) using query filters.'
          displayName: Stream Label Configuration
          path: outputs[0].lokiStack.labelKeys
        - description: Application contains the label keys configuration for the "application" tenant.
          displayName: Application Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.application
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.application.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.application.labelKeys
        - description: Audit contains the label keys configuration for the "audit" tenant.
          displayName: Audit Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.audit
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.audit.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.audit.labelKeys
        - description: "Global contains a list of record keys which are used for all tenants.\n\nIf LabelKeys is not set, the default keys are:\n\n - log_type\n\n - kubernetes.container_name\n\n - kubernetes.namespace_name\n\n - kubernetes.pod_name\n\nOne additional label \"kubernetes_host\" is not part of the label keys configuration. It contains the hostname\nwhere the collector is running and is always present."
          displayName: Global Configuration
          path: outputs[0].lokiStack.labelKeys.global
        - description: Infrastructure contains the label keys configuration for the "infrastructure" tenant.
          displayName: Infrastructure Tenant Configuration
          path: outputs[0].lokiStack.labelKeys.infrastructure
        - description: 'If IgnoreGlobal is true, then the tenant will not use the labels configured in the Global section of the label

            keys configuration.'
          displayName: Ignore Global Settings
          path: outputs[0].lokiStack.labelKeys.infrastructure.ignoreGlobal
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: 'LabelKeys contains a list of log record keys that are mapped to Loki stream labels.


            By default, this list is combined with the labels specified in the Global configuration.

            This behavior can be changed by setting IgnoreGlobal to true.'
          displayName: Label Keys
          path: outputs[0].lokiStack.labelKeys.infrastructure.labelKeys
        - description: Target points to the LokiStack resources that should be used as a target for the output.
          displayName: Target LokiStack Reference
          path: outputs[0].lokiStack.target
        - description: Name of the in-cluster LokiStack resource.
          displayName: LokiStack Name
          path: outputs[0].lokiStack.target.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Namespace of the in-cluster LokiStack resource.


            If unset, this defaults to "openshift-logging".'
          displayName: LokiStack Namespace
          path: outputs[0].lokiStack.target.namespace
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].lokiStack.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].lokiStack.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].lokiStack.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].lokiStack.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].lokiStack.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].lokiStack.tuning.minRetryDuration
        - description: Name used to refer to the output from a `pipeline`.
          displayName: Output Name
          path: outputs[0].name
        - displayName: OpenTelemetry Output
          path: outputs[0].otlp
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].otlp.authentication
        - description: Password to use for authenticating requests.
          displayName: Password
          path: outputs[0].otlp.authentication.password
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.password.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.password.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Token specifies a bearer token to be used for authenticating requests.
          displayName: Bearer Token
          path: outputs[0].otlp.authentication.token
        - description: From is the source from where to find the token
          displayName: Token Source
          path: outputs[0].otlp.authentication.token.from
        - description: Use Secret if the value should be sourced from a Secret in the same namespace.
          displayName: Token Secret
          path: outputs[0].otlp.authentication.token.secret
        - description: Name of the key used to get the value from the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.token.secret.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of secret
          displayName: Secret Name
          path: outputs[0].otlp.authentication.token.secret.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Username to use for authenticating requests.
          displayName: Username
          path: outputs[0].otlp.authentication.username
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].otlp.authentication.username.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].otlp.authentication.username.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].otlp.tuning
        - description: 'Compression causes data to be compressed before sending over the network.

            It is an error if the compression type is not supported by the output.'
          displayName: Compression
          path: outputs[0].otlp.tuning.compression
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Delivery Mode
          path: outputs[0].otlp.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].otlp.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].otlp.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].otlp.tuning.minRetryDuration
        - description: 'URL to send log records to.


            An absolute URL, with a valid http scheme. Must terminate with `/v1/logs`


            Basic TLS is enabled if the URL scheme requires it (for example ''https'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].otlp.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'Limit imposes a limit in records-per-second on the total aggregate rate of logs forwarded

            to this output from any given collector container. The total log flow from an individual collector

            container to this output cannot exceed the limit.  Generally, one collector is deployed per cluster node

            Logs may be dropped to enforce the limit. Missing or 0 means no rate limit.'
          displayName: Rate Limiting
          path: outputs[0].rateLimit
        - description: 'MaxRecordsPerSecond is the maximum number of log records

            allowed per input/output in a pipeline'
          displayName: Max Records Per Second
          path: outputs[0].rateLimit.maxRecordsPerSecond
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:number
        - displayName: Splunk
          path: outputs[0].splunk
        - description: Authentication sets credentials for authenticating the requests.
          displayName: Authentication Options
          path: outputs[0].splunk.authentication
        - description: Token points to the secret containing the Splunk HEC token used for authenticating requests.
          displayName: Splunk HEC Token
          path: outputs[0].splunk.authentication.token
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].splunk.authentication.token.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].splunk.authentication.token.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Index is the index for the logs. This supports template syntax to allow dynamic per-event values.\n\nThe Index can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}"
          displayName: Index
          path: outputs[0].splunk.index
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].splunk.tuning
        - description: Compression causes data to be compressed before sending over the network.
          displayName: Compression
          path: outputs[0].splunk.tuning.compression
        - displayName: Delivery Mode
          path: outputs[0].splunk.tuning.deliveryMode
        - description: MaxRetryDuration is the maximum time to wait between retry attempts after a delivery failure.
          displayName: Maximum Retry Duration
          path: outputs[0].splunk.tuning.maxRetryDuration
        - description: MaxWrite limits the maximum payload in terms of bytes of a single "send" to the output.
          displayName: Batch Size
          path: outputs[0].splunk.tuning.maxWrite
        - description: MinRetryDuration is the minimum time to wait between attempts to retry after delivery a failure.
          displayName: Minimum Retry Duration
          path: outputs[0].splunk.tuning.minRetryDuration
        - description: 'URL to send log records to.

            Basic TLS is enabled if the URL scheme requires it (for example ''https'' or ''tls'').

            The ''username@password'' part of `url` is ignored.'
          displayName: Destination URL
          path: outputs[0].splunk.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Syslog configures forwarding log events to a receiver using the syslog protocol
          displayName: Syslog Output
          path: outputs[0].syslog
        - description: "AppName is APP-NAME part of the syslog-msg header.\n\nAppName needs to be specified if using rfc5424. The maximum length of the final values is truncated to 48\nThis supports template syntax to allow dynamic per-event values.\n\nThe AppName can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n"
          displayName: App Name
          path: outputs[0].syslog.appName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Enrichment is an additional modification the log message before forwarding it to the receiver
          displayName: Enrichment Type
          path: outputs[0].syslog.enrichment
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "Facility to set on outgoing syslog records.\n\nFacility values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1.\n\nThe value can be a decimal integer. Facility keywords are not standardized,\nthis API recognizes at least the following case-insensitive keywords\n(defined by https://en.wikipedia.org/wiki/Syslog#Facility_Levels):\n\n    kernel user mail daemon auth syslog lpr news\n    uucp cron authpriv ftp ntp security console solaris-cron\n    local0 local1 local2 local3 local4 local5 local6 local7\n\nThe Facility can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. {.foo||\"user\"}"
          displayName: Facility
          path: outputs[0].syslog.facility
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "MsgId is MSGID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\nThe MsgId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\nMsgId needs to be specified if using rfc5424.  The maximum length of the final values is truncated to 32"
          displayName: MSGID
          path: outputs[0].syslog.msgId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "PayloadKey specifies record field to use as payload. This supports template syntax to allow dynamic per-event values.\n\nThe PayloadKey must be a single field path encased in single curly brackets `{}`.\n\nField paths must only contain alphanumeric and underscores. Any field with other characters must be quoted.\n\nIf left empty, Syslog will use the whole message as the payload key\n\nExample:\n\n 1. {.bar}\n\n 2. {.foo.bar.baz}\n\n 3. {.foo.bar.\"baz/with/slashes\"}"
          displayName: Payload Key
          path: outputs[0].syslog.payloadKey
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: "ProcId is PROCID part of the syslog-msg header. This supports template syntax to allow dynamic per-event values.\n\nThe ProcId can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\n\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. foo-{.bar||\"none\"}\n\n 2. {.foo||.bar||\"missing\"}\n\n 3. foo.{.bar.baz||.qux.quux.corge||.grault||\"nil\"}-waldo.fred{.plugh||\"none\"}\n\nProcId needs to be specified if using rfc5424. The maximum length of the final values is truncated to 128"
          displayName: PROCID
          path: outputs[0].syslog.procId
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - displayName: Syslog RFC
          path: outputs[0].syslog.rfc
        - description: "Severity to set on outgoing syslog records.\n\nSeverity values are defined in https://tools.ietf.org/html/rfc5424#section-6.2.1\n\nThe value can be a decimal integer or one of these case-insensitive keywords:\n\n    Emergency Alert Critical Error Warning Notice Informational Debug\n\nThe Severity can be a combination of static and dynamic values consisting of field paths followed by `||` followed by another field path or a static value.\nA dynamic value is encased in single curly brackets `{}` and MUST end with a static fallback value separated with `||`.\n\nStatic values can only contain alphanumeric characters along with dashes, underscores, dots and forward slashes.\n\nExample:\n\n 1. {.foo||\"Error\"}"
          displayName: Severity
          path: outputs[0].syslog.severity
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Tuning specs tuning for the output
          displayName: Tuning Options
          path: outputs[0].syslog.tuning
        - displayName: Delivery Mode
          path: outputs[0].syslog.tuning.deliveryMode
        - description: "An absolute URL, with a scheme. Valid schemes are: `tcp`, `tls`, `udp` and `udps`\nFor example, to send syslog records using secure UDP:\n    url: udps://syslog.example.com:1234"
          displayName: Destination URL
          path: outputs[0].syslog.url
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLS contains settings for controlling options on TLS client connections.
          displayName: TLS Options
          path: outputs[0].tls
        - description: CA can be used to specify a custom list of trusted certificate authorities.
          displayName: Certificate Authority Bundle
          path: outputs[0].tls.ca
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.ca.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.ca.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.ca.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Certificate points to the server certificate to use.
          displayName: Certificate
          path: outputs[0].tls.certificate
        - description: ConfigMapName contains the name of the ConfigMap containing the referenced value.
          displayName: ConfigMap Name
          path: outputs[0].tls.certificate.configMapName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: Name of the key used to get the value in either the referenced ConfigMap or Secret.
          displayName: Key Name
          path: outputs[0].tls.certificate.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.certificate.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: 'If InsecureSkipVerify is true, then the TLS client will be configured to skip validating server certificates.


            This option is *not* recommended for production configurations.'
          displayName: Skip Certificate Validation
          path: outputs[0].tls.insecureSkipVerify
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:booleanSwitch
        - description: Key points to the private key of the server certificate.
          displayName: Certificate Key
          path: outputs[0].tls.key
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.key.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.key.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: KeyPassphrase points to the passphrase used to unlock the private key.
          displayName: Certificate Key Passphrase
          path: outputs[0].tls.keyPassphrase
        - description: Key contains the name of the key inside the referenced Secret.
          displayName: Key Name
          path: outputs[0].tls.keyPassphrase.key
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: SecretName contains the name of the Secret containing the referenced value.
          displayName: Secret Name
          path: outputs[0].tls.keyPassphrase.secretName
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: TLSSecurityProfile is the security profile to apply to the output connection.
          displayName: TLS Security Profile
          path: outputs[0].tls.securityProfile
        - description: Type of output sink.
          displayName: Output Type
          path: outputs[0].type
        - description: Pipelines forward the messages selected by a set of inputs to a set of outputs.
          displayName: Log Forwarder Pipelines
          path: pipelines
        - description: 'Filters lists the names of filters to be applied to records going through this pipeline.


            Each filter is applied in order.

            If a filter drops a records, subsequent filters are not applied.'
          displayName: Filters
          path: pipelines[0].filterRefs
        - description: "InputRefs lists the names (`input.name`) of inputs to this pipeline.\n\nThe following built-in input names are always available:\n\n - `application` selects all logs from application pods.\n\n - `infrastructure` selects logs from openshift and kubernetes pods and some node logs.\n\n - `audit` selects node logs related to security audits."
          displayName: Inputs
          path: pipelines[0].inputRefs
        - description: Name of the pipeline
          displayName: Name
          path: pipelines[0].name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        - description: OutputRefs lists the names (`output.name`) of outputs from this pipeline.
          displayName: Outputs
          path: pipelines[0].outputRefs
        - description: ServiceAccount points to the ServiceAccount resource used by the collector pods.
          displayName: Service Account
          path: serviceAccount
        - description: Name of the ServiceAccount to use to deploy the Forwarder.  The ServiceAccount is created by the administrator
          displayName: ServiceAccount Name
          path: serviceAccount.name
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:text
        statusDescriptors:
        - description: Conditions of the log forwarder.
          displayName: Forwarder Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: FilterConditions maps filter name to condition of the filter.
          displayName: Filter Conditions
          path: filterConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: InputConditions maps input name to condition of the input.
          displayName: Input Conditions
          path: inputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: OutputConditions maps output name to condition of the output.
          displayName: Output Conditions
          path: outputConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        - description: PipelineConditions maps pipeline name to condition of the pipeline.
          displayName: Pipeline Conditions
          path: pipelineConditions
          x-descriptors:
          - urn:alm:descriptor:io.kubernetes.conditions
        version: v1
      - description: A Log File Metric Exporter instance. LogFileMetricExporter is the Schema for the logFileMetricExporters API
        displayName: Log File Metric Exporter
        kind: LogFileMetricExporter
        name: logfilemetricexporters.logging.openshift.io
        resources:
        - kind: DaemonSet
          name: ''
          version: v1
        - kind: Pod
          name: ''
          version: v1
        - kind: Service
          name: ''
          version: v1
        - kind: ServiceMonitor
          name: ''
          version: v1
        specDescriptors:
        - description: Define which Nodes the Pods are scheduled on.
          displayName: LogFileMetricExporter Node Selector
          path: nodeSelector
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:ConfigMap
        - description: The resource requirements for the LogFileMetricExporter
          displayName: LogFileMetricExporter Resource Requirements
          path: resources
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:resourceRequirements
        - description: Define the tolerations the Pods will accept
          displayName: LogFileMetricExporter Pod Tolerations
          path: tolerations
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:selector:core:v1:Toleration
        statusDescriptors:
        - description: Conditions of the Log File Metrics Exporter.
          displayName: Log File Metrics Exporter Conditions
          path: conditions
          x-descriptors:
          - urn:alm:descriptor:com.tectonic.ui:logFileMetricsExporterConditions
        version: v1alpha1
    description: '# Red Hat OpenShift Logging

      The Red Hat OpenShift Logging Operator orchestrates log collection and forwarding to Red Had managed log stores and

      other third-party receivers.


      ##Features

      * **Create/Destroy**: Deploy log collectors and forwarders to support observability of OCP cluster.

      * **Simplified Configuration**: Spec collectors using a simplified API to configure log collection from opinionated sources to one or more third-party receivers.


      ## Prerequisites and Requirements

      ### Red Hat OpenShift Logging Namespace

      It is recommended to deploy the Red Hat OpenShift Logging Operator to the **openshift-logging** namespace. This namespace

      must be explicitly created by a cluster administrator (e.g. `oc create ns openshift-logging`). To enable metrics

      service discovery add namespace label `openshift.io/cluster-monitoring: "true"`.


      For additional installation documentation see [Deploying cluster logging](https://docs.openshift.com/container-platform/latest/logging/cluster-logging-deploying.html)

      in the OpenShift product documentation.'
    displayName: Red Hat OpenShift Logging
    installModes:
    - supported: true
      type: OwnNamespace
    - supported: true
      type: SingleNamespace
    - supported: true
      type: MultiNamespace
    - supported: true
      type: AllNamespaces
    keywords:
    - logging
    - observability
    - loki
    - vector
    - otlp
    labels:
      operatorframework.io/arch.amd64: supported
      operatorframework.io/arch.arm64: supported
      operatorframework.io/arch.ppc64le: supported
      operatorframework.io/arch.s390x: supported
    links:
    - name: Documentation
      url: https://github.com/openshift/cluster-logging-operator/blob/master/README.adoc
    - name: Red Hat OpenShift Logging Operator
      url: https://github.com/openshift/cluster-logging-operator
    - name: Vector
      url: https://vector.dev/
    minKubeVersion: 1.18.3
    provider:
      name: Red Hat
relatedImages:
- image: registry.redhat.io/openshift-logging/cluster-logging-operator-bundle@sha256:98ccff244568d2e388e2f8c270d2ce47515b8254754b43b2deefd63bdd4dd19b
  name: ''
- image: registry.redhat.io/openshift-logging/cluster-logging-rhel9-operator@sha256:9563279ac943e6cecef10877d9298146bed6571bcef846574e06752cd3a7fac9
  name: cluster-logging-rhel9-operator
- image: registry.redhat.io/openshift-logging/log-file-metric-exporter-rhel9@sha256:7a2d5e9e72c6b80a857207643886197be6f37e3ea2b1ad848c951a4f0d5f654f
  name: log-file-metric-exporter-rhel9
- image: registry.redhat.io/openshift-logging/vector-rhel9@sha256:b420e2d5aeb604d5d32e4095595c90dc749b7b2a09d2fac46fa44be04e7c449b
  name: vector-rhel9
schema: olm.bundle
